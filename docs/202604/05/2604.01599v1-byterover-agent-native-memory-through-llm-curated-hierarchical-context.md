---
title: "ByteRover: Agent-Native Memory Through LLM-Curated Hierarchical Context"
title_zh: ByteRover：通过大语言模型策划的层级化上下文实现智能体原生记忆
authors: "Andy Nguyen, Danh Doan, Hoang Pham, Bao Ha, Dat Pham, Linh Nguyen, Hieu Nguyen, Thien Nguyen, Cuong Do, Phat Nguyen, Toan Nguyen"
date: 2026-04-02
pdf: "https://arxiv.org/pdf/2604.01599v1"
tags: ["query:agent"]
score: 9.0
evidence: 面向LLM推理的智能体原生存储架构
tldr: ByteRover 提出了一种 Agent 原生记忆架构，旨在解决传统记忆增强生成中外部存储与智能体推理脱节导致的语义漂移问题。该架构摒弃了向量数据库，由 LLM 直接管理分层上下文树（Context Tree），通过自适应知识生命周期和五级渐进式检索实现高效知识维护。系统仅依赖本地 Markdown 文件存储，在 LoCoMo 等基准测试中达到 SOTA 性能，实现了低延迟、高可解释性的长上下文推理。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.01599v1/fig-001.webp\", \"caption\": \"Table 4: LLM-as-Judge accuracy (%) on LongMemEval-S (500 questions). KU = Knowledge Update, SSU = Single-Session User, SSA =Single-Session Assistant, SSP= Single-Session Preference, TR =Temporal Reasoning, MS= Multi-Session. Hindsight and HonCho evaluated with our harness (Gemini 3 Flash judge). †Results from respective papers; backbone and judge configurations differ.\", \"page\": 11, \"index\": 1, \"width\": 736, \"height\": 290}, {\"url\": \"assets/figures/arxiv/2604.01599v1/fig-002.webp\", \"caption\": \"Table 3: LLM-as-Judge accuracy (%) on LoCoMo (4 categories, 1,982 questions). All systems evaluated under identical conditions using our benchmark harness with Gemini 3 Flash as the judge.\", \"page\": 10, \"index\": 2, \"width\": 876, \"height\": 238}, {\"url\": \"assets/figures/arxiv/2604.01599v1/fig-003.webp\", \"caption\": \"Table 7: Hyperparameter configuration for ByteRover and evaluation pipeline.\", \"page\": 18, \"index\": 3, \"width\": 836, \"height\": 939}, {\"url\": \"assets/figures/arxiv/2604.01599v1/fig-004.webp\", \"caption\": \"Figure 2: The 5-tier progressive retrieval pipeline. Search is initiated in parallel with fingerprint computation. Tiers 0–1 resolve from cache without awaiting search. Tier 2 serves high-confidence results directly from MiniSearch. Only novel or ambiguous queries escalate to Tier 3 (single optimized LLM call with pre-fetched context) or Tier 4 (full agentic loop with tool access). Approximate latencies shown on right.\", \"page\": 8, \"index\": 4, \"width\": 640, \"height\": 458}, {\"url\": \"assets/figures/arxiv/2604.01599v1/fig-005.webp\", \"caption\": \"Figure 3: A complete knowledge entry in the Context Tree, showing the YAML frontmatter with lifecycle metadata, explicit relation annotations, raw concept (provenance), and narrative (interpreted structure).\", \"page\": 19, \"index\": 5, \"width\": 979, \"height\": 990}, {\"url\": \"assets/figures/arxiv/2604.01599v1/fig-006.webp\", \"caption\": \"Figure 1: Architectural overview of ByteRover. Clients (TUI, CLI, MCP) connect via Socket.IO to a daemon that manages a per-project task queue and agent pool. Each agent process contains three logical layers: (1) an Agent Layer where curate and search_knowledge are first-class tools in the LLM’s reasoning loop; (2) an Execution Layer with a query executor for 5-tier progressive retrieval and a sandboxed curation environment; and (3) a Knowledge Layer with the Context Tree, BM25 full-text index, and query cache, all backed by the local filesystem with no external infrastructure.\", \"page\": 5, \"index\": 6, \"width\": 511, \"height\": 495}]"
motivation: 传统记忆系统将存储与推理分离，导致语义漂移、协作上下文丢失以及复杂流水线带来的维护困难。
method: 采用由 LLM 驱动的分层上下文树结构，结合自适应知识生命周期管理和五级渐进式检索策略，将知识以结构化 Markdown 形式存储于本地。
result: 在 LoCoMo 基准测试中取得 SOTA 成绩，在 LongMemEval 上表现优异，且实现了亚 100 毫秒的检索延迟，无需任何外部数据库基础设施。
conclusion: ByteRover 证明了通过 Agent 原生、文件化的分层记忆管理，可以在显著降低系统复杂性的同时，提升长上下文推理的准确性与效率。
---

## 摘要
记忆增强生成 (MAG) 通过外部记忆扩展大语言模型以支持长上下文推理，但现有方法普遍将记忆视为智能体调用的外部服务，将存储委托给由分块、嵌入和图提取组成的独立流水线。这种架构上的分离意味着存储知识的系统并不理解知识，导致智能体意图记忆的内容与流水线实际捕获的内容之间出现语义漂移、多智能体协作上下文丢失以及故障后恢复脆弱等问题。在本文中，我们提出了 ByteRover，这是一种反转了记忆流水线的智能体原生记忆架构：负责任务推理的同一个大语言模型 (LLM) 同时负责知识的策划、结构化和检索。ByteRover 将知识表示为层级化上下文树 (Context Tree)，这是一种基于文件的知识图谱，按领域 (Domain)、主题 (Topic)、子主题 (Subtopic) 和条目 (Entry) 进行组织，其中每个条目都带有显式关系、来源以及包含重要性评分、成熟度层级和近期衰减的自适应知识生命周期 (AKL)。检索采用五层渐进式策略，在无需 LLM 调用的情况下，能以低于 100 毫秒的延迟解决大多数查询，仅针对新颖问题才升级到智能体推理。在 LoCoMo 和 LongMemEval 上的实验表明，ByteRover 在 LoCoMo 上达到了最先进的准确率，并在 LongMemEval 上取得了具有竞争力的结果，同时无需任何外部基础设施、向量数据库、图数据库或嵌入服务，所有知识均以人类可读的 Markdown 文件形式存储在本地文件系统中。

## Abstract
Memory-Augmented Generation (MAG) extends large language models with external memory to support long-context reasoning, but existing approaches universally treat memory as an external service that agents call into, delegating storage to separate pipelines of chunking, embedding, and graph extraction. This architectural separation means the system that stores knowledge does not understand it, leading to semantic drift between what the agent intended to remember and what the pipeline actually captured, loss of coordination context across agents, and fragile recovery after failures. In this paper, we propose ByteRover, an agent-native memory architecture that inverts the memory pipeline: the same LLM that reasons about a task also curates, structures, and retrieves knowledge. ByteRover represents knowledge in a hierarchical Context Tree, a file-based knowledge graph organized as Domain, Topic, Subtopic, and Entry, where each entry carries explicit relations, provenance, and an Adaptive Knowledge Lifecycle (AKL) with importance scoring, maturity tiers, and recency decay. Retrieval uses a 5-tier progressive strategy that resolves most queries at sub-100 ms latency without LLM calls, escalating to agentic reasoning only for novel questions. Experiments on LoCoMo and LongMemEval demonstrate that ByteRover achieves state-of-the-art accuracy on LoCoMo and competitive results on LongMemEval while requiring zero external infrastructure, no vector database, no graph database, no embedding service, with all knowledge stored as human-readable markdown files on the local filesystem.

---

## 论文详细总结（自动生成）

### ByteRover: 通过大语言模型策划的层级化上下文实现智能体原生记忆

#### 1. 核心问题与整体含义（研究动机和背景）
大语言模型（LLM）在长短期记忆维持上存在局限，现有的记忆增强生成（MAG）系统普遍采用“外部服务模式”：智能体将数据发送给独立的流水线（分块、嵌入、向量存储），检索时再调用。这种架构导致了三个核心问题：
*   **语义漂移**：存储流水线（如机械分块）不理解智能体的推理意图，导致检索内容与需求不符。
*   **协作上下文丢失**：多智能体共享数据但无法共享背后的推理逻辑和来源（Provenance）。
*   **恢复脆弱性**：系统崩溃后，智能体难以通过碎片化的向量数据重建复杂的任务状态。
**ByteRover** 的核心动机是实现“智能体原生记忆”，让负责推理的同一个 LLM 直接负责知识的策划、结构化和维护，消除存储与理解之间的鸿沟。

#### 2. 方法论：核心思想与关键技术
ByteRover 颠覆了传统 RAG 架构，其核心思想是**由 LLM 驱动的层级化文件存储**。
*   **层级化上下文树 (Context Tree)**：知识按 `领域 > 主题 > 子主题 > 条目` 组织，以人类可读的 Markdown 文件存储在本地。每个条目包含显式关系（@引用）、来源（任务、作者、时间戳）和叙述性结构。
*   **自适应知识生命周期 (AKL)**：引入重要性评分（随访问增加、随时间衰减）和成熟度层级（草稿 → 已验证 → 核心）。通过公式 `Score = wr·BM25 + wι·重要性 + wt·近期性` 动态管理知识。
*   **五级渐进式检索策略**：
    1.  **Tier 0/1**：精确/模糊缓存命中（~0-50ms）。
    2.  **Tier 2**：高性能全文搜索（MiniSearch BM25，~100ms）。
    3.  **Tier 3**：单次优化 LLM 调用（预取上下文，<5s）。
    4.  **Tier 4**：全智能体推理循环（多轮工具调用导航知识树，8-15s）。
*   **原子化操作与反馈**：提供 ADD、UPDATE、UPSERT、MERGE、DELETE 五种工具。智能体能看到每步操作的成功/失败反馈，实现闭环纠错。

#### 3. 实验设计
*   **数据集/场景**：
    *   **LoCoMo**：超长对话基准（平均 20k token），测试长程时间与因果检索。
    *   **LongMemEval-S**：大规模记忆基准（上下文 > 100k token），涵盖知识更新、多会话推理等 6 个维度。
*   **对比方法**：包括 Mem0、Zep、Hindsight、HonCho、Memobase、OpenAI Memory (ChatGPT) 以及 Chronos、SmartSearch 等 SOTA 系统。
*   **评估方式**：采用 LLM-as-a-Judge（使用 Gemini 3 Flash 作为裁判），确保语义层面的评分客观性。

#### 4. 资源与算力
*   论文**未明确说明**具体的 GPU 型号、数量或训练时长。
*   由于 ByteRover 强调“零外部基础设施”（无需向量/图数据库），其运行开销主要集中在推理侧的 API 调用（实验中使用了 Gemini 系列模型）。
*   其存储完全基于本地文件系统，检索层（Tier 0-2）在 CPU 上即可实现亚 100ms 响应。

#### 5. 实验数量与充分性
*   **实验规模**：在 LoCoMo 上测试了 1,982 个问题，在 LongMemEval-S 上测试了 500 个问题。
*   **消融实验**：针对“渐进式检索”、“域外（OOD）检测”和“关系图谱”进行了专项消融，证明了分层架构对准确率的巨大提升（禁用后准确率下降 29.4%）。
*   **充分性评价**：实验覆盖了从单步到多步、从时间推理到知识更新的多种复杂场景。对比对象包含了当前主流的开源和商业记忆系统，实验设计较为客观、公平。

#### 6. 主要结论与发现
*   **性能领先**：ByteRover 在 LoCoMo 上达到 96.1% 的准确率（SOTA），在 LongMemEval-S 上达到 92.8%，优于绝大多数基于向量或图数据库的系统。
*   **结构化优势**：在多跳（Multi-hop）和时间推理任务上表现尤为突出，证明了显式层级关系比隐式向量相似度更有效。
*   **效率平衡**：通过五级检索，系统在保持高精度的同时，将中位检索延迟控制在 1.6s 左右，且大部分简单查询无需 LLM 参与。

#### 7. 优点与亮点
*   **架构创新**：提出“智能体原生”概念，让 LLM 像人类管理笔记一样管理记忆，极大地提高了可解释性和可维护性。
*   **极简部署**：无需安装向量数据库或嵌入服务，仅靠 Markdown 文件即可运行，具有极高的便携性和版本控制友好性。
*   **鲁棒性**：通过状态反馈循环和原子化写入，解决了智能体在复杂任务中的状态丢失和写入冲突问题。

#### 8. 不足与局限
*   **写入成本高**：知识策划（Curation）过程需要 LLM 进行推理和总结，比简单的向量分块更慢且 API 成本更高。
*   **模型依赖性**：系统性能高度依赖底层 LLM 的推理能力，若使用较弱的开源模型，可能会出现格式错误或策划质量下降。
*   **扩展性瓶颈**：基于本地文件和内存索引（MiniSearch）的设计在处理超大规模（如超过 10 万个条目）知识库时，可能面临性能下降，需引入分片机制。
*   **冷启动延迟**：对于缓存未命中的新颖查询，依赖多轮智能体循环会导致响应时间显著增加。

（完）
