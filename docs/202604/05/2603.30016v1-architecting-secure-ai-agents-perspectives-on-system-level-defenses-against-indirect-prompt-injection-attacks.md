---
title: "Architecting Secure AI Agents: Perspectives on System-Level Defenses Against Indirect Prompt Injection Attacks"
title_zh: 构建安全的 AI 智能体：关于针对间接提示注入攻击的系统级防御的视角
authors: "Chong Xiang, Drew Zagieboylo, Shaona Ghosh, Sanjay Kariyappa, Kai Greshake, Hanshen Xiao, Chaowei Xiao, G. Edward Suh"
date: 2026-03-31
pdf: "https://arxiv.org/pdf/2603.30016v1"
tags: ["query:agent"]
score: 9.0
evidence: 针对大语言模型驱动的AI智能体的系统级防御，防止提示注入攻击
tldr: 针对大语言模型驱动的AI智能体易受间接提示注入攻击的问题，本文提出了一套系统级防御愿景。研究强调了动态任务中安全策略更新的必要性，主张在严格约束下利用模型进行安全决策，并提出将个性化与人机交互作为解决歧义的核心。该研究为构建更安全、受控的智能体系统架构提供了理论指导和设计思路。
source: arxiv
selection_source: fresh_fetch
motivation: 现有的AI智能体在处理不可信数据时，极易受到恶意指令诱导（间接提示注入），导致执行危险操作。
method: 提出结合动态重规划、受限的模型决策机制以及人机交互协作的系统级防御框架。
result: 明确了系统级防御在结构化控制智能体行为、整合规则与模型检查以及提升研究针对性方面的核心价值。
conclusion: 仅靠模型鲁棒性不足以应对安全挑战，必须通过系统架构层面的设计来确保AI智能体的安全性和实用性。
---

## 摘要
AI 智能体（主要由大语言模型 (LLM) 驱动）容易受到间接提示注入攻击，即嵌入在不可信数据中的恶意指令可能会触发危险的智能体行为。本立场论文讨论了我们对针对间接提示注入攻击的系统级防御的愿景。我们阐述了三个立场：(1) 对于动态任务和现实环境，动态重新规划和安全策略更新通常是必要的；(2) 某些依赖上下文的安全决策仍需要 LLM（或其他学习模型），但应仅在严格限制模型观察和决策范围的系统设计中进行；(3) 在本质上存在歧义的情况下，个性化和人类交互应被视为核心设计考量。除了主要立场外，我们还讨论了现有基准测试的局限性，这些局限性可能会产生效用和安全性的错觉。我们还强调了系统级防御的价值，它们通过结构化和控制智能体行为、集成基于规则和基于模型的安全检查，以及支持针对模型鲁棒性和人类交互的更具针对性的研究，构成了智能体系统的骨架。

## Abstract
AI agents, predominantly powered by large language models (LLMs), are vulnerable to indirect prompt injection, in which malicious instructions embedded in untrusted data can trigger dangerous agent actions. This position paper discusses our vision for system-level defenses against indirect prompt injection attacks. We articulate three positions: (1) dynamic replanning and security policy updates are often necessary for dynamic tasks and realistic environments; (2) certain context-dependent security decisions would still require LLMs (or other learned models), but should only be made within system designs that strictly constrain what the model can observe and decide; (3) in inherently ambiguous cases, personalization and human interaction should be treated as core design considerations. In addition to our main positions, we discuss limitations of existing benchmarks that can create a false sense of utility and security. We also highlight the value of system-level defenses, which serve as the skeleton of agentic systems by structuring and controlling agent behaviors, integrating rule-based and model-based security checks, and enabling more targeted research on model robustness and human interaction.

---

## 论文详细总结（自动生成）

这篇论文是由 NVIDIA、约翰霍普金斯大学等机构的研究人员撰写的**立场论文（Position Paper）**，旨在探讨如何通过系统架构设计来防御针对 AI 智能体（Agents）的**间接提示注入（Indirect Prompt Injection, IPI）**攻击。

以下是对该论文的结构化深入总结：

### 1. 论文的核心问题与整体含义
*   **研究背景**：随着 LLM 驱动的智能体被授予更高的自主权（如调用工具、访问外部数据），它们面临严重的间接提示注入风险。攻击者可以将恶意指令嵌入网页、邮件或第三方工具输出中，诱导智能体执行未经授权的操作（如转账、删除文件）。
*   **核心问题**：现有的模型级（微调）或文本级（提示词加强）防御难以在保证智能体“实用性（Utility）”的同时提供强有力的“安全性（Security）”。
*   **整体含义**：论文提出了一种**系统级防御（System-Level Defenses）**的愿景，主张将智能体视为一个完整的计算系统，通过结构化的计划（Plan）和策略（Policy）来约束行为，而不是仅仅依赖模型本身的鲁棒性。

### 2. 论文提出的方法论
论文提出了一个包含编排器（Orchestrator）、执行器（Executor）和策略执行器（Policy Enforcer）的通用架构，并阐述了三个核心立场：

*   **核心思想**：通过“计划”描述智能体能做什么，通过“策略”描述智能体被允许做什么。
*   **关键技术细节（三个立场）**：
    1.  **动态重规划与策略更新**：在现实动态环境中，静态计划容易因 API 变更或环境错误而失效。因此，系统必须允许智能体根据环境反馈动态更新计划和安全策略，但关键挑战在于如何区分“良性更新”与“恶意注入导致的更新”。
    2.  **受限的模型决策机制**：复杂的安全决策（如判断某个操作是否符合上下文语义）仍需 LLM 参与，但不能让 LLM 直接接触原始的、可能包含攻击代码的外部文本。应将外部反馈转化为**结构化构件**（如计划差异对比、类型化追踪），让 LLM 在严格受限的输入和决策范围内充当“法官”。
    3.  **个性化与人机交互**：对于本质上存在歧义的任务（如定义什么是“紧急邮件”）或高风险操作，必须引入人类干预（HITL）。
*   **具体方案示例**：
    *   **指令跟随意图分析**：将“识别指令”与“决定是否执行”解耦。让模型先口头复述它打算执行的指令，由系统根据来源（信任区 vs 不可信区）决定是否允许。
    *   **合成验证器**：利用 LLM 针对特定步骤生成程序化的验证代码，对环境返回的数据进行完整性检查。

### 3. 实验设计
*   **性质说明**：由于这是一篇立场论文，它**没有进行大规模的基准测试实验**，而是通过**案例研究（Case Studies）**和对现有研究的批判性分析来支撑论点。
*   **对比对象/基准**：
    *   论文分析并指出了现有基准测试（如 **AgentDojo**, **AgentDyn**, **InjecAgent**）的局限性。
    *   对比了现有的防御模式，如 **CaMeL**（计划-执行隔离）、**Progent**（可编程权限控制）、**AgentArmor**（信息流控制）等。
*   **场景示例**：使用了“账单 API 调用失败后的重规划”、“Slack 邀请新同事”、“GitHub Issue 修复”等具体场景来论证动态策略和语义歧义的重要性。

### 4. 资源与算力
*   **算力说明**：论文中**未明确说明**使用了多少算力或具体的 GPU 型号。
*   **原因**：本文侧重于架构设计和理论框架的构建，而非训练大型模型或进行大规模推理实验。

### 5. 实验数量与充分性
*   **实验形式**：论文主要通过逻辑推演和定性分析展开。
*   **充分性评价**：
    *   **理论充分性**：论文深入探讨了安全与实用性之间的权衡，逻辑严密。
    *   **客观性**：作者客观地承认了 LLM 作为安全法官本身也可能被攻击的风险，并提出了“受限输入”的对策。
    *   **局限性**：缺乏定量数据支持。虽然提出的架构在理论上更安全，但尚未在复杂的真实攻击场景下给出大规模的防御成功率统计。

### 6. 论文的主要结论与发现
*   **系统级防御是骨架**：系统级设计通过结构化行为、集成规则检查和模型检查，构成了智能体安全的骨架。
*   **协同设计是趋势**：未来的研究应侧重于“系统-模型-人类”的协同设计。系统负责缩小攻击面，模型负责语义推理，人类负责解决歧义。
*   **基准测试需进化**：现有的基准测试过于简单且缺乏自适应攻击者，容易产生“安全幻觉”。

### 7. 优点
*   **前瞻性**：在智能体自主性不断提升的背景下，及时提出了系统级防御的必要性。
*   **实用性导向**：不同于纯学术的安全性研究，本文高度关注如何不损害智能体处理复杂动态任务的能力。
*   **解耦思想**：提出的“指令识别与决策解耦”以及“结构化 diff 评审”是非常具有落地潜力的工程化思路。

### 8. 不足与局限
*   **缺乏实证数据**：作为立场论文，没有提供防御效果的量化指标（如 ASR 攻击成功率的下降幅度）。
*   **实施复杂度高**：动态更新策略和频繁的人机交互可能会显著增加系统的延迟和开发难度。
*   **对 LLM “法官”的依赖**：尽管提出了限制输入，但如果攻击者能通过极其隐蔽的语义诱导改变结构化输出，受限的 LLM 决策模块仍可能失效。

（完）
