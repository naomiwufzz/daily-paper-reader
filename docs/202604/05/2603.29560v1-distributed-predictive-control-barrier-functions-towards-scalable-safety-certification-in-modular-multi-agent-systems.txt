Title: Distributed Predictive Control Barrier Functions: Towards Scalable Safety Certification in Modular Multi-Agent Systems

URL Source: https://arxiv.org/pdf/2603.29560v1

Published Time: Wed, 01 Apr 2026 01:00:49 GMT

Number of Pages: 8

Markdown Content:
# Distributed Predictive Control Barrier Functions: Towards Scalable Safety Certification in Modular Multi-Agent Systems 

Jonas Ohnemus 1, Alexandre Didier 1, Ahmed Aboudonia 2, Andrea Carron 1, and Melanie N. Zeilinger 1

Abstract — We consider safety-critical multi-agent systems with distributed control architectures and potentially varying network topologies. While learning-based distributed control enables scalability and high performance, a lack of formal safety guarantees in the face of unforeseen disturbances and unsafe network topology changes may lead to system failure. To address this challenge, we introduce structured control barrier functions (s-CBFs) as a multi-agent safety framework. The s-CBFs are augmented to a distributed predictive control barrier function (D-PCBF) , a predictive, optimization-based safety layer that uses model predictions to guarantee recoverable safety at all times. The proposed approach enables a permissive yet formal plug-and-play protocol, allowing agents to join or leave the network while ensuring safety recovery if a change in network topology requires temporarily unsafe behavior. We validate the formulation through simulations and real-time experiments of a miniature race-car platoon. 

I. INTRODUCTION Modern robotic systems, such as connected autonomous vehicle fleets [1], aerial swarms [2], and collaborative indus-trial manipulators [3], are increasing in scale and complexity, necessitating appropriate control architectures [4]. As these systems scale, centralized control becomes computationally intractable, motivating a shift toward distributed architec-tures [5]. While data-driven methods such as multi-agent reinforcement learning have emerged as powerful tools for achieving high-level performance in these applications [6][7], the learned policies generally lack formal safety guarantees and are difficult to certify. As a result, these policies are unsuitable for direct deployment in safety-critical domains. To bridge the gap between safety and performance, safety filters offer a modular solution [8]. These filters minimally modify a potentially unsafe candidate input (e.g., from a learning-based controller) to ensure safety. By leveraging model predictions, predictive safety filters have successfully enabled aggressive maneuvers, by enforcing safety con-straints only when necessary [9]. Predictive safety filters, however, merely enforce positive invariance of the safe con-straint set, and the optimization may become infeasible due to disturbances that drive the state outside this safe set [10]. A common remedy is to relax constraints via soft-penalty formulations [11]. While this approach improves feasibility,           

> This work was supported by the Swiss National Science Foundation under grant agreement P500PT 230223.
> 1Institute for Dynamic Systems and Control, ETH Zurich, 8092 Zurich, Switzerland,

{johnemus, adidier, carrona, mzeilinger }@ethz.ch       

> 2University of California Berkeley, 94720 CA, USA

aboudonia@berkeley.edu Agent  Agent     

> Fig. 1. Network-level recoverable safety using s-CBFs. The formulation defines safe sets Sℓand regions of attraction DℓzSℓ, ensuring constraint satisfaction and network-level convergence even when individual trajectories temporarily have to violate safety.

it sacrifices formal guarantees. Soft-constrained methods lack a rigorous mechanism to ensure the system eventually returns to the safe set, possibly leading to indefinite constraint viola-tions. Recently, predictive control barrier functions (PCBFs) have been proposed in the single-agent setting to extend the feasible region while preserving both invariance (safety) and convergence (recovery) properties [12]. While PCBFs offer a robust solution for individual agents, maintaining these benefits in multi-agent systems with dis-tributed learning-based control architectures remains a sig-nificant challenge. Issues of infeasibility are further com-pounded by the modularity of modern multi-agent systems, in which subsystems may join, leave, or reconfigure during operation. Classical approaches address the challenge of varying topology by steering the system to a specific steady state before allowing reconfiguration [13]. However, this approach is time-consuming and diverts the system from its primary operation. On the other hand, existing distributed predictive safety filters [14], [15], [16] or controllers [17] may fail during modular network reconfiguration because the corresponding optimization problems become infeasible. These limitations highlight a need for recoverable safety: safety notions that not only ensure invariance of the safe set but also guarantee convergence back to it. To address this open challenge, we propose a novel PCBF-based approach for multi-agent systems. We address the safety-performance gap in multi-agent systems by developing a distributed predictive control barrier function (D-PCBF) framework. Our approach certifies both invariance and convergence using only local information exchange. The specific contributions are: 1) We introduce structured CBFs (s-CBFs) (see Fig. 1), a novel formulation that enables permissive global safety recovery using only local neighbor information. 2) We ef-fectively enlarge the offline computed safe set and region of attraction of the s-CBFs using an online D-PCBF distributed 

> arXiv:2603.29560v1 [eess.SY] 31 Mar 2026

optimization that serves as a less conservative safety and recovery certificate. 3) We design a Plug-and-Play (PnP) protocol that leverages the slack variables from the D-PCBF to certify the admission or removal of agents online without requiring the system to disrupt its operation. 4) We validate the framework in simulation and on a real-time miniature vehicle platoon , demonstrating that our D-PCBF successfully filters unsafe inputs from aggressive controllers and ensures collision-free recovery during high-speed plug-and-play maneuvers where existing approaches would fail. Section II details the general problem formulation. Sec-tion III introduces s-CBFs and defines the D-PCBF, before Section IV outlines the PnP protocol. After the synthesis of s-CBFs for linear systems is described in Section V-A, simulation and real-time experimental results are presented in Section V-B. Finally, Section VI concludes the paper. II. PROBLEM FORMULATION We consider multi-agent dynamical systems that can be decomposed into individual dynamically coupled subsystems (or agents). We denote the index set of subsystems by L

and the number of subsystems by L. The global nonlinear discrete-time dynamics are 

xpt ` 1q “ f pxptq, u ptqq , t P N0, (1) where x P Rn and u P Rm are the global state and input vectors, t denotes time and f : Rn ˆ Rm Ñ Rn

is a continuous function. We further assume that the local dynamics of the ℓ-th subsystem with ℓ P L are given by 

xℓpt ` 1q “ fℓ pxNℓ ptq, u ℓptqq @ℓ P L, (2) where xℓ P Rnℓ and uℓ P Rmℓ are the local state and input vectors, fℓ : RnNℓ ˆ Rmℓ Ñ Rnℓ is a continuous function, and the set Nℓ denotes the indices of neighboring subsystems including the ℓ-th subsystem itself, i.e., ℓ P Nℓ.The vector xNℓ “ col jPNℓ pxj q P RnNℓ denotes the con-catenation of neighbor states. The global state and input are the concatenation of all local states and inputs, respectively, i.e., x “ col ℓPLpxℓq and u “ col ℓPLpuℓq. We also assume that communication is bidirectional between neighboring subsystems and that the communication graph is connected. The ℓ-th subsystem is subject to the local constraints 

uℓ P Uℓ Ă Rmℓ , @ℓ P L,xℓ P Xℓ :“ t xℓ | cℓpxℓq ď 0u Ă Rnℓ , @ℓ P L,

where Uℓ and Xℓ are compact sets, and cℓ : Rnℓ Ñ Rnx,ℓ is a continuous function. The global constraint sets are given by X “ X1 ˆ . . . ˆ XL and U “ U1 ˆ . . . ˆ UL.We are interested in the recoverable safety of dynamical systems, which is formally defined by the existence of a safe set of states S Ď X that is control invariant and can be rendered attractive from a larger region DzS, where S Ă

D. The following definition characterizes the safe set as the sublevel set of a continuous function hp¨q .

Definition II.1 (Control barrier function (CBF) [12, Def. III.1]) . A function h : D Ñ R is a CBF for system (1) with safe set S “ t x P Rn | hpxq ď 0u Ď X where S Ă D if S

and D are non-empty and compact, h is continuous on D,and there exists a continuous function ∆h : D Ñ R with 

∆hpxq ą 0 for all x P DzS such that 

@x P DzS : inf  

> uPU

hpf px, u qq ´ hpxq ď ´ ∆hpxq,

@x P S : inf  

> uPU

hpf px, u qq ď 0.

CBFs thus encode recoverable safety: While hpxq ď 0

implies that the constraints X can be satisfied via the invari-ance of S Ď X , they also provide a quantitative violation measure whenever hpxq ą 0 and a recovery guarantee that the system converges to S from DzS [12, Theorem III.4]. While D is the domain of h, DzS is the region of attraction. Based on the safety requirements in Definition II.1, one can design a safety filter [8] that projects the input up proposed by any controller providing system performance onto the set of safe inputs. Formally, the safety filter is given by 

uptq “ arg min  

> uPU

}u ´ upptq} (3) s.t. hpf pxptq, u qq ď 0 if xptq P S

hpf pxptq, u qq ď hpxptqq ´ ∆hpxptqq else, where the constraints encode Definition II.1. This paper aims to develop a network-level CBF that ensures recoverable overall system safety, as defined in Defi-nition II.1, without imposing excessive local restrictions. For example, requiring a monotonic decrease hℓpfℓpxNℓ , u ℓqq ´ 

hℓpxℓq ď ´ ∆hℓpxℓq in local CBFs hℓp¨q for each subsystem 

ℓ P L individually would yield the network-level CBF 

hpxq “ ř hℓpxℓq, but is often prohibitive in distributed control [17]. Such a constraint prevents scenarios in which a safe agent must temporarily sacrifice safety to enable the recovery of a coupled unsafe agent. Consequently, we propose s-CBFs, which allow local increases in constraint violation while maintaining a strictly decreasing network-level CBF, and integrate them in a predictive optimization problem to reduce design conservatism. III. DISTRIBUTED PREDICTIVE CONTROL BARRIER FUNCTIONS (D-PCBF S)This section develops the theoretical D-PCBF framework. We first introduce s-CBFs to allow local relaxations of safety conditions, and then embed them as constraints in a distributed optimization problem to enlarge the safe region. 

A. Structured Control Barrier Functions 

Building on the previous arguments that motivate relax-ation of local decrease conditions, we formally define s-CBFs as follows. For notational convenience, we write Ś 

> ℓPL

Cℓ “

C1 ˆ . . . ˆ CL for a Cartesian product of sets defined by individual sets Cℓ for ℓ P t 1, . . . L u “ L.

Definition III.1 (Structured CBFs (s-CBFs)) . A set of local continuous functions thℓuLℓ“1, hℓ : Dℓ Ñ Rě0, is aset of s-CBFs for system (2) with local safe sets Sℓ :“txℓ | hℓpxℓq “ 0u and domains Dℓ :“ t xℓ | hℓpxℓq ď γf u(where γf ą 0 and Dℓ, Sℓ non-empty & compact), if there exist continuous decrease functions ∆hℓ : Dℓ Ñ Rě0

satisfying ∆hℓpxℓq ą 0 for all xℓ P DℓzSℓ and relaxation functions βℓ : Ś 

> jPNℓ

Dj Ñ R such that for all ℓ P L:

@xNℓ P ą

> jPNℓ

Dj : inf 

> uℓPUℓ

hℓpfℓpxNℓ , u ℓqq ´ hℓpxℓqď ´ ∆hℓpxℓq ` βℓpxNℓ q (4a) 

@xNℓ P ą

> jPNℓ

Sj : inf 

> uℓPUℓ

hℓpfℓpxNℓ , u ℓqq “ 0 (4b) 

@x P ą

> jPL

Dj :

> L

ÿ

> ℓ“1

βℓpxNℓ q ď 0. (4c) Following Definition III.1, the individual s-CBFs are not local CBFs themselves, as the relaxation functions βℓ allow for an increase in s-CBF values. In this case, individual agents can increase their s-CBF values if necessary so that the network-level system eventually becomes safe. Although the individual s-CBFs are not CBFs for the respective agents, we show that the network-level candidate 

hpxq “ ÿ

> ℓPL

hℓpxℓq (5) is indeed a CBF according to Definition II.1. To this end, we make the following assumption. 

Assumption III.2 (Initial s-CBF upper bound) . For the initial state xp0q it holds that ř 

> ℓPL

hℓpxℓp0qq ď γf .This assumption is common in CBF literature and implies that the initial system state is within the region of attraction of the CBF. While this may be restrictive for conservative s-CBF designs that yield small γf , we show that predictive optimization can alleviate this issue. Under Assumption III.2, we can show that the system state will remain within the global CBF domain for all times. This leads to the following theorem, which proves that the network-level candidate CBF in (5) is indeed a CBF according to Definition II.1, defining a control invariant safe set Sglob and region of attraction Dglob zSglob as follows. 

Theorem III.3 (Network-level CBF from s-CBFs) . Under Assumption III.2, the function hpxq “ řLℓ“1 hℓpxℓq con-structed with s-CBFs thℓuℓPL according to Definition III.1 is a CBF according to Definition II.1 with safe set Sglob “tx | hpxq ď 0u “ Ś 

> ℓPL

Sℓ and domain Dglob “tx | hpxq ď γf u.Proof. The claim follows from summing the local s-CBF inequalities: the relaxation terms satisfy ř 

> ℓ

βℓ ď 0, yielding decrease of the global candidate hpxq “ ř 

> ℓ

hℓpxℓq, while (4b) gives invariance of the zero-level safe set. 

B. Distributed Predictive Control Barrier Function 

To enlarge the domain Dglob and the safe set Sglob of s-CBFs, we define D-PCBFs. By using a model to predict state evolution towards the safe set of the s-CBFs, we can enlarge the effective safe set to be SDPB Ą Sglob and the effective domain to be DDPB Ą Dglob . D-PCBFs thus System  D-PCBF                 

> Dist. Pred.
> Safety Filter
> Local Controllers
> Fig. 2. Possible distributed predictive control barrier function (D-PCBF) control loop. Local control policies propose inputs up,ℓ that a distributed model predictive safety filter (see [16], but with the same constraint formulation as the D-PCBF) subsequently filters to generate the control inputs uℓ, using the slack values ξiℓthat result from the D-PCBF. The D-PCBF design ensures that the feasible set is forward-invariant and attractive. Instead of using local controllers and a distributed predictive safety filter, distributed MPC can be employed directly by modifying the cost in (7).

enable the stabilization of a larger safe set while ensuring its positive invariance under any proposed control policy. This objective is achieved by solving the D-PCBF optimization in parallel to a distributed learning-based controller (e.g., multi-agent reinforcement learning) and using the resulting optimal slack values in a distributed predictive safety filter [16]. The proposed architecture is shown in Figure 2. At each time step t, the D-PCBF optimization problem (presented here in its most general form, including the nonlinear dynamics and the general constraints of Section II) is solved as 

hDPB pxptqq “ min 

> tuiℓ, ξ iℓ, x iℓu
> L

ÿ

> ℓ“1

˜

αf ξNℓ ` 

> N´1

ÿ

> i“0

|| ξiℓ|| 

¸

(6a) s.t. @ ℓ P L:

x0 

> ℓ

“ xℓptq (6b) 

xi`1 

> ℓ

“ fℓ

`xi 

> Nℓ

, u iℓ

˘ @ i P t 0, . . . , N ´1u (6c) 

xiℓ P X iℓ pξiℓq, @ i P t 0, . . . , N ´1u (6d) 

xNℓ P Sf,ℓ pξNℓ q (6e) 

uiℓ P Uℓ @ i P t 0, . . . , N ´1u (6f) 

ξiℓ ě 0 @ i P t 0, . . . , N u. (6g) Furthermore, the distributed safety filter is given by 

tuℓptqu ℓPL “ arg min 

> tuiℓ, x iℓu
> L

ÿ

> ℓ“1

|| u0 

> ℓ

´ up 

> ℓ

ptq|| 

s.t. @ ℓ P L: (6b) , (6c) , (6f) (6d) & (6e) with ξiℓ “ p ξiℓq‹.

(7) 

Remark III.4 (Multi-objective formulation) . Instead of the two-stage optimization, i.e., (6) and (7), a single-stage for-mulation could enforce a suboptimal MPC-style decrease constraint using a valid warm-start [18]. Here, the objective (6a) is a sum of slacks over all agents, representing the global constraint violation of the multi-agent system over the prediction horizon. As is typical in MPC, the initial state parametrizes the optimization problem via (6b), and the dynamics (6c) propagate until the end of the prediction horizon N based on the input sequence 

tu0 

> ℓ

, . . . u N ´1 

> ℓ

u for all ℓ P L. For each agent ℓ P L, the set X iℓ pξiℓq “ 

!

xℓ P Rnℓ

ˇˇ cℓpxℓq ď ξiℓ ´ ∆i 1nx,ℓ 

)

repre-sents tightened, slacked state constraints (6d). The tightening 

∆0 “ 0, ∆i`1 ą ∆i is mainly technical and can be chosen arbitrarily small without loss of convergence at the cost of its rate. The set Sf,ℓ pξNℓ q “ 

!

xℓ P Rnℓ

ˇˇ hf,ℓ pxℓq ď ξNℓ

)

is defined via the function hf,ℓ and represents the slacked terminal constraint (6e). The slack values are constrained to be nonnegative (6g). The inputs along the prediction horizon have to satisfy the decoupled input constraints (6f). From now on, we assume that the terminal constraint functions hf,ℓ 

form a set of structured CBFs according to Definition III.1. 

Assumption III.5 (Terminal constraints of the D-PCBF) .

The terminal constraint functions hf,ℓ in the optimization problem (6) are a set of s-CBFs according to Definition III.1 and the corresponding safe set satisfies Ś 

> ℓPL

Sf,ℓ p0q ĂŚ 

> ℓPL

X N ´1 

> ℓ

p0q.This construction allows us to prove that the optimal value function of the distributed optimization (6) is a CBF accord-ing to Definition II.1. The following theorem formalizes the theoretical properties of the proposed distributed architecture and provides the desired guarantees for recoverable closed-loop safety. 

Theorem III.6 (D-PCBF) . Consider a distributed PCBF 

hDPB as defined by (6) and assume that U “ U1 ˆ ¨ ¨ ¨ ˆ UL

and X 0pξq “ X 01 pξ1q ˆ ¨ ¨ ¨ ˆ X 0

> L

pξLq are compact for all finite ξ ě 0. Under Assumption III.5, the optimization (6) 

admits an optimal solution, and for a finite, large enough αf ,it follows that hDPB is a CBF according to Definition II.1 with domain DDPB :“ t x | hDPB pxq ď αf γf u Ą Dglob and safe set SDPB :“ t x | hDPB pxq “ 0u Ą Sglob .Proof. The argument follows the PCBF construction of [12], with the terminal condition provided by the s-CBFs and the identity pξNℓ q‹ “ hf,ℓ pp xNℓ q‹q, which places the terminal prediction in the recoverable terminal set. Since Theorem III.6 certifies that the optimal value func-tion of (6) is a CBF according to Definition II.1, the set 

SDPB is forward invariant and all trajectories that start in the region DDPB zSDPB converge to SDPB . In fact, the proof for convergence only requires the predicted terminal state 

pxN q‹ to be in the terminal domain Dglob , leading to an implicitly defined domain tx | p xN q‹ P Dglob u Ą DDPB .By performing predictive optimization online, we have ef-fectively enlarged the safe set and the region of attraction defined by the s-CBFs. The resulting certificate, in contrast to the centralized formulation in [12], respects the multi-agent structure: state and input constraints are enforced per agent, the terminal safe set is a Cartesian product built from s-CBFs, and the only coupling appears in the dynamics (6c). As a consequence, the construction is amenable to distributed optimization and preserves the communication structure. 

Remark III.7 (Disturbance robustness) . The results above assume perfect modeling. However, the CBF framework naturally yields robustness margins. If disturbances or model error are bounded, the existence of a PCBF implies input-to-state stability [19]. IV. PLUG-AND-PLAY PROTOCOL In distributed control architectures for multi-agent sys-tems, network topological uncertainty poses a major chal-lenge to safe operation. This section develops a certifiable PnP procedure for topology-varying multi-agent systems controlled via the D-PCBF mechanism introduced in Sec-tion III. Even when individual agents violate constraints immediately after a PnP operation, the proposed procedure guarantees that the network converges to a safe operating state. The protocol performs a single D-PCBF solve with re-synthesized terminal s-CBFs (for the affected neighborhood), and makes an accept/reject decision based on two certificates: (i) a violation certificate (using predetermined bounds on allowed state constraint violations) ensuring that temporary violations remain within user-specified bounds, and (ii) a recovery certificate ensuring convergence back to the safe set SDPB .

A. Plug-and-Play Setup 

We adopt the general problem setting for dynamics and constraints of Section II. Moreover, each agent defines a maximum violation set for state constraints as follows. 

Definition IV.1 (Maximum violation sets) . Each agent ℓ P L

defines constraint violation tolerances vmax ,ℓ P Rnx,ℓ  

> ě0

(limits for temporary state constraint violations) that give rise to the sets 

Vℓ :“ t xℓ | cℓpxℓq ď vmax ,ℓ u (8) that contain all states that are temporarily admissible. This agent-based definition of admissible constraint vi-olations allows for individual levels of risk aversion. We assume here that the s-CBF domains are contained within the predefined maximum violation sets Vℓ.

Assumption IV.2 (Max. violation in Df,ℓ ). The maximum constraint violation in each agent’s domain Dℓ satisfies 

max 

> xℓPDℓ

cℓ,k pxℓq ď vmax ,ℓ,k @k P r nx,ℓ s, @ℓ P L.

Recall: nx,ℓ is the number of state constraints for agent ℓ.This assumption forces the recoverable s-CBF domain to respect the user-defined limits ( Dℓ Ď Vℓ), ensuring that safety recovery never yields unacceptable violations. Given the fixed bounds vmax ,ℓ,k , the constraint in Assumption IV.2 can be enforced in the synthesis of s-CBFs for linear systems in (12) without loss of convexity. 

B. Certificates and Algorithm 

Given Assumption IV.2, we introduce Algorithm 1 to perform the PnP procedure online. The re-synthesis step for the new, proposed network in Line 2 (also see (12)) can be restricted to a neighborhood of subsystems affected by the plug-in or plug-out operation, or performed offline before op-eration if there are only a limited number of possible network topologies and agent dynamics configurations. Solving the D-PCBF in Line 3 (also see (6)) involves solving an online distributed optimization problem that depends on the current state. The recovery certificate in Line 4 checks whether the optimal terminal slacks are upper-bounded by γf , and is justified by Theorem III.6 to verify whether the global terminal predicted state is in the region of attraction of the terminal s-CBFs. It involves a global summation that requires a predefined global protocol but can be performed within the connected communication graph. The violation certificate in Line 5 ensures recursive feasibility, in that predicted states over the horizon will never leave the maximum violation sets (8) for future time steps. 

Algorithm 1 PnP procedure with D-PCBF certificates  

> 1:

Request: L Ñ Lnew (new total set of agent indices)  

> 2:

Re-synthesize hf,ℓ for ℓ P Lnew  

> 3:

Solve D-PCBF for tp ξiℓq‹uℓPLnew , i Pt 0,...,N u 

> 4:

Recovery Certificate: Compute and check whether 

ÿ

> ℓPLnew

pξNℓ q‹ ď γf . 

> 5:

Violation Certificate: For ℓ P Lnew , i P r N ´ 1s, check 

max p0, pξiℓq‹ ´ ∆i1q ď vmax ,ℓ . 

> 6:

If both certificates pass: accept, else : reject. 

Remark IV.3 (Distributed computation of recovery certifi-cate) . The global summation check in Line 4 is required only once per change of network topology. It can be computed via standard consensus protocols or hop-by-hop propaga-tion [20]. Alternatively, one may enforce conservative local bounds pξNℓ q‹ ď γf {Lnew through division by the new total number of agents Lnew to avoid the global consensus problem. V. IMPLEMENTATION AND RESULTS In this section, we will first show methods of how to syn-thesize s-CBFs for linear distributed systems. Then, we will show results of using the D-PCBF framework in simulation and on real miniature cars. 

A. Synthesis of s-CBFs for Linear Systems 

In this section, we describe distributed methods to syn-thesize s-CBFs. We focus here on two constructive synthesis approaches for linear systems subject to polytopic state and input constraints: one in which the origin is the safe set, and one in which agents compute ellipsoidal safe sets. While the former approach follows from synthesizing structured control Lyapunov functions as in [17], it yields the smallest safe set. On the other hand, the latter yields possibly larger safe sets, but requires an iterative construction method due to nonconvexity. While it is possible to synthesize barrier functions for general nonlinear systems (e.g., via sum-of-squares techniques or learning-based methods [21]), this remains computationally intensive compared with the fol-lowing scalable, efficient synthesis for linear systems. We consider here linear time-invariant multi-agent sys-tems, with the ℓ-th agent’s dynamics written as 

xℓpt ` 1q “ ANℓ xNℓ ptq ` Bℓuℓptq @ℓ P L, (9) where ANℓ P RnℓˆnNℓ , and Bℓ P Rnℓˆmℓ . The state and input constraint sets are formally written for all ℓ P L as 

Xℓ “ t xℓ | Hxℓ xℓ ď hxℓ u and Uℓ “ t uℓ | Huℓ uℓ ď huℓ u,

where Hxℓ P Rnx,ℓ ˆnℓ , hxℓ P Rnx,ℓ , Huℓ P Rnu,ℓ ˆmℓ , and 

huℓ P Rnu,ℓ for all ℓ P L. We assume that all constraint sets contain the origin in their interior, i.e., 0 P int Xℓ

and 0 P int Uℓ. Motivated by standard synthesis proce-dures for quadratic Lyapunov functions [22], the s-CBFs are parametrized as 

hℓpxℓq “ max p0, x J 

> ℓ

Pℓxℓ ´ γx,ℓ q @ℓ P L, (10) with matrices Pℓ “ P J 

> ℓ

ą 0 of appropriate dimension and nonnegative scalars γx,ℓ . Given this parametrization, the local safe sets and domains follow as 

Sℓ “ t xℓ | xJ 

> ℓ

Pℓxℓ ď γx,ℓ u

Dℓ “ t xℓ | xJ 

> ℓ

Pℓxℓ ď γx,ℓ ` γf u.

For the s-CBF synthesis, we aim to leverage linear state-feedback control policies, as is common in the design of Lyapunov functions for linear systems. Since each agent can leverage information from neighboring agents, the consid-ered local policies are given by the matrices KNℓ , and the corresponding control action is 

uℓ “ KNℓ xNℓ . (11) To facilitate notation, we define selection matrices from network-level to local and neighboring states for each agent 

ℓ P L as Tℓ P Rnℓˆn and Wℓ P RnNℓ ˆn, respectively. 

1) Synthesis with Origin as Safe Sets: Using the origin as the safe set is equivalent to setting γx,ℓ “ 0 for all ℓ P L. We choose a constant γf “ 1, as the matrices Pℓ can be rescaled, and use the function candidates βℓpxNℓ q “ xJ 

> Nℓ

ΓNℓ xNℓ

and ∆hℓpxℓq “ xJ 

> ℓ

Dℓxℓ for all ℓ P L where ΓJ 

> Nℓ

“ ΓNℓ

and DJ 

> ℓ

“ Dℓ ą 0. To satisfy the s-CBF conditions in Definition III.1, a distributed semidefinite program can be solved. This approach is similar to the synthesis of terminal control Lyapunov functions in [17]. While [17] synthesizes time-varying terminal safe sets, we design time-varying regions of attraction. Condition (4a) becomes a Lyapunov decrease LMI, (4b) is satisfied by construction, and (4c) can be enforced via coupled structured LMIs with block-diagonal matrices SNℓ that bound ΓNℓ . However, unlike [17], we only enforce input and not state constraints for all states in Dℓ, as the recoverable region is not required to be within the state constraints Xℓ. The synthesis optimizes for δo 

> ℓ

“ t Eℓ, Y Nℓ , S Nℓ , ΓNℓ , D ℓu, where Eℓ “ P ´1 

> ℓ

and 

YNℓ “ KNℓ ENℓ . The matrix ENℓ “ blkdiag jPNℓ pEj q is simply the block-diagonal matrix of neighboring Ej .

2) Synthesis with Ellipsoidal Safe Sets: Aiming to enlarge the safe sets, we propose using the s-CBF candidates (10) with γx,ℓ ą 0. In this case, we choose γx,ℓ “ 1 and the function candidates βℓpxNℓ q “ ř 

> jPNℓ

bℓ,j hj pxj q and 

∆hℓpxℓq “ ρℓhℓpxℓq with some constant ρℓ ą 0 for all 

ℓ P L. For the resulting s-CBFs to satisfy the conditions of Definition III.1, we compute the local decision variables 

δe 

> ℓ

“ t Eℓ, tbℓ,j ujPNℓ , Y Nℓ u and the network-level decision variable γf by solving the following optimization problem, where Eℓ “ P ´1 

> ℓ

and YNℓ “ KNℓ ENℓ are defined as in the previous subsection, 

min  

> tδe
> ℓuℓPL,γ f

´

> L

ÿ

> ℓ“1

log det pEℓq (12a) s.t. @ℓ P L :

Eℓ ą 0, ρ ℓ ą 0, (12b) 

bℓ,j ě 0 @j P Nℓzt ℓu, 1 ´ ρℓ ` bℓ,ℓ ě 0, (12c) 

ÿ

> jPNℓ

bℓ,j ď ρℓ, (12d) 

Mℓ :“ ÿ

> jPNℓ

bℓ,j WℓT J 

> j

Ej Tj W J 

> ℓ

,

„p1 ´ ρℓq ¨ ¯Eℓ ` Mℓ ENℓ AJ 

> Nℓ

` YNℓ

> J

BJ

> ℓ

ANℓ ENℓ ` BℓYNℓ Eℓ

ȷ

ľ 0, (12e) 

ÿ

> jPNℓ

bj,ℓ ď 0, (12f) 

„phuℓ,i q2{p| Nℓ|p 1 ` γf qq pHuℓ,i qYNℓ

YNℓ

> J

pHuℓ,i qJ ENℓ

ȷ

ľ 0 @i P r nu,ℓ s, (12g) 

pHxℓ,i qEℓpHxℓ,i qJ ď p hxℓ,i q2 @i P r nx,ℓ s, (12h) where ¯Eℓ “ WℓT J 

> ℓ

EℓTℓW J 

> ℓ

and the other variables are as defined above. Inequalities (12c), (12d), and (12e) ensure conditions (4a) and (4b). Inequality (12f) ensures condition (4c). Finally, (12g) and (12h) ensure state and input con-straint satisfaction, respectively. The proof of sufficiency is omitted here for brevity. To solve the non-convex (bilinear) problem (12) approximately, we employ a distributed block-coordinate descent (alternating minimization) scheme [23]. By fixing the variables ptt bℓ,j ujPNℓ uℓ, γ f q, the problem be-comes convex in pt Eℓuℓ, tYNℓ uℓq and by fixing pt Eℓuℓq, the problem is convex in `tt bℓ,j ujPNℓ uℓ, tKNℓ uℓ, p1 ` γf q´1˘.Crucially, because the constraints respect the network topol-ogy [17], each of these LMI subproblems can be solved via distributed optimization techniques such as the alternating direction method of multipliers (ADMM) [24], enabling a fully distributed synthesis procedure. 

B. Simulation Results and Experiments 

We validate the proposed D-PCBF architecture using a vehicle platooning example. The validation proceeds in three stages: s-CBF synthesis for the longitudinal model, simulations to assess scalability, and hardware experiments on miniature race cars to demonstrate real-world robustness. 012 

> 3
> Fig. 3. Platoon example system. Assuming perfect lateral tracking and a kinematic model, the distributed dynamics consider the arc length to the preceding vehicle and the velocity magnitude.

1) Car Platoon Synthesis: The platoon is modeled as a double integrator with purely longitudinal dynamics, assum-−0.5 0.0 0.5˜d1                          

> −1
> 0
> 1˜v1
> X1
> Sf, 1
> Df, 1
> −0.50.00.5˜d2
> −1
> 0
> 1˜v2
> X2
> Sf, 2
> Df, 2
> 12345678Iteration
> −33
> −32
> ∝ | E|
> 12345678Iteration
> 12 .5
> 15 .0
> γf
> Fig. 4. Synthesis results for the vehicle platoon model when maximizing the safe set for L“5agents. The top plots show the resulting s-CBF safe sets (green) and domains (blue) for the second and third car of the platoon. The bottom plots visualize the alternating optimization results described in Section V-A to approximately solve (12). The safe set size is plotted as the log-determinants of E, i.e., log det pEq “ ř
> ℓlog det pEℓq9| E|, while γf
> is used to indicate the size of the terminal region of attraction.

ing perfect lateral tracking (Fig. 3). The state of the ℓ-th vehicle evolves according to discretized dynamics [25], with sampling time dt “ 0.1 s ,

˜dℓpt ` 1q “ ˜dℓptq ´ dt ˜vℓptq ` dt ˜vℓ´1ptq, (13) 

˜vℓpt ` 1q “ ˜vℓptq ` dt a ℓptq, (14) where ˜dℓ and ˜vℓ are errors relative to the reference distance 

dref “ 1.0 m and velocity vref “ 1.0 m {s. We constrain inputs aℓ P r´ 5.0, 5.0s m{s2 and states dℓ P r 0.5, 1.5s m,

vℓ P r 0.5, 1.5s m{s. Agent ℓ “ 0 has no distance state (13). We first approximately solve (12) to maximize the safe set. As shown in Fig. 4, the alternating optimization (cf. Section V-A) successfully expands the volume of the safe set measured by log det pEq and the terminal region of attraction measured by γf .

2) Scalability Analysis (Simulation): To evaluate the framework’s scalability, we simulate a scenario in which an adversarial controller commands a constant, unsafe accelera-tion ap 

> ℓ

“ 10 .0 m {s2 for all agents. To filter these inputs, we employ the control architecture presented in Fig. 2. We use the origin as a safe set and synthesize the s-CBFs according to Section V-A. The platoon is initialized in an unsafe state, with vehicles 1 and 2 at zero distance to their predecessors (i.e., the cars are in contact) and all other relative states set to zero. Constraints and dynamics are the same as previously described. We set αf “ 10 3, ∆i “ 10 ´3i, and N “ 10 .As shown in Fig. 5 (top), the filter mechanism successfully intervenes, overriding the unsafe commands to recover safe spacing. We compare the solve times of distributed and centralized implementations (Fig. 5, bottom). MOSEK [26] is used as the centralized solver and for the distributed agent-level problems. To solve (12) in a distributed manner, we implement a custom solver based on ADMM [24]. The prob-lem is decomposed into local subproblems, in which agents optimize local trajectories and copies of coupled neighbor states, subject to consensus constraints. We use warm-starting to improve convergence speed, and the ADMM procedure for (6) converges in an average of 15 iterations. Since the distributed algorithm is executed on a sequential CPU, we evaluate it using an idealized parallel runtime, computed by summing, over ADMM iterations, the maximum local subproblem solve time and the maximum consensus and dual 0 1                         

> d`[m]
> 0.5
> 1.0
> 1.5
> v` [m/s]
> `= 1
> `= 2
> 01
> d`[m]
> 0
> 1`= 3
> `= 4
> 510 15 20 25 30 35 40 Number of Agents
> 0.01
> 0.02
> 0.03 Solve Time [s]
> Centralized Distributed Fig. 5. Simulation results for the vehicle platoon model. The top plots show the state space trajectories (start=dot, end=cross) of the distributed simulation for the first four follower agents, where agents ℓ“1,2are initialized at an unsafe distance. The top-left plot shows results for 5 agents, and the right plot shows results for 40 agents. In both cases, the network-level system converges to safe operation within the state constraints, although all agents try to accelerate with ap
> ℓ“10 .0 m {s2. The bottom plot shows the solve times of (6) for the centralized solver and the distributed implementation, where the latter is reported as an idealized parallel runtime.

update times across agents, yielding a communication-free lower bound on the runtime of a fully parallel implementa-tion. While the centralized solver’s computation time grows with network size, the idealized distributed runtime remains approximately constant, indicating favorable scalability of the distributed formulation. 

3) Real-time Miniature Race Car Platoon: In the previous simulation, the closed-loop system was entirely simulated with perfect model knowledge. To provide deeper insight into the inherent robustness of the D-PCBF formulation and to demonstrate its potential for online PnP operation, we conduct a vehicle platoon experiment within the minia-ture race car framework [27]. A decentralized longitudinal controller proposes unsafe inputs, which are subsequently filtered using the D-PCBF and the predictive safety filter control loop shown in Figure 2. Local lateral pure-pursuit controllers are assumed to provide safe steering inputs that keep the cars on a given reference track. The time step is chosen to be dt “ 0.1 s , and we set vref “ 0.5m {s and 

dref “ 1.0m . The constraints are now aℓ P r´ 0.5, 1.5s m{s2,

dℓ P r0.8, 1.4s m, and vℓ P r0.1, 0.9s m{s. The leader’s acceleration a0 is further limited to 25% of the follower’s maximum acceleration to induce unsafe platoon situations by artificially limiting the leader’s ability to accelerate away from the approaching follower. To synthesize the terminal s-CBFs, we use the origin as the safe set and perform the associated synthesis optimization described in Section V-A. For the experiment, we again employ the control architecture presented in Figure 2. To demonstrate the framework’s inherent robustness, we select an unsafe PID controller that aims to exceed the speed limit at 1.5m {s, thereby creating unsafe driving conditions. This controller proposes acceleration inputs ap 

> ℓ

, which are filtered by the proposed D-PCBF. The D-PCBF parameters are set to αf “ 10 4, N “ 40 , and ∆i “ 10 ´3i. The closed loop with D-PCBF and safety filter runs at 100 Hz , and we use HPIPM QCQP [28] as a solver for both. We perform a high-speed join maneuver at the tail of the platoon. This setup replicates an end-of-queue scenario , a critical safety case in traffic management [29], in which a vehicle approaches a formed platoon with significant relative velocity, requiring the platoon to temporarily reduce relative distances below the safety threshold. A platoon consisting of cars 0, 1, and 2 is launched to drive a rectangular track with rounded corners. At some time, car 3 is launched to approach the end of the platoon at high speed. Shortly before a collision at t « 47 s, a plug-in is performed. Subsequently, car 0 performs a plug-out at t « 59 s and stops to the side of the track. It is then launched again, approaching the end of the platoon at high speed to perform another plug-in at 

t « 78 s, repeating car 3’s earlier high-speed approach. The resulting state and input trajectories are shown in Figure 6, where it can be seen that temporary violations of the distance and velocity constraints (shaded in gray) cannot be avoided. Yet the closed-loop system converges to safe operation, indicating that selecting the origin as the safe set is sufficient for the chosen horizon length. This experiment highlights the effectiveness of the distributed formulation: While a distributed MPC [17] or a standard predictive safety filter [16] would become infeasible, the proposed approach guarantees convergence to safe opera-tion. Furthermore, conducting these experiments with fully decentralized controllers (e.g., PID) would likely result in collisions because the approaching vehicle cannot decelerate sufficiently due to actuator constraints. Note that we disable the PnP protocol (Algorithm 1) and admit all PnP requests to allow extreme scenarios and demonstrate the robustness of D-PCBFs. While the recovery (Line 4) and violation certificate (Line 5) would have held in both plug-in scenarios, we did not enforce Assumption IV.2 in the synthesis of the s-CBFs. Although we have omitted Algorithm 1 here, it would allow us to accommodate varying levels of risk aversion and formally guarantee safety recovery. In addition to this experiment, which involved high-speed plug-in behavior, we have performed plug-ins from standstill and plug-outs of cars at various positions within the platoon. Videos of the experiments accompany this manuscript. 1

Remark V.1 (Distributed implementation) . In these hardware experiments, because of the small number of agents, central-ized solvers were used to validate the formulation . In a real-world deployment, the achievable control frequency would depend on solver convergence speed and network bandwidth, a typical trade-off in distributed optimization [30]. VI. CONCLUSION This paper proposes a distributed safety-filter framework based on predictive control barrier functions for multi-agent systems. A distributed optimization combines local s-CBFs into a terminal constraint, and the resulting optimal value function serves as a CBF for the overall network. This construction yields network-level forward invariance and convergence to a safe set after temporary constraint viola-tions. It further underpins a plug-and-play protocol that uses a slack-based admissibility check based solely on neighbor information, enabling safe admission and removal of agents without requiring preparatory system behavior. Validation                     

> 1Available at https://youtu.be/Anr12ZRGJuM and
> https://youtu.be/Btexrte7vWQ .40 50 60 70 80 90
> 0
> 1
> 2
> d [m]  Car 0 Car 1 Car 2 Car 3
> 40 50 60 70 80 90
> 0.5
> 1.0
> 1.5
> v [m/s]
> 40 50 60 70 80 90

time [s]      

> 0
> 2
> a [m/s 2] Car 0
> 40 50 60 70 80 90

time [s]      

> Car 1
> 40 50 60 70 80 90

time [s]      

> Car 2
> proposed commanded
> 40 50 60 70 80 90

time [s] 

> Car 3

Fig. 6. State and input signals of the platoon experiment. The experiment starts with the configuration 0 Ð 1 Ð 2. It consists of the following events: Car 3 approaches with high speed from behind and plugs in at t « 47 s. Then, the leading car 0 plugs out at t « 59 s. Finally, at t « 78 s, this car approaches with high speed from behind and plugs in again. It can be seen that, in both plug-in scenarios, temporary violations of velocity and distance constraints occur, since the cars have limited acceleration. Although the infeasible commands proposed by the unsafe PID controllers (gray signals) would violate state constraints, the framework prevents unsafe operation whenever possible by applying the filtered commands (red signals). Moreover, it enables safety recovery in cases of constraint violations (gray shaded times). The leader vehicle is constrained to a smaller maximum acceleration to generate visible distance-constraint violations. 

on a simulated and a physical vehicle platoon demonstrates recovery to safety, sustained invariance under coupling, and safe plug-and-play behavior, indicating the practicality of the approach for scalable safety certification. Current limita-tions include qualitative treatment of robustness, potentially conservative s-CBF synthesis for linear polytopic systems, and the restriction to coupling via dynamics. Future work includes deploying distributed solvers on hardware to assess latency and network effects, designing less conservative (e.g., time-varying) terminal sets to enlarge safe regions, establishing robustness guarantees for bounded uncertainties, and extending the framework to coupled state constraints. REFERENCES [1] C. F. Caruntu, C. Braescu, A. Maxim, R. C. Rafaila, and A. Tiganasu, “Distributed model predictive control for vehicle platooning: A brief survey,” in 2016 20th International Conference on System Theory, Control and Computing (ICSTCC) , 2016. [2] Y. Ding, Z. Yang, Q.-V. Pham, Y. Hu, Z. Zhang, and M. Shikh-Bahaei, “Distributed Machine Learning for UAV Swarms: Computing, Sensing, and Semantics,” IEEE Internet of Things Journal , 2024. [3] C. Wu, H. Fang, Q. Yang, X. Zeng, Y. Wei, and J. Chen, “Distributed Cooperative Control of Redundant Mobile Manipulators With Safety Constraints,” IEEE Transactions on Cybernetics , 2023. [4] R. Scattolini, “Architectures for distributed and hierarchical Model Predictive Control - A review,” Journal of Process Control , 2009. [5] X. Ge, F. Yang, and Q.-L. Han, “Distributed networked control systems: A brief overview,” Information Sciences , 2017. [6] C. Tang, B. Abbatematteo, J. Hu, R. Chandra, R. Martin-Martin, and P. Stone, “Deep Reinforcement Learning for Robotics: A Survey of Real-World Successes,” Annual Review Control, Robotics, and Autonomous Systems , Sept. 2024. [7] K. Zhang, Z. Yang, and T. Bas ¸ar, “Multi-Agent Reinforcement Learn-ing: A Selective Overview of Theories and Algorithms,” in Handbook of Reinforcement Learning and Control , Studies in Systems, Decision and Control, Springer, 2021. [8] K.-C. Hsu, H. Hu, and J. F. Fisac, “The Safety Filter: A Unified View of Safety-Critical Control in Autonomous Systems,” Annual Review of Control, Robotics, and Autonomous Systems , 2024. [9] B. Tearle, K. P. Wabersich, A. Carron, and M. N. Zeilinger, “A Predictive Safety Filter for Learning-Based Racing Control,” IEEE Robotics and Automation Letters , 2021. [10] K. P. Wabersich and M. N. Zeilinger, “Linear Model Predictive Safety Certification for Learning-Based Control,” in IEEE Conference on Decision and Control , 2018. [11] E. C. Kerrigan and J. M. Maciejowski, “Soft Constraints and Exact Penalty Functions in Model Predictive Control,” 2000. [12] K. P. Wabersich and M. N. Zeilinger, “Predictive Control Barrier Functions: Enhanced Safety Mechanisms for Learning-Based Control,” 

IEEE Transactions on Automatic Control , 2023. [13] M. Zeilinger, Y. Pu, S. Riverso, G. Ferrari-Trecate, and C. Jones, “Plug and play distributed model predictive control based on distributed invariance and optimization,” in IEEE Conference on Decision and Control , 2013. [14] R. B. Larsen, A. Carron, and M. N. Zeilinger, “Safe Learning for Dis-tributed Systems with Bounded Uncertainties,” IFAC-PapersOnLine ,2017. [15] A. Carron, J. Sieber, and M. N. Zeilinger, “Distributed Safe Learning using an Invariance-based Safety Framework,” IFAC-PapersOnLine ,2021. [16] S. Muntwiler, K. P. Wabersich, A. Carron, and M. N. Zeilinger, “Distributed Model Predictive Safety Certification for Learning-based Control,” IFAC-PapersOnLine , 2020. [17] C. Conte, C. N. Jones, M. Morari, and M. N. Zeilinger, “Distributed synthesis and stability of cooperative distributed model predictive control for linear systems,” Automatica , 2016. [18] A. Didier and M. N. Zeilinger, “A multiobjective approach to robust predictive control barrier functions for discrete-time systems,” Mar. 2025. arXiv:2503.19485 [eess]. [19] A. Didier and M. N. Zeilinger, “Approximate Predictive Control Barrier Function for Discrete-Time Systems,” IEEE Transactions on Automatic Control , 2026. [20] N. A. Lynch, Distributed Algorithms . Morgan Kaufmann Publishers Inc., 1996. [21] C. Dawson, S. Gao, and C. Fan, “Safe Control With Learned Certifi-cates: A Survey of Neural Lyapunov, Barrier, and Contraction Methods for Robotics and Control,” IEEE Transactions on Robotics 2023 .[22] S. Boyd, L. El Ghaoui, E. Feron, and V. Balakrishnan, Linear Matrix Inequalities in System and Control Theory . Society for Industrial and Applied Mathematics, 1994. [23] T. Iwasaki and R. E. Skelton, “The XY-centring algorithm for the dual LMI problem: a new approach to fixed-order control design,” 

International Journal of Control , 1995. [24] S. Boyd, “Distributed Optimization and Statistical Learning via the Alternating Direction Method of Multipliers,” Foundations and Trends in Machine Learning , 2010. [25] A. Alam, Fuel-efficient distributed control for heavy duty vehicle platooning . KTH Royal Institute of Technology, 2011. [26] MOSEK ApS, “The MOSEK optimization API for python manual. Version 10.0.38,” manual, MOSEK ApS, 2024. [27] A. Carron, S. Bodmer, L. Vogel, R. Zurbruegg, D. Helm, R. Rick-enbach, S. Muntwiler, J. Sieber, and M. N. Zeilinger, “Chronos and CRS: Design of a miniature car-like robot and a software framework for single and multi-agent robotics and control,” in IEEE International Conference on Robotics and Automation , 2023. [28] G. Frison, J. Frey, F. Messerer, A. Zanelli, and M. Diehl, “Introducing the quadratically-constrained quadratic programming framework in HPIPM,” in European Control Conference , 2022. [29] A. Khan, “Intelligent infrastructure-based queue-end warning system for avoiding rear impacts,” IET Intelligent Transport Systems , 2007. [30] K. Tsianos, S. Lawlor, and M. Rabbat, “Communication/Computation Tradeoffs in Consensus-Based Distributed Optimization,” in NeurIPS ,2012.