---
title: "The Observability Gap: Why Output-Level Human Feedback Fails for LLM Coding Agents"
title_zh: 观测性鸿沟：为什么输出层级的人类反馈对大语言模型编程智能体失效
authors: "Yinghao Wang, Cheng Wang"
date: 2026-03-27
pdf: "https://arxiv.org/pdf/2603.26942v1"
tags: ["query:agent"]
score: 9.0
evidence: 大语言模型多智能体代码系统与自主性获取
tldr: "本研究探讨了LLM编程智能体在“获得性自主”设定下的表现，即智能体从零开始通过人类对视觉输出的反馈逐步构建功能库。在Blender 3D场景生成任务中，尽管智能体能发现基础功能，但在仅依赖输出层反馈时，全场景成功率为0%。研究揭示了“观测性鸿沟”：代码逻辑与视觉输出间的复杂映射导致反馈无法定位根因。通过引入少量代码级干预，系统恢复了收敛性，强调了中间观测性在人机协作中的重要性。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.26942v1/fig-001.webp\", \"caption\": \"Figure 1: Overview of the generate–evaluate–evolve loop in the earned autonomy setting. In each cycle, a coding agent generates code, an execution agent runs the script and renders the scene, a human evaluator provides feedback on rendered output only, and a review agent promotes validated functions into the reusable library for the next cycle. The example illustrates library growth from 0 validated functions in Cycle 1 to 15 in Cycle 2.\", \"page\": 2, \"index\": 1, \"width\": 794, \"height\": 479}, {\"url\": \"assets/figures/arxiv/2603.26942v1/fig-002.webp\", \"caption\": \"Figure 3: Example successful scene generated after the diagnostic intervention in Group D. With the ground plane excluded from collision checks, the system can satisfy object completeness, ground contact, collision avoidance, and scale plausibility simultaneously.\", \"page\": 3, \"index\": 2, \"width\": 406, \"height\": 373}, {\"url\": \"assets/figures/arxiv/2603.26942v1/fig-003.webp\", \"caption\": \"Figure 2: Failure-mode oscillation across three consecutive runs in Group C. The task specifies BVH-based collision detection against “all mesh objects in the scene”. Left: the ground plane is included in the collision list, so every candidate placement intersects at z=0, causing timeout before trees or cars are placed; from rendered output alone, this appears as missing objects. Center: after feedback on missing objects, the agent restructures import and placement logic and places the trees and cars, but now excludes the house from collision checks, producing overlap. Right: feedback on overlap causes the agent to re-include all meshes in collision checks, re-triggering the original timeout failure.\", \"page\": 3, \"index\": 3, \"width\": 1056, \"height\": 255}]"
motivation: 探究仅依赖输出层的人类反馈是否足以让LLM编程智能体通过自我演化构建复杂的编程能力。
method: 在Blender 3D建模任务中，让智能体从零开始构建函数库，并仅根据最终视觉输出接受人类的轻量级反馈。
result: 智能体虽能复现基础工具函数，但在仅有输出反馈的情况下无法完成复杂的全场景任务，成功率为零。
conclusion: 视觉输出与内部代码逻辑间的观测性鸿沟是主要瓶颈，有效的人机协作需要超越输出层的中间状态反馈。
---

## 摘要
大语言模型（LLM）多智能体编程系统通常在设计时固定智能体的能力。我们研究了一种替代设定，即“获得性自主”（earned autonomy），在这种设定下，编程智能体从零预定义函数开始，仅通过针对视觉输出的轻量级人类反馈，逐步构建可重用的函数库。我们在基于 Blender 的 3D 场景生成任务中评估了这一设定，该任务需要空间推理和程序化几何控制。尽管智能体重新发现了与人类参考实现相当的核心实用函数，但在仅有输出反馈的情况下，跨多种指令粒度的全场景成功率均为 0%，而成功要求同时满足物体完整性、地面接触、碰撞避免和比例合理性。我们的分析确定了一个结构性的观测性鸿沟：Bug 源于代码逻辑和执行状态，而人类评估仅发生在输出层，且从内部状态到可见结果的多对一映射使得症状层级的反馈无法可靠地识别根本原因。这种不匹配导致了持续的失败模式振荡，而非收敛。一项注入了极少量代码级知识的诊断性干预恢复了收敛，有力地支持了这一解释，即主要瓶颈在于反馈的可观测性，而非编程能力。我们将这一现象形式化为在内部代码逻辑与感知结果之间存在深层因果链的领域中的“反馈悖论”，并认为在这些设定下，有效的人机协作需要超越仅输出评估的中间可观测性。

## Abstract
Large language model (LLM) multi-agent coding systems typically fix agent capabilities at design time. We study an alternative setting, earned autonomy, in which a coding agent starts with zero pre-defined functions and incrementally builds a reusable function library through lightweight human feedback on visual output alone. We evaluate this setup in a Blender-based 3D scene generation task requiring both spatial reasoning and programmatic geometric control. Although the agent rediscovered core utility functions comparable to a human reference implementation, it achieved 0% full-scene success under output-only feedback across multiple instruction granularities, where success required satisfying object completeness, ground contact, collision avoidance, and scale plausibility simultaneously. Our analysis identifies a structural observability gap: bugs originate in code logic and execution state, while human evaluation occurs only at the output layer, and the many-to-one mapping from internal states to visible outcomes prevents symptom-level feedback from reliably identifying root causes. This mismatch leads to persistent failure mode oscillation rather than convergence. A diagnostic intervention that injected minimal code-level knowledge restored convergence, strongly supporting the interpretation that the main bottleneck lies in feedback observability rather than programming competence. We formalize this phenomenon as a feedback paradox in domains with deep causal chains between internal code logic and perceptual outcomes, and argue that effective human-agent collaboration in such settings requires intermediate observability beyond output-only evaluation.

---

## 论文详细总结（自动生成）

这是一份关于论文《The Observability Gap: Why Output-Level Human Feedback Fails for LLM Coding Agents》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：在 LLM 编程智能体（Coding Agents）中，仅依靠人类对**最终输出结果**（如渲染图）的轻量级反馈，是否足以让智能体通过自我演化（从零构建函数库）来解决复杂的编程任务？
*   **研究背景**：现有的多智能体系统通常在设计时就固定了工具集。作者提出了一种“获得性自主”（Earned Autonomy）的设定，让智能体从零开始，通过与人类的交互逐步积累可重用的代码库。
*   **研究动机**：探索在视觉导向的编程任务中，人类反馈的“低成本（仅看结果）”与智能体诊断“高难度（定位代码 Bug）”之间的矛盾。

### 2. 方法论：核心思想与流程
*   **核心思想**：构建一个“生成-评估-演化”（Generate–Evaluate–Evolve）的闭环系统。
*   **关键流程**：
    1.  **生成（Coding Agent）**：根据任务描述编写 Python 脚本（使用 Blender API），初始状态下没有任何预定义函数。
    2.  **执行（Execution Agent）**：运行脚本并渲染出 3D 场景图像。
    3.  **评估（Human Evaluator）**：人类仅观察渲染图，根据物体完整性、地面接触、碰撞避免、比例合理性四个维度给出自然语言反馈。
    4.  **演化（Review Agent）**：将通过验证的代码片段提取并存入持久化的“函数库”，供下一轮循环调用。
*   **关键技术细节**：研究重点在于不同指令粒度（从粗糙到精细）对反馈有效性的影响，并定义了“观测性鸿沟”（Observability Gap），即代码逻辑层与视觉输出层之间的断层。

### 3. 实验设计
*   **场景设定**：基于 Blender 的 3D 场景生成。任务是生成一个包含中心房屋、2-3 棵树和 1-2 辆车的场景。
*   **实验分组（对比方法）**：
    *   **Group A（粗粒度）**：仅提供高层场景目标。
    *   **Group B（中粒度）**：增加定量约束和比例范围。
    *   **Group C（细粒度）**：指定具体算法（如 BVH 碰撞检测），但不提供代码实现。
    *   **Group D（诊断性干预）**：在 Group C 基础上增加一条代码级的架构约束（如“碰撞检测时排除地面”）。
*   **评估指标**：全场景成功率（必须同时满足四个物理/空间准则）。

### 4. 资源与算力
*   **模型使用**：文中提到使用了 **Claude** 系列模型（具体型号未详述，但属于主流大模型）。
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量或训练时长。由于该研究侧重于推理和人机交互逻辑，而非模型训练，其算力消耗主要集中在 LLM API 调用和 Blender 渲染上。

### 5. 实验数量与充分性
*   **实验规模**：
    *   Group A-C 各进行了 2 个周期，每个周期 5 次运行（共 30 次核心实验）。
    *   Group D 进行了 3 次独立测试。
*   **充分性评价**：
    *   **客观性**：实验设计了严谨的对照组（从粗到细的指令），并引入了诊断性干预来验证假设，逻辑链条完整。
    *   **局限性**：实验样本量相对较小（总计几十次运行），且仅由单一评估者和单一模型家族完成，在统计学广度上略显不足，但作为定性分析和原理验证（Proof of Concept）是充分的。

### 6. 主要结论与发现
*   **观测性鸿沟**：在 A-C 组中，全场景成功率为 **0%**。尽管智能体能写出基础工具函数，但无法解决复杂的逻辑冲突。
*   **失败模式振荡**：系统陷入了“修复一个症状引发另一个症状”的循环。例如：为了修复“物体缺失”而修改逻辑，结果导致“物体重叠”；为了修复“重叠”又回到了导致“超时/缺失”的旧逻辑。
*   **反馈悖论**：最省力的人类反馈（仅看输出）无法提供足够的诊断信息；而能解决问题的反馈（代码级诊断）又违背了减少人类工作量的初衷。
*   **干预效果**：Group D 证明，只需注入极少量的代码级知识，系统就能迅速收敛并成功。

### 7. 优点
*   **理论深刻**：提出了“观测性鸿沟”和“反馈悖论”的概念，深刻揭示了当前 LLM 编程智能体在复杂物理/视觉任务中的瓶颈。
*   **实验巧妙**：通过 Group D 的微小干预成功剥离了“编程能力不足”和“反馈信息不足”这两个变量，证明了瓶颈在于后者。
*   **开源贡献**：提供了代码库，方便社区复现和进一步研究。

### 8. 不足与局限
*   **领域单一**：仅在 Blender 3D 生成任务上进行了验证，结论在其他领域（如 Web 开发、数据分析）的普适性有待验证。
*   **人工成本**：虽然目标是减少人类工作量，但实验过程仍高度依赖人类的实时评估。
*   **缺乏自动化中间层**：论文指出了需要“中间观测层”，但未在文中提出并实现一套自动化的中间状态反馈机制（如自动化的碰撞日志摘要）。

（完）
