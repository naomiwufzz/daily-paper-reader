---
title: "HabitatAgent: An End-to-End Multi-Agent System for Housing Consultation"
title_zh: HabitatAgent：一种用于住房咨询的端到端多智能体系统
authors: "Hongyang Yang, Yanxin Zhang, Yang She, Yue Xiao, Hao Wu, Yiyang Zhang, Jiapeng Hou, Rongshan Zhang"
date: 2026-04-01
pdf: "https://arxiv.org/pdf/2604.00556v1"
tags: ["query:agent"]
score: 9.0
evidence: 用于真实世界住房咨询的LLM驱动多智能体架构
tldr: "针对住房咨询中决策复杂、约束多及事实性保障不足的问题，本文提出了首个端到端多智能体系统 HabitatAgent。该系统通过记忆、检索、生成和验证四个专业化智能体协作，利用多层用户记忆管理、混合图检索（GraphRAG）及多级验证机制，提供可审计且可靠的咨询服务。实验显示，其在真实场景下的准确率从基准的75%显著提升至95%。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.00556v1/fig-001.webp\", \"caption\": \"Table 2. Performance on the complex query subset (60 queries with ≥3 constraints).\", \"page\": 10, \"index\": 1, \"width\": 718, \"height\": 297}, {\"url\": \"assets/figures/arxiv/2604.00556v1/fig-002.webp\", \"caption\": \"Fig. 3. Product Demo of Fangdongdong. Scan the QR code above, or search for \\\"Fangdongdong\\\" in WeChat to access the product and experience the real estate decision-making system powered by HabitatAgent.\", \"page\": 13, \"index\": 2, \"width\": 364, \"height\": 571}, {\"url\": \"assets/figures/arxiv/2604.00556v1/fig-003.webp\", \"caption\": \"Fig. 1. HabitatAgent overview. Four specialized agents—Memory, Retrieval, Generation, and Validation—are executed as a five-stage workflow. In particular, Stage 1a (constraint extraction), Stage 1b (memory fusion), and Stage 5 (memory update) are internal substeps of the Memory Agent.\", \"page\": 4, \"index\": 3, \"width\": 1153, \"height\": 630}, {\"url\": \"assets/figures/arxiv/2604.00556v1/fig-004.webp\", \"caption\": \"Table 1. Overall performance comparison on the full dataset (300 queries). Best results are in bold.\", \"page\": 9, \"index\": 4, \"width\": 721, \"height\": 546}, {\"url\": \"assets/figures/arxiv/2604.00556v1/fig-005.webp\", \"caption\": \"Fig. 2. Use case of HabitatAgent. A user query is processed through constraint extraction, hybrid retrieval (GraphRAG), evidence-grounded generation, and multi-tier validation to produce a verified recommendation.\", \"page\": 5, \"index\": 5, \"width\": 710, \"height\": 700}]"
motivation: 现有的住房平台和LLM助手在处理复杂约束和保证事实准确性方面存在局限，难以支持高风险的住房决策。
method: 提出由记忆、检索、生成和验证四个角色组成的多智能体架构，结合GraphRAG和多层验证机制实现端到端咨询。
result: "在100个真实用户咨询场景的评估中，HabitatAgent 将端到端准确率从强基准线的75%显著提升至95%。"
conclusion: HabitatAgent 证明了多智能体协作架构在处理高约束、高风险决策支持任务中的有效性和可靠性。
---

## 摘要
住房选择是一个高风险且在很大程度上不可逆的决策问题。我们将住房咨询作为住房选择的决策支持界面进行研究。现有的住房平台和许多基于大语言模型（LLM）的助手通常将这一过程简化为排序或推荐，导致推理过程不透明、多约束处理能力脆弱，且事实性保证有限。我们提出了 HabitatAgent，这是首个用于端到端住房咨询的、由大语言模型驱动的多智能体架构。HabitatAgent 包含四个专门的智能体角色：记忆（Memory）、检索（Retrieval）、生成（Generation）和验证（Validation）。记忆智能体通过约束提取、记忆融合和验证门控更新等内部阶段来维护多层用户记忆；检索智能体执行混合向量-图检索（GraphRAG）；生成智能体产生引用证据的推荐和解释；验证智能体应用多层级验证和针对性修复。这些智能体共同为端到端住房咨询提供了一个可审计且可靠的工作流。我们在端到端正确性协议下，针对 100 个真实用户咨询场景（300 个多轮问答对）对 HabitatAgent 进行了评估。一个强大的单阶段基准模型（Dense+Rerank）达到了 75% 的准确率，而 HabitatAgent 达到了 95%。

## Abstract
Housing selection is a high-stakes and largely irreversible decision problem. We study housing consultation as a decision-support interface for housing selection. Existing housing platforms and many LLM-based assistants often reduce this process to ranking or recommendation, resulting in opaque reasoning, brittle multi-constraint handling, and limited guarantees on factuality.   We present HabitatAgent, the first LLM-powered multi-agent architecture for end-to-end housing consultation. HabitatAgent comprises four specialized agent roles: Memory, Retrieval, Generation, and Validation. The Memory Agent maintains multi-layer user memory through internal stages for constraint extraction, memory fusion, and verification-gated updates; the Retrieval Agent performs hybrid vector--graph retrieval (GraphRAG); the Generation Agent produces evidence-referenced recommendations and explanations; and the Validation Agent applies multi-tier verification and targeted remediation. Together, these agents provide an auditable and reliable workflow for end-to-end housing consultation.   We evaluate HabitatAgent on 100 real user consultation scenarios (300 multi-turn question--answer pairs) under an end-to-end correctness protocol. A strong single-stage baseline (Dense+Rerank) achieves 75% accuracy, while HabitatAgent reaches 95%.

---

## 论文详细总结（自动生成）

这篇论文介绍了一个名为 **HabitatAgent** 的端到端多智能体系统，专门用于解决高风险、多约束的住房咨询决策支持问题。以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **研究背景**：住房选择是一项高风险、高成本且具有强路径依赖（不可逆性）的决策。购房者需要平衡预算、位置、学区、交通、政策等多种异构且可能冲突的因素。
*   **核心问题**：现有的住房平台或基于大语言模型（LLM）的助手通常将其简化为简单的搜索和排序任务，存在以下局限：
    1.  **用户偏好演变**：用户需求往往在多轮对话中逐渐明确，系统难以维持一致的状态。
    2.  **复杂关系约束**：语义搜索难以处理如“距离地铁500米且在某学区内”的硬性关系约束。
    3.  **推理不透明**：缺乏可审计的推荐理由。
    4.  **事实性错误风险**：房产信息的幻觉（如价格、面积错误）可能导致严重的决策失误。

### 2. 方法论：HabitatAgent 架构
HabitatAgent 采用闭环多智能体协作模式，包含四个核心角色：
*   **记忆智能体 (Memory Agent)**：
    *   **四层结构**：维护对话记忆（短期）、实体记忆（中期）、偏好/偏差记忆（长期）和检索记忆（去重）。
    *   **验证门控更新**：只有通过验证的回答所包含的信息才会被更新到长期记忆中，防止错误信息污染用户画像。
*   **检索智能体 (Retrieval Agent)**：
    *   **自适应路由**：根据查询复杂度（约束数量、关系词等）决定是使用简单的向量检索，还是调用 **GraphRAG**（结合 Neo4j 知识图谱的图检索）。
    *   **混合检索**：整合 SQL 过滤、向量相似度和图路径查询。
*   **生成智能体 (Generation Agent)**：
    *   **任务感知编排**：内置 14 种特定任务模板（如学区分析、投资评估）。
    *   **证据引用**：在生成内容中强制标记证据 ID（Evidence Tags），确保透明度。
*   **验证智能体 (Validation Agent)**：
    *   **三层校验**：事实一致性（对比证据快照）、实体准确性（核对名称）、合规性检查。
    *   **故障类型感知修复**：针对不同错误（实体缺失、约束冲突、事实错误）采取针对性补救措施（如放宽阈值或局部修正），而非盲目重新生成。

### 3. 实验设计
*   **数据集**：使用来自北京住房平台的私有数据集，包含 5,000 个房产列表。知识图谱包含 6,016 个节点和 45,000 条边。
*   **测试场景**：100 个真实用户咨询场景，共 300 个多轮问答对。分为简单（1-2 个约束）和复杂（≥3 个约束）两类。
*   **对比基准 (Baselines)**：
    *   B1: Monolithic RAG（单提示词检索增强生成）
    *   B2: Dense+Rerank（深度检索+重排序）
    *   B3: GraphRAG-Fixed（固定使用图检索）
    *   B4: LLM-Ranker（LLM 排序）
    *   B5: Self-RAG（自我纠错）
    *   B6: Rule-Verifier（基于规则的验证）
*   **评估指标**：端到端准确率（核心指标）、nDCG@5（推荐质量）、Faithfulness（忠实度）、CSR@5（约束满足率）及延迟。

### 4. 资源与算力
*   **模型使用**：文中提到使用了 **DeepSeek** 和 **Qwen-7B** 作为底层 LLM 驱动。
*   **算力细节**：论文**未明确说明**具体的 GPU 型号、数量或训练时长。由于该系统主要基于 Agent 编排和推理，其算力消耗主要集中在推理阶段的 API 调用或本地模型部署上。

### 5. 实验数量与充分性
*   **实验规模**：进行了 300 组多轮对话测试，并针对复杂查询进行了子集分析。
*   **消融实验**：针对“验证门控”、“自适应路由”、“故障修复”和“多级验证”分别做了消融测试，证明了各模块的必要性。
*   **客观性**：使用了三名经过培训的人员进行人工标注，并计算了 Cohen’s κ 系数（0.82），显示出较高的一致性和客观性。实验对比了多种主流 RAG 变体，覆盖面较广。

### 6. 主要结论与发现
*   **性能提升**：HabitatAgent 的端到端准确率达到 **95%**，远高于强基准线 Dense+Rerank 的 75%。
*   **复杂约束处理**：在复杂查询下，传统检索的约束满足率（CSR）仅为 0.08，而 HabitatAgent 保持在 0.95，证明了 GraphRAG 和自适应路由的有效性。
*   **可靠性**：验证门控机制有效防止了多轮对话中的错误累积。

### 7. 优点与亮点
*   **闭环验证机制**：引入了“验证通过才更新记忆”的逻辑，极大地增强了长程对话的稳定性。
*   **GraphRAG 的工程化应用**：通过自适应路由平衡了图检索的高精度和向量检索的低延迟。
*   **可审计性**：强制性的证据引用标签（Evidence Tags）让 AI 的每一句推荐都有据可查，符合高风险决策场景的需求。

### 8. 不足与局限
*   **地理局限性**：实验仅基于北京的数据集，不同城市的房产逻辑（如学区政策差异）可能影响系统的泛化能力。
*   **延迟成本**：虽然比固定图检索快，但 720ms 的 P95 延迟仍高于简单 RAG 系统，多智能体多次交互会带来额外的响应时间。
*   **私有数据依赖**：核心性能依赖于高质量的房产知识图谱，而构建和维护此类图谱的成本较高。
*   **评估规模**：虽然 300 个问答对在垂直领域尚可，但相比通用领域基准，样本量仍显略小。

（完）
