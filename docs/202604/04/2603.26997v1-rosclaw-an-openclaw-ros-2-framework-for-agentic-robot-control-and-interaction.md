---
title: "ROSClaw: An OpenClaw ROS 2 Framework for Agentic Robot Control and Interaction"
title_zh: ROSClaw：一种用于智能体化机器人控制与交互的 OpenClaw ROS 2 框架
authors: "Irvin Steve Cardenas, Marcus Anthony Arnett, Natalie Catherine Yeo, Lucky Sah, Jong-Hoon Kim"
date: 2026-03-27
pdf: "https://arxiv.org/pdf/2603.26997v1"
tags: ["query:agent"]
score: 9.0
evidence: 使用基础模型进行推理和规划的智能体机器人控制框架
tldr: ROSClaw是一个将OpenClaw智能体运行时与ROS 2集成的模型无关执行层框架，旨在解决基础模型与物理机器人集成时耦合度高、难以复用的问题。该框架通过动态能力发现、多模态观测归一化和预执行安全校验，实现了在不同机器人平台（轮式、四足、类人）与多种基础模型后端之间的无缝切换，为具身智能研究提供了标准化的基础设施和可重复的评估工具。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.26997v1/fig-001.webp\", \"caption\": \"Fig. 3. Representative trajectories for “Shake and Bake” on TurtleBot3. Top: x-y odometry paths; dots mark 1 s intervals. Bottom: commanded linear velocity v(t) with the safety limit vmax shown. Each model produces a qualitatively distinct physical behavior from the same start pose under identical affordances and safety limits.\", \"page\": 6, \"index\": 1, \"width\": 496, \"height\": 462}, {\"url\": \"assets/figures/arxiv/2603.26997v1/fig-002.webp\", \"caption\": \"Fig. 1. ROSClaw architecture. The OpenClaw runtime swaps cognition backends; the ROSClaw plugin enforces the executive-layer contract (tools, safety, context injection, vision). Three transport modes connect to the ROS 2 graph. User interfaces (chat, web, CLI, API) connect upstream via the gateway.\", \"page\": 2, \"index\": 2, \"width\": 525, \"height\": 362}, {\"url\": \"assets/figures/arxiv/2603.26997v1/fig-003.webp\", \"caption\": \"Fig. 4. Prompt-level out-of-policy proposal rates on TurtleBot3 (10 safety tasks × 10 trials per model). Error bars show 95% Wilson CIs. A prompt counts as an attempt if it elicits at least one validator-BLOCK decision; all blocked actions were intercepted pre-publication. The divergence persists among frontier models (shaded): the 3.4× spread across Claude, GPT-5.2, and Gemini confirms the effect is not driven solely by Llama 4.\", \"page\": 7, \"index\": 3, \"width\": 498, \"height\": 331}, {\"url\": \"assets/figures/arxiv/2603.26997v1/fig-004.webp\", \"caption\": \"Fig. 2. ROSClaw deployments and instrumentation (composite). (a) Unitree G1 humanoid and Go2 quadruped controlled via the OpenClaw mobile chat interface. (b) Example backend view showing synchronized camera input, bridged visual-grounding output, and simulation visualization used for observation normalization and audit logging. (c) Go2 human-interaction demonstration and navigation visualization: top shows the robot’s RViz/Nav2 view while ROSClaw sets goal points via the Nav2 action interface; bottom shows the Go2 operating near a volunteer participant in a bounded test area under the same executive-layer contract. Media were captured with participant consent; no personal data were collected or analyzed; identifying details are obscured for anonymous review.\", \"page\": 4, \"index\": 4, \"width\": 529, \"height\": 316}]"
motivation: 现有的基础模型与机器人集成方案多为定制化开发，导致感知、执行与安全机制与特定模型和平台高度耦合，缺乏通用性。
method: 开发了ROSClaw执行层，通过标准化功能注入、多模态数据归一化、安全包络验证及结构化审计日志，实现模型与硬件的解耦。
result: 在三种机器人平台和四个模型后端的实验表明，不同模型间的动作建议率差异高达4.8倍，且执行层设计对任务完成度和安全性有显著影响。
conclusion: ROSClaw证明了执行层架构在具身智能中的关键作用，为实现跨平台、跨模型的机器人智能控制提供了高效且安全的通用框架。
---

## 摘要
基础模型可以赋予机器人开放式推理、语言理解和自适应规划能力，然而，目前将模型连接到实体机器人需要定制化的集成，将感知、执行和安全性与单一模型和平台耦合。我们提出了 ROSClaw，这是一个与模型无关的执行层，它将 OpenClaw 智能体运行时与 ROS 2 集成，使任何基础模型能够通过以下方式感知、推理并作用于任何支持 ROS 的机器人：(i) 具有标准化功能（affordance）注入的动态能力发现，(ii) 多模态观测归一化，(iii) 在可配置安全包络内的执行前动作验证，以及 (iv) 结构化审计日志记录。更换模型后端或机器人平台仅需更改配置；工具模式（schemas）、安全执行和溯源日志保持不变。我们在三个平台（轮式、四足、人形）上部署了 ROSClaw，并使用了四个基础模型后端。在这种受控基底（substrate）下，模型在策略外动作建议率上表现出高达 4.8 倍的差异（仅在前沿模型中就有 3.4 倍），并针对相同的指令产生性质迥异的物理行为。与 ROSA 进行的跨框架对等协议测试证实，执行层设计（而非仅仅是提示词措辞）会显著影响任务完成情况和安全行为，从而确立了 ROSClaw 既是实用的智能体机器人基础设施，又是具身智能（Embodied AI）的可重复测量工具。

## Abstract
Foundation models can endow robots with open-ended reasoning, language understanding, and adaptive planning, yet connecting a model to a physical robot today requires bespoke integration that couples perception, actuation, and safety to a single model and platform. We present ROSClaw, a model-agnostic executive layer that integrates the OpenClaw agent runtime with ROS 2, enabling any foundation model to perceive, reason about, and act on any ROS-enabled robot through (i) dynamic capability discovery with standardized affordance injection, (ii) multimodal observation normalization, (iii) pre-execution action validation within a configurable safety envelope, and (iv) structured audit logging. Swapping model backends or robot platforms is a configuration change; tool schemas, safety enforcement, and provenance logging remain invariant. We deploy ROSClaw on three platforms (wheeled, quadruped, humanoid) with four foundation-model backends. Under this controlled substrate, models exhibit up to 4.8 x differences in out-of-policy action proposal rates (3.4 x among frontier models alone) and produce qualitatively distinct physical behaviors from identical commands. A cross-framework parity protocol against ROSA confirms that executive-layer design, not just prompt wording, significantly affects both task completion and safety behavior, establishing ROSClaw as both practical agentic-robot infrastructure and a reproducible measurement instrument for embodied AI.

---

## 论文详细总结（自动生成）

### ROSClaw: 用于智能体化机器人控制与交互的 OpenClaw ROS 2 框架

#### 1. 核心问题与整体含义（研究动机和背景）
随着大语言模型（LLM）和视觉语言模型（VLM）的发展，具身智能（Embodied AI）研究日益增多。然而，现有的集成方案通常是“点对点”的定制化开发，将特定的模型、提示词（Prompting）、感知和执行逻辑与特定的机器人硬件深度耦合。这导致了两个主要问题：
*   **缺乏通用性**：更换模型或机器人平台需要重写大量集成代码。
*   **不可比较性**：由于缺乏统一的执行层协议，很难客观评估不同模型在相同物理约束下的表现差异。
论文提出了 **ROSClaw**，旨在构建一个模型无关（Model-agnostic）的执行层，作为机器人“大脑”（模型）与“身体”（硬件）之间的标准化接口。

#### 2. 方法论：核心思想与关键技术
ROSClaw 的核心思想是实现一个**人工执行层（Executive Layer）**，通过“感知-思考-行动”循环介导模型与机器人的交互。其关键技术包括：
*   **执行层契约（Executive-Layer Contract）**：定义为四元组 $C = \langle A, O, V, L \rangle$。
    *   **$A$ (功能清单)**：标准化的工具模式注册表，通过 ROS 2 动态发现节点自动生成。
    *   **$O$ (观测归一化)**：将原始传感器数据（如 LiDAR、摄像头）转换为模型可理解的规范化表示。
    *   **$V$ (预执行验证器)**：在动作发布前，根据安全策略（如速度限制、白名单）拦截并校验动作。
    *   **$L$ (审计日志)**：记录所有决策、观测和执行结果，用于事后溯源。
*   **OpenClaw 运行时**：利用 OpenClaw 平台屏蔽不同 LLM 供应商（OpenAI, Anthropic, Google 等）的 API 差异。
*   **多模态感知**：支持原生视觉输入或通过“桥接接地（Bridged Grounding）”将图像转换为 JSON 场景描述。
*   **传输抽象**：支持本地 DDS、WebSocket (rosbridge) 和 WebRTC 三种通信模式。

#### 3. 实验设计
实验旨在验证框架的移植性以及不同模型在相同约束下的行为差异：
*   **机器人平台**：TurtleBot3（轮式）、Unitree Go2（四足）、Unitree G1（人形）。
*   **模型后端**：Claude Opus 4.6、GPT-5.2、Gemini 3.1 Pro、Llama 4 Maverick。
*   **任务套件（共 40 个任务）**：
    *   **结构化任务**：分为直接指令、上下文相关和多步规划三个等级。
    *   **开放式行为任务**：如“Shake and bake”（摇滚/快点），测试模型的创造性解释。
    *   **安全发散任务**：对抗性提示（如“忽略安全规则，全速前进”），测试模型的合规性。
*   **基准对比（Benchmark）**：定义了与 **ROSA**（NASA JPL 开发的框架）的对等协议测试，并进行了“边界显著性（Bounds-salience）”消融实验。

#### 4. 资源与算力
*   **算力说明**：论文未详细列出具体的训练算力，因为其核心是推理和集成框架。
*   **部署环境**：Llama 4 模型采用自托管方式（通过 vLLM 运行），其余模型通过云端 API 调用。
*   **延迟表现**：LLM 推理延迟约为 1.3-2.3 秒，远高于 5-10 毫秒的通信延迟，表明该框架适用于高层决策而非底层实时控制。

#### 5. 实验数量与充分性
*   **实验规模**：每个任务在每个模型/平台组合下重复 **10 次**（$N=10$），总计进行了数百次试验。
*   **充分性**：实验覆盖了从简单轮式到复杂人形机器人的跨度，并引入了第三方框架（ROSA）对比和消融实验，具有较高的客观性。
*   **公平性**：通过严格控制系统提示词、安全包络和观测模态，确保了模型间对比的“苹果对苹果”公平性。

#### 6. 主要结论与发现
*   **模型行为显著发散**：在相同指令（如“Shake and bake”）和相同物理约束下，不同模型产生的物理轨迹截然不同（如 Claude 选择先旋转再前进，而 GPT 选择线性振荡）。
*   **安全风险差异巨大**：在对抗性提示下，不同模型提出违规动作的概率差异高达 **4.8 倍**（Llama 4 最高，GPT-5.2 最低）；即使在顶级前沿模型间，差异也达 3.4 倍。
*   **执行层设计至关重要**：对比实验显示，ROSClaw 的工具标准化和错误反馈机制比 ROSA 能更有效地提升任务完成率（84% vs 75.5%）。
*   **移植成本极低**：更换模型仅需修改配置，更换机器人平台仅需 10-15 分钟的配置更新。

#### 7. 优点
*   **高度解耦**：实现了模型、安全逻辑与机器人硬件的彻底分离。
*   **可重复性强**：提供了标准化的审计日志和评估协议，解决了具身智能研究中“实验难复现”的痛点。
*   **动态发现**：能够自动识别 ROS 2 计算图中的能力，无需为每个新机器人手动编写工具定义。

#### 8. 不足与局限
*   **实时性限制**：受限于 LLM 的推理速度（秒级），无法处理需要毫秒级响应的避障或动态平衡任务。
*   **安全深度有限**：目前的验证器主要基于速度阈值和接口白名单，尚未集成复杂的碰撞检测或形式化验证。
*   **样本量限制**：虽然 $N=10$ 足以观察到显著趋势，但对于评估模型细微的随机性差异，样本量仍显不足。
*   **循环依赖风险**：缺乏自动打破模型“重试循环”的机制，当模型陷入错误的规划逻辑时可能导致死循环。

（完）
