---
title: "Learning to Learn-at-Test-Time: Language Agents with Learnable Adaptation Policies"
title_zh: 学习在测试时学习：具有可学习自适应策略的语言智能体
authors: "Zhanzhi Lou, Hui Chen, Yibo Li, Qian Wang, Bryan Hooi"
date: 2026-04-01
pdf: "https://arxiv.org/pdf/2604.00830v2"
tags: ["query:agent"]
score: 9.0
evidence: 语言智能体在测试时的可学习自适应策略
tldr: 针对现有语言智能体在测试时学习（TTL）中依赖手工设计适配策略的局限，本文提出 Meta-TTL 框架。该框架将适配策略的发现建模为双层优化问题，通过外层循环的进化搜索在多样化任务上优化策略，使智能体能根据历史交互经验自动改进后续行为。实验证明，Meta-TTL 在 Jericho 和 WebArena-Lite 任务中显著优于手工基准，展现了强大的泛化能力。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.00830v2/fig-001.webp\", \"caption\": \"Table 1: Jericho ID Results. Comparison of single-agent baselines and actor+meta-agent methods on meta-training games. Single-agent baselines are shown at the top for reference; within each meta-agent block, META-TTL improves overall performance over the Naive variant.\", \"page\": 7, \"index\": 1, \"width\": 822, \"height\": 243}, {\"url\": \"assets/figures/arxiv/2604.00830v2/fig-002.webp\", \"caption\": \"Table 2: Jericho OOD Results. Zero-shot generalization on held-out games, with singleagent baselines shown at the top for reference. META-TTL generally improves performance across all three meta-agent backbones.\", \"page\": 7, \"index\": 2, \"width\": 822, \"height\": 247}, {\"url\": \"assets/figures/arxiv/2604.00830v2/fig-003.webp\", \"caption\": \"Table 4: WebArena-Lite OOD Results. Zero-shot generalization on out-of-distribution domains, with single-agent baselines shown at the top for reference. META-TTL improves OOD average performance for all meta-model backbones.\", \"page\": 8, \"index\": 3, \"width\": 822, \"height\": 268}, {\"url\": \"assets/figures/arxiv/2604.00830v2/fig-004.webp\", \"caption\": \"Table 3: WebArena-Lite ID Results. Comparison of single-agent baselines and actor+metaagent methods on in-distribution domains. Within each meta-agent block, META-TTL improves average performance across all meta-agent models.\", \"page\": 8, \"index\": 4, \"width\": 820, \"height\": 263}, {\"url\": \"assets/figures/arxiv/2604.00830v2/fig-005.webp\", \"caption\": \"Figure 2: Overview of META-TTL. Outer loop (meta-training): A proposer LM reflects and proposes candidate meta-prompts, which are validated locally and globally before entering a per-task expert pool. After training, a single optimized meta-prompt ϕ∗ is selected from this pool. Inner loop (test-time learning): The meta-agent, governed by ϕ∗, observes the actor’s trajectory after each episode and generates verbal feedback that rewrites the actor’s system prompt for the next attempt.\", \"page\": 4, \"index\": 5, \"width\": 822, \"height\": 369}, {\"url\": \"assets/figures/arxiv/2604.00830v2/fig-006.webp\", \"caption\": \"Figure 3: Per-episode score trajectories on the six Jericho evaluation games. META-TTL exhibits clearer upward trends across episodes than the baselines, supporting W-AUC as a metric of sustained test-time improvement.\", \"page\": 9, \"index\": 6, \"width\": 824, \"height\": 406}]"
motivation: 现有的测试时学习方法依赖于人工设计的固定适配策略，无法针对特定任务环境实现最优的性能提升。
method: 提出 Meta-TTL 框架，利用双层优化结构，通过内层 TTL 过程评估适配效果，并利用外层进化搜索在训练任务分布上迭代优化适配策略。
result: 在 Jericho 和 WebArena-Lite 基准测试中，Meta-TTL 在分布内和分布外设置下均一致优于手工设计的基准方法。
conclusion: 学习得到的适配策略能够编码可迁移的策略，有效提升语言智能体在推理阶段的自我改进能力和泛化性能。
---

## 摘要
测试时学习（TTL）使语言智能体能够在推理阶段通过与环境的重复交互来迭代优化其性能。TTL 的核心是一个自适应策略，它根据先前回合的经验更新执行者策略，从而改善未来的行为。现有方法依赖于固定的、手工设计的自适应策略，而不是针对下游性能提升进行优化。我们认为，最优的自适应策略应该从任务环境中学习，而不是基于人类直觉进行手工设计。为了实现这一目标，我们引入了 Meta-TTL，这是一个将有效自适应策略的发现建模为双层优化问题的框架。在该框架内，内循环执行标准的 TTL 过程，衡量候选自适应策略在连续回合中帮助智能体纠正错误的有效性。在外循环中，受智能体性能的引导，通过在多样化的训练任务分布上进行进化搜索，来迭代优化自适应策略。我们在 Jericho 和 WebArena-Lite 上，针对分布内（ID）和分布外（OOD）设置，使用多种元智能体骨干网络对 Meta-TTL 进行了评估。两个基准测试的结果均显示，Meta-TTL 一致优于手工设计的基准方法，这表明优化后的自适应策略编码了可迁移的策略，能够泛化到训练任务分布之外。

## Abstract
Test-Time Learning (TTL) enables language agents to iteratively refine their performance through repeated interactions with the environment at inference time. At the core of TTL is an adaptation policy that updates the actor policy based on experience from previous episodes, thereby improving future behavior. Existing methods rely on fixed, hand-crafted adaptation policies rather than optimizing them for downstream improvement. We argue that optimal adaptation policies should be learned from task environments, not hand-engineered based on human intuition. To achieve this, we introduce Meta-TTL, a framework that formulates the discovery of effective adaptation policies as a bi-level optimization problem. Within this framework, the inner loop executes the standard TTL process, measuring how effectively a candidate adaptation policy helps an agent correct errors across sequential episodes. Guided by the agent's performance, the outer loop employs evolutionary search over a diverse distribution of training tasks to iteratively refine the adaptation policy. We evaluate Meta-TTL on Jericho and WebArena-Lite across both in-distribution (ID) and out-of-distribution (OOD) settings, using multiple meta-agent backbones. Results on both benchmarks show that Meta-TTL consistently outperforms hand-crafted baselines, suggesting that the optimized adaptation policy encodes transferable strategies that generalize beyond the training task distribution.

---

## 论文详细总结（自动生成）

这是一份关于论文《Learning to Learn-at-Test-Time: Language Agents with Learnable Adaptation Policies》的结构化深入总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的语言智能体（LLM Agents）在面对新环境时，往往无法通过重复尝试实现有效的“测试时学习”（Test-Time Learning, TTL）。
*   **研究动机**：
    *   **现有局限**：目前的 TTL 方法（如 Reflexion）通常依赖于手工设计的、固定的自适应策略（即如何根据失败经验修改下一步行为）。这些策略基于人类直觉，而非针对特定任务环境的最优解。
    *   **核心观点**：作者认为“如何学习”本身应该是可学习的。有效的自适应策略应从任务分布中自动发现，从而使智能体在推理阶段能够根据历史交互轨迹（Trajectories）更精准地自我改进。

### 2. 方法论：Meta-TTL 框架
Meta-TTL 将自适应策略的发现建模为一个**双层优化（Bi-level Optimization）**问题：

*   **核心思想**：通过在训练任务集上进行进化搜索，优化一个“元提示词”（Meta-Prompt $\phi$），该提示词指导元智能体（Meta-Agent）如何重写执行者（Actor）的系统提示词。
*   **关键技术细节**：
    *   **内层循环（Inner Loop）**：执行标准的 TTL 过程。Actor 在任务 $g$ 中进行 $K$ 个回合的尝试。每回合结束后，Meta-Agent 根据历史轨迹 $H_k$ 和当前策略 $\pi_k$，生成更新后的 Actor 提示词 $\rho_{k+1}$。
    *   **外层循环（Outer Loop）**：进化元训练。使用一个“提议 LLM”（Proposer）对当前的元提示词进行反思和修改。
    *   **算法流程**：
        1.  **提议与局部验证**：从专家池中采样元提示词，在训练任务上运行 TTL，Proposer 生成新候选并进行局部验证。
        2.  **全局验证与专家池更新**：通过局部验证的候选在整个验证集上评估，若在某任务上表现最优，则更新该任务的“专家”。
        3.  **专家选择**：训练结束后，选择在验证集上平均表现（经 Z-score 标准化）最好的元提示词作为最终策略 $\phi^*$。
*   **评价指标**：采用加权学习曲线下面积（W-AUC），对后期回合赋予更高权重，以奖励持续的改进。

### 3. 实验设计
*   **数据集/场景**：
    *   **Jericho**：交互式小说（文字冒险游戏），具有密集的得分奖励。
    *   **WebArena-Lite**：网页导航任务，具有稀疏的二元奖励（成功/失败）。
*   **实验设置**：
    *   **ID（分布内）**：在训练过的游戏/领域内评估。
    *   **OOD（分布外）**：在完全未见过的游戏/领域上评估泛化能力。
*   **对比方法**：
    *   **Static**：无自适应。
    *   **Reflexion**：经典的手工反馈方法。
    *   **Memory Agent**：带有进化内存的方法。
    *   **Naive Meta-Agent**：使用未优化的初始元提示词。
*   **模型骨干**：使用了 Gemini 3 Flash, GLM-5, 和 GPT-5 作为元智能体（注：原文中使用的模型版本号较高，可能为预设或特定版本）。

### 4. 资源与算力
*   **算力说明**：论文未明确提及具体的 GPU 型号或数量。
*   **训练时长**：文中提到一个典型的实验案例：GPT-5 元智能体在 Jericho ID 任务上的优化过程经历了 **26 次迭代**，总耗时约 **27 小时**。
*   **成本特征**：由于该方法基于 Prompt 搜索而非权重微调，主要的算力消耗在于调用 LLM API 进行大量的轨迹生成和验证。

### 5. 实验数量与充分性
*   **实验规模**：在两个截然不同的基准（文字游戏 vs 网页操作）上进行了测试，涵盖了 ID 和 OOD 场景。
*   **消融与对比**：针对三种不同的主流大模型骨干分别进行了独立的元训练和评估，证明了方法的普适性。
*   **客观性**：
    *   引入了 **Z-score 标准化** 来处理不同任务间奖励尺度不一的问题，确保专家选择的公平性。
    *   通过每回合得分轨迹图（Learning Curves）直观展示了改进的稳定性，而非仅依赖单一的最终得分。

### 6. 主要结论与发现
*   **性能提升**：Meta-TTL 在所有基准上均显著优于手工设计的基准。在 Jericho ID 上，平均得分从 50.4 提升至 110.8（约 120% 提升）。
*   **强泛化性**：学习到的自适应策略在 OOD 任务上依然有效，证明其捕获的是通用的“学习策略”而非简单的任务知识。
*   **策略涌现**：优化后的元提示词自动演化出了**信用分配（Credit Assignment）**、**受控探索（Exploration Management）**和**条件化知识库**等高级策略。

### 7. 优点（亮点）
*   **范式创新**：将“测试时学习”从手工设计转向自动化优化，提出了“学习如何学习”的智能体新范式。
*   **可解释性**：最终产出的是自然语言形式的元提示词，人类可以直观理解智能体学到了哪些自适应规则。
*   **灵活性**：无需更新模型权重，生成的元提示词具有高度的可移植性，可跨模型骨干使用。

### 8. 不足与局限
*   **奖励依赖性**：在奖励信号极其稀疏的环境（如 WebArena）中，进化搜索的效率和提升幅度明显低于奖励密集的场景（如 Jericho）。
*   **搜索成本**：进化搜索过程需要大量的环境交互和 API 调用，对于实时性要求极高的场景可能不适用。
*   **Proposer 偏差**：优化效果高度依赖于作为 Proposer 的大模型的能力，若 Proposer 本身反思能力不足，可能陷入局部最优。

（完）
