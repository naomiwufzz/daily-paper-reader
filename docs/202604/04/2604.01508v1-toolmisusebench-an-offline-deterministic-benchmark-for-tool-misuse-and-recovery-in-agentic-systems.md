---
title: "ToolMisuseBench: An Offline Deterministic Benchmark for Tool Misuse and Recovery in Agentic Systems"
title_zh: ToolMisuseBench：智能体系统中工具误用与恢复的离线确定性基准测试
authors: "Akshey Sigdel, Rista Baral"
date: 2026-04-02
pdf: "https://arxiv.org/pdf/2604.01508v1"
tags: ["query:agent"]
score: 10.0
evidence: 智能体系统中工具误用与恢复的基准测试
tldr: 本研究针对智能体在工具使用中因参数错误、接口漂移及恢复能力弱导致的失败，提出了ToolMisuseBench。这是一个包含6800个任务的离线确定性基准测试，涵盖CRUD、检索和调度等场景，通过故障注入评估智能体在特定预算下的错误恢复与效率。该基准为提升智能体鲁棒性提供了可复现的评估框架，填补了工具误用研究的空白。
source: arxiv
selection_source: fresh_fetch
motivation: 智能体在实际应用中常因无效参数、接口变动和低效重试等操作性问题而非语言理解问题导致工具调用失败。
method: 构建了一个包含6800个任务的离线确定性基准，通过可重现的故障注入评估智能体在步数、调用和重试预算下的恢复表现。
result: 实验表明感知模式的方法能提升特定故障的恢复率，但在严格授权和硬故障设置下，智能体的整体成功率依然较低。
conclusion: ToolMisuseBench 揭示了当前智能体在处理工具误用方面的局限性，并为开发更具鲁棒性的工具调用策略提供了重要参考。
---

## 摘要
即使在语言理解能力很强的情况下，使用工具的智能体也常因操作性原因而失败。常见原因包括无效参数、接口漂移、恢复能力弱以及重试行为效率低下。我们推出了 ToolMisuseBench，这是一个离线确定性基准测试，用于在明确的步骤、调用和重试预算下评估工具的误用与恢复。该基准测试涵盖了增删改查（CRUD）、检索、文件和调度环境，并支持可重现的故障注入。它报告成功率、无效调用行为、策略违规、恢复质量以及预算效率。我们发布了一个包含 6800 个任务的公开数据集和一个可重复的评估流水线。基准测试结果显示，模式感知（schema-aware）方法在特定故障恢复方面有所提升，但在发布的授权和硬故障设置下，整体成功率仍然有限。

## Abstract
Tool using agents often fail for operational reasons even when language understanding is strong. Common causes include invalid arguments, interface drift, weak recovery, and inefficient retry behavior. We introduce ToolMisuseBench, an offline deterministic benchmark for evaluating tool misuse and recovery under explicit step, call, and retry budgets. The benchmark covers CRUD, retrieval, file, and scheduling environments with replayable fault injection. It reports success, invalid call behavior, policy violations, recovery quality, and budgeted efficiency. We release a public dataset with 6800 tasks and a reproducible evaluation pipeline. Baseline results show fault specific recovery gains for schema aware methods, while overall success remains limited under the released authorization and hard failure settings.

---

## 论文详细总结（自动生成）

这是一份关于论文《ToolMisuseBench: An Offline Deterministic Benchmark for Tool Misuse and Recovery in Agentic Systems》的结构化深入总结：

### 1. 论文的核心问题与整体含义（研究动机和背景）
在实际应用中，集成工具的语言智能体（Agents）往往并非因为语义理解能力不足而失败，而是由于**操作性可靠性**问题。这些问题包括：无效的工具参数、接口模式漂移（Schema Drift）、不稳定的故障后行为以及低效的重试逻辑。
现有的基准测试（如 HELM 或 BIG-bench）侧重于通用能力，但在隔离工具调用错误、接口变化及严格预算下的恢复能力方面存在不足。论文旨在填补这一空白，提供一个**离线、确定性且可重现**的基准测试，专门用于评估智能体在面对工具误用时的鲁棒性和恢复效率。

### 2. 论文提出的方法论
*   **核心思想**：通过确定性模拟器和声明式故障计划（Fault Plans），在受控的预算（步骤、调用次数、重试次数）内，对智能体进行压力测试。
*   **关键技术细节**：
    *   **确定性故障注入**：支持五类故障：模式漂移（接口变更）、速率限制（Rate Limit）、超时（Timeout）、授权失败（Authorization）以及对抗性错误重写（模糊反馈）。
    *   **任务构成**：每个任务包含指令、工具模式、初始状态、成功标准、故障计划和预算限制。
    *   **评估指标**：除了最终成功率，还引入了**预算感知成功率 $S(k)$**，即在不超过 $k$ 次工具调用的前提下完成任务的比例，通过计算该曲线下的面积（AUC）来衡量效率。
    *   **验证机制**：结合状态检查（如字典值匹配）和转录检查（如工具调用次数），确保任务完成的真实性。

### 3. 实验设计
*   **数据集/场景**：发布了包含 **6800 个任务** 的数据集（5000 训练 / 800 开发 / 1000 测试），涵盖 CRUD（增删改查）、检索、文件操作和日程调度四个领域。
*   **对比方法（Baselines）**：
    1.  **确定性启发式策略（Heuristic）**：基础的规则化调用。
    2.  **模式修复策略（Schema Repair）**：在基础策略上增加了针对参数错误的模式感知修复。
    3.  **策略感知层（Policy Aware）**：在模式修复基础上增加了一个轻量级的策略意识层。
*   **Benchmark 特性**：所有实验在完全离线的模拟环境中运行，确保了 100% 的可复现性。

### 4. 资源与算力
*   论文**未明确说明**具体的 GPU 型号、数量或训练时长。
*   由于该基准测试主要基于**离线确定性模拟器**和现成的模型推理（作为 Agent 核心），其评估过程对算力的需求主要取决于所调用的语言模型，而非大规模的基准测试环境本身。

### 5. 实验数量与充分性
*   **实验规模**：在 1000 个公共测试任务上进行了评估，涵盖了多种故障组合。
*   **充分性与公平性**：
    *   实验对比了三种不同复杂度的基线方法，并针对每种故障类型（超时、漂移等）进行了细分分析。
    *   **公平性**：所有基线方法使用相同的观察接口，无特权访问，且在相同的预算限制下运行。
    *   **客观性**：通过确定性种子和声明式元数据消除了环境随机性，使得对比非常客观。

### 6. 论文的主要结论与发现
*   **恢复能力的局限性**：模式感知和策略感知方法在处理“超时”和“模式漂移”故障时表现出明显的恢复增益（恢复率约 50%），但在“授权失败”和“速率限制”等硬性故障下，所有基线方法的成功率均为 0。
*   **效率与成功的脱钩**：启发式基线虽然调用次数最少，但成功率并未提高；修复类方法虽然增加了调用次数并提升了局部恢复率，但整体成功率仍受限于故障混合比例。
*   **瓶颈在于规划**：预算成功率曲线（AUC）在不同 Agent 间表现持平，表明当前的瓶颈在于高层规划和策略质量，而非简单的预算增加。

### 7. 优点
*   **确定性与可复现性**：通过模拟器和固定种子解决了 Agent 评估中常见的随机性问题，支持故障的精确重放。
*   **细粒度归因**：能够清晰区分失败是源于调用构造错误、违反策略还是恢复能力不足。
*   **预算感知评估**：引入调用次数和重试预算，更符合真实生产环境对成本和效率的要求。

### 8. 不足与局限
*   **环境合成性**：虽然提高了受控性，但合成环境可能无法完全覆盖真实生产工具的所有语义复杂性。
*   **基线多样性有限**：目前仅测试了简单的启发式和修复策略，缺乏对最先进的大规模强化学习 Agent 或复杂模型适配器的评估。
*   **现实因素缺失**：尚未模拟货币成本（Token 计费）、网络抖动分布或异构服务的 SLA 保证。
*   **静态故障分类**：随着工具生态演进，可能会出现当前五类故障之外的新型误用模式。

（完）
