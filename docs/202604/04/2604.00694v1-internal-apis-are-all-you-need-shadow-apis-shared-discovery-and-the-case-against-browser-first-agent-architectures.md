---
title: "Internal APIs Are All You Need: Shadow APIs, Shared Discovery, and the Case Against Browser-First Agent Architectures"
title_zh: 内部 API 正是你所需要的：影子 API、共享发现以及反对浏览器优先的智能体架构
authors: "Lewis Tham, Nicholas Mac Gregor Garcia, Jungpil Hahn"
date: 2026-04-01
pdf: "https://arxiv.org/pdf/2604.00694v1"
tags: ["query:agent"]
score: 9.0
evidence: 智能体使用内部API而非基于浏览器的交互
tldr: 针对AI智能体在网页交互中面临的DOM解析慢、稳定性差等问题，本文提出Unbrowse系统。该系统通过挖掘网站现有的内部API（影子API），构建了一个共享的路由图索引，使智能体能直接调用API而非模拟浏览器操作。实验显示，该方法相比传统自动化工具提速显著，并引入了基于微支付的激励机制，为构建高效的“智能体原生Web”提供了新架构。
source: arxiv
selection_source: fresh_fetch
motivation: 现有的浏览器优先架构导致智能体在处理复杂DOM和逆向工程API时效率低下、成本高昂且存在大量重复劳动。
method: 提出Unbrowse系统，通过被动学习流量构建共享路由图，并结合三路径执行模型及基于x402协议的微支付激励机制。
result: 在94个域名的测试中，API直接调用比Playwright自动化平均提速3.6倍，中位数提速5.4倍，且缓存路由响应可低于100毫秒。
conclusion: 转向以内部API为核心的共享索引架构能显著提升智能体性能，是解决智能体与网页交互效率瓶颈的关键路径。
---

## 摘要
自主智能体越来越多地与 Web 交互，但大多数网站仍是为人类浏览器设计的——这是新兴的“智能体网络”（Agentic Web）必须解决的根本性不匹配。智能体必须反复浏览页面、检查 DOM 并逆向工程可调用路由，这一过程缓慢、脆弱，且在不同智能体之间冗余重复。我们观察到，每个现代网站在其用户界面背后都已经暴露了内部 API（有时称为“影子 API”），即驱动网站自身功能的第一方端点。我们提出了 Unbrowse，这是一个共享路由图，它将基于浏览器的路由发现转化为这些可调用第一方接口的集体维护索引。该系统从真实的浏览流量中被动学习路由，并通过直接 API 调用提供缓存路由。在跨 94 个域的等效信息检索任务的单主机实时 Web 基准测试中，完全预热的缓存执行平均耗时 950 毫秒，而 Playwright 浏览器自动化为 3,404 毫秒（平均加速 3.6 倍，中位数加速 5.4 倍），且缓存良好的路由在 100 毫秒内即可完成。一种包含本地缓存、共享图或浏览器回退的三路径执行模型确保了系统的自愿性和自我修正。通过 x402 协议实现的三层微支付模型对图查询收取单次查询搜索费（第 3 层），对发现文档收取一次性安装费（第 1 层），并为选择加入的网站所有者提供可选的单次执行费（第 2 层）。所有层级都基于理性采用的必要条件：只有当总费用低于浏览器重新发现的预期成本时，智能体才会使用共享图。

## Abstract
Autonomous agents increasingly interact with the web, yet most websites remain designed for human browsers -- a fundamental mismatch that the emerging ``Agentic Web'' must resolve. Agents must repeatedly browse pages, inspect DOMs, and reverse-engineer callable routes -- a process that is slow, brittle, and redundantly repeated across agents. We observe that every modern website already exposes internal APIs (sometimes called \emph{shadow APIs}) behind its user interface -- first-party endpoints that power the site's own functionality. We present Unbrowse, a shared route graph that transforms browser-based route discovery into a collectively maintained index of these callable first-party interfaces. The system passively learns routes from real browsing traffic and serves cached routes via direct API calls. In a single-host live-web benchmark of equivalent information-retrieval tasks across 94 domains, fully warmed cached execution averaged 950\,ms versus 3{,}404\,ms for Playwright browser automation (3.6$\times$ mean speedup, 5.4$\times$ median), with well-cached routes completing in under 100\,ms. A three-path execution model -- local cache, shared graph, or browser fallback -- ensures the system is voluntary and self-correcting. A three-tier micropayment model via the x402 protocol charges per-query search fees for graph lookups (Tier~3), a one-time install fee for discovery documentation (Tier~1), and optional per-execution fees for site owners who opt in (Tier~2). All tiers are grounded in a necessary condition for rational adoption: an agent uses the shared graph only when the total fee is lower than the expected cost of browser rediscovery.

---

## 论文详细总结（自动生成）

这是一份关于论文《Internal APIs Are All You Need: Shadow APIs, Shared Discovery, and the Case Against Browser-First Agent Architectures》的结构化深入分析总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：当前的 Web 智能体（Agents）主要采用“浏览器优先”架构，通过模拟人类行为（解析 DOM、点击 UI）与网页交互。这种方式存在**速度慢、成本高、易碎性强**（UI 变动即失效）以及**重复劳动**（不同智能体在同一网站重复进行逆向工程）等痛点。
*   **研究背景**：作者观察到现代网站在 UI 背后普遍存在供其自身前端调用的“内部 API”（即影子 API）。如果智能体能直接调用这些 API，效率将大幅提升。论文旨在构建一种“智能体原生 Web”基础设施，通过共享这些 API 路由信息来消除“发现税”。

### 2. 论文提出的方法论
*   **核心思想**：提出 **Unbrowse** 系统，通过被动观察真实浏览流量来学习 API 路由，并构建一个集体维护的共享路由图索引。
*   **关键技术细节**：
    *   **三路径执行模型**：
        1.  **Path 1 (本地缓存)**：直接从本地调用已知的 API 路由。
        2.  **Path 2 (共享图)**：若本地无缓存，从云端索引下载由其他智能体发现的“技能包”（Skill）。
        3.  **Path 3 (浏览器回退)**：若无已知路由，启动轻量级浏览器（Kuri）进行实时捕获和逆向工程，并将新路由发布至共享图。
    *   **Kuri 代理**：一个基于 Zig 语言编写的轻量级 CDP（Chrome DevTools Protocol）代理，替代沉重的 Playwright 框架。
    *   **经济模型 (x402 协议)**：引入基于区块链的微支付机制，分为三层：
        *   **Tier 1 (安装费)**：一次性支付给路由贡献者。
        *   **Tier 2 (执行费)**：可选，支付给选择加入该生态的网站所有者。
        *   **Tier 3 (搜索费)**：支付给维护索引的平台。
*   **核心公式**：智能体采用共享路由的理性条件为：$f_{route} < c_{rediscovery}$。即：使用共享路由的总费用必须低于智能体自行通过浏览器重新发现该路由的预期成本（包括延迟、算力、Token 消耗及失败重试成本）。

### 3. 实验设计
*   **数据集/场景**：从 Unbrowse 路由图中随机选取了 **94 个真实域名**，涵盖政府、SaaS、电商、医疗、金融、媒体和社交网络。
*   **Benchmark**：模拟 WebArena 的信息检索任务（例如：获取商品价格、提取新闻列表）。
*   **对比方法**：
    *   **Playwright 浏览器自动化**：作为基准线，执行完整的页面加载、JS 渲染和 CSS 选择器提取。
    *   **Unbrowse (Warmed Cache)**：完全预热状态下的直接 API 调用。
    *   **Unbrowse (Cold Start)**：从零开始的路由发现过程。

### 4. 资源与算力
*   **硬件环境**：实验在一台配备 **Apple M4 Max 芯片、64GB 统一内存** 的 MacBook Pro 上完成。
*   **网络环境**：位于新加坡的住宅宽带（约 28 Mbps 下行）。
*   **训练算力**：由于该系统主要基于启发式过滤和向量搜索，**文中未提及大规模 GPU 训练需求**。其核心逻辑在于系统架构的优化而非模型参数的训练。

### 5. 实验数量与充分性
*   **实验规模**：
    *   对 94 个域名进行了热缓存对比测试，每项任务运行 3 次取均值。
    *   对 20 个新域名进行了冷启动性能测试。
*   **充分性评价**：实验覆盖了多样化的真实网站，具有较强的代表性。对比实验考虑了 DNS 和 TCP 连接重用的干扰。
*   **客观性**：作者诚实地指出了在重度反爬（如 Cloudflare 保护）网站上加速效果会减弱，并公开了基准测试的源代码和原始数据。但实验受限于单机环境和特定地理位置（新加坡），可能存在一定的网络延迟偏差。

### 6. 主要结论与发现
*   **显著提速**：在热缓存状态下，Unbrowse 平均耗时 **950ms**，而 Playwright 需 **3,404ms**。**平均加速 3.6 倍，中位数加速 5.4 倍**。部分 API 响应极快的网站加速比高达 30 倍。
*   **成本降低**：通过跳过浏览器渲染和大模型对 DOM 的推理，单次任务的 Token 和算力成本可降低 **90% 以上**。
*   **冷启动开销**：首次发现路由平均需 12.4 秒，但该成本仅需支付一次，通常在 3-5 次复用后即可实现盈亏平衡。

### 7. 优点
*   **架构创新**：打破了“智能体必须像人一样看网页”的思维定式，利用现有的影子 API 实现了“降维打击”。
*   **激励闭环**：通过 x402 微支付协议解决了“谁来维护路由图”的动力问题，将逆向工程转化为可获利的资产。
*   **兼容性**：支持 MCP 等主流智能体协议，可作为现有 Agent 框架（如 Claude Code, Cursor）的插件直接使用。

### 8. 不足与局限
*   **反爬风险**：非浏览器流量更容易触发高级 Web 应用防火墙（WAF）的拦截。
*   **动态鉴权难题**：对于需要复杂 OAuth 流程或频繁更换 Token 的网站，自动化的 API 调用仍面临挑战。
*   **地理位置偏差**：所有测试均在新加坡进行，全球不同地区的 CDN 响应可能导致结果差异。
*   **安全性隐患**：虽然凭证存储在本地，但共享路由图可能被恶意贡献者注入钓鱼端点（尽管有信任评分机制，但仍存在风险）。
*   **法律灰色地带**：对于未明确授权 API 访问的网站，这种“影子 API”调用在法律和道德层面仍存在争议（尽管论文提到了 opt-in 补偿模型）。

（完）
