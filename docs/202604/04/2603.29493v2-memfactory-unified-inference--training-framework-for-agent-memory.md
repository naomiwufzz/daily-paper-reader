---
title: "MemFactory: Unified Inference & Training Framework for Agent Memory"
title_zh: MemFactory：智能体记忆的统一推理与训练框架
authors: "Ziliang Guo, Ziheng Li, Bo Tang, Feiyu Xiong, Zhiyu Li"
date: 2026-03-31
pdf: "https://arxiv.org/pdf/2603.29493v2"
tags: ["query:agent"]
score: 9.0
evidence: 记忆增强型智能体架构的统一模块化框架
tldr: 针对记忆增强型大语言模型智能体开发中存在的实现碎片化、缺乏统一框架等问题，本文推出了首个统一且高度模块化的训练与推理框架MemFactory。该框架借鉴LLaMA-Factory的成功经验，将记忆生命周期抽象为原子化组件，支持“乐高式”构建自定义智能体，并原生集成GRPO算法以优化记忆管理策略。MemFactory支持Memory-R1等前沿范式，显著降低了开发门槛，为长时AI智能体的创新提供了标准化基础设施。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.29493v2/fig-001.webp\", \"caption\": \"Figure 1 The overall architecture of MemFactory, illustrating the interdependencies among the Modules, Agent, Environment, and Trainer layers.\", \"page\": 4, \"index\": 1, \"width\": 929, \"height\": 482}, {\"url\": \"assets/figures/arxiv/2603.29493v2/fig-002.webp\", \"caption\": \"Table 1 Standard interfaces supported by modules in the Module Layer.\", \"page\": 5, \"index\": 2, \"width\": 968, \"height\": 219}]"
motivation: 现有的记忆增强型智能体实现方案往往针对特定任务且高度碎片化，缺乏一个统一的框架来简化复杂记忆流水线的集成、训练与评估。
method: MemFactory将记忆的提取、更新和检索等环节抽象为可插拔的模块，并利用组相对策略优化（GRPO）算法，根据多维环境奖励对内部记忆管理策略进行微调。
result: "在MemAgent架构上的实验表明，MemFactory在领域内和分布外评估集中均优于基础模型，性能相对提升最高可达14.8%。"
conclusion: MemFactory通过提供标准化、可扩展且易用的基础设施，有效降低了记忆驱动型AI智能体的研究门槛，为未来长时智能体的发展奠定了基础。
---

## 摘要
记忆增强型大语言模型（LLMs）对于开发具备强大能力的长期 AI 智能体至关重要。近期，应用强化学习（RL）来优化记忆操作（如提取、更新和检索）已成为一个极具前景的研究方向。然而，现有的实现方式仍然高度碎片化且针对特定任务，缺乏统一的基础设施来简化这些复杂流水线的集成、训练和评估。为填补这一空白，我们推出了 MemFactory，这是首个专为记忆增强型智能体设计的统一、高度模块化的训练与推理框架。受 LLaMA-Factory 等统一微调框架成功的启发，MemFactory 将记忆生命周期抽象为原子化的、即插即用的组件，使研究人员能够通过“乐高式”架构无缝构建自定义记忆智能体。此外，该框架原生集成了群体相对策略优化（GRPO），以微调由多维环境奖励驱动的内部记忆管理策略。MemFactory 为近期前沿范式（包括 Memory-R1、RMM 和 MemAgent）提供开箱即用的支持。我们在开源的 MemAgent 架构上，利用其公开的训练和评估数据对 MemFactory 进行了实证验证。在域内和分布外评估集上，MemFactory 相比相应的基座模型均持续提升了性能，相对增益最高达 14.8%。通过提供标准化、可扩展且易于使用的基础设施，MemFactory 显著降低了准入门槛，为记忆驱动型 AI 智能体的未来创新铺平了道路。

## Abstract
Memory-augmented Large Language Models (LLMs) are essential for developing capable, long-term AI agents. Recently, applying Reinforcement Learning (RL) to optimize memory operations, such as extraction, updating, and retrieval, has emerged as a highly promising research direction. However, existing implementations remain highly fragmented and task-specific, lacking a unified infrastructure to streamline the integration, training, and evaluation of these complex pipelines. To address this gap, we present MemFactory, the first unified, highly modular training and inference framework specifically designed for memory-augmented agents. Inspired by the success of unified fine-tuning frameworks like LLaMA-Factory, MemFactory abstracts the memory lifecycle into atomic, plug-and-play components, enabling researchers to seamlessly construct custom memory agents via a "Lego-like" architecture. Furthermore, the framework natively integrates Group Relative Policy Optimization (GRPO) to fine-tune internal memory management policies driven by multi-dimensional environmental rewards. MemFactory provides out-of-the-box support for recent cutting-edge paradigms, including Memory-R1, RMM, and MemAgent. We empirically validate MemFactory on the open-source MemAgent architecture using its publicly available training and evaluation data. Across both in-domain and out-of-distribution evaluation sets, MemFactory consistently improves performance over the corresponding base models, with relative gains of up to 14.8%. By providing a standardized, extensible, and easy-to-use infrastructure, MemFactory significantly lowers the barrier to entry, paving the way for future innovations in memory-driven AI agents.

---

## 论文详细总结（自动生成）

以下是对论文《MemFactory: Unified Inference & Training Framework for Agent Memory》的结构化深入总结：

### 1. 核心问题与研究动机
*   **核心问题**：尽管记忆增强型大语言模型（LLM）对长期 AI 智能体至关重要，且利用强化学习（RL）优化记忆操作（提取、更新、检索）已成为前沿趋势，但目前的实现方案高度**碎片化**且**任务特定**。
*   **研究动机**：开发者缺乏一个像 LLaMA-Factory 那样统一、模块化且易于扩展的基础设施，导致复现 SOTA 算法（如 Memory-R1、MemAgent）或自定义记忆策略时面临极高的工程门槛和重复劳动。

### 2. 方法论：MemFactory 框架设计
MemFactory 采用四层解耦架构，支持“乐高式”构建记忆智能体：
*   **模块层 (Module Layer)**：将记忆生命周期抽象为原子操作。
    *   **Extractor**：从原始上下文解析事实和经验。
    *   **Updater**：执行增删改查（ADD/DEL/UPDATE/NONE）操作维护记忆库。
    *   **Retriever**：支持语义检索及基于推理模型（LRM）的重排序（Rerank）。
    *   **Agent Module**：支持端到端的循环记忆（Recurrent Memory）策略。
*   **智能体层 (Agent Layer)**：作为策略执行器，通过组合上述模块实现不同的记忆范式，并负责生成训练所需的轨迹（Rollout）。
*   **环境层 (Environment Layer)**：充当数据加载器和奖励管理器。提供 `MemoryBankEnv`（长期记忆库）和 `LongcontextEnv`（短期长上下文）两种环境，并支持格式奖励和 LLM-as-a-Judge 等多维反馈。
*   **训练层 (Trainer Layer)**：核心优化引擎。原生集成 **GRPO（群体相对策略优化）** 算法，通过组内奖励归一化估计优势函数（Advantage），无需额外的 Critic 网络，显著降低显存占用。

### 3. 实验设计
*   **实验对象**：以 **MemAgent** 架构为基础，验证框架的有效性。
*   **基座模型**：选用 Qwen3-1.7B 和 Qwen3-4B-Instruct。
*   **数据集/Benchmark**：使用 MemAgent 公开的训练和评估集。
    *   **主任务评估**：`eval_50` 和 `eval_100`（不同长度的上下文）。
    *   **分布外（OOD）评估**：`eval_fwe_16384`（测试模型的泛化能力）。
*   **对比方法**：对比了模型在 RL 微调前后的性能表现。

### 4. 资源与算力
*   **硬件环境**：实验明确指出整个训练和评估流水线可以在 **单张 NVIDIA A800 80GB GPU** 上运行。
*   **训练细节**：每个模型训练 250 个有效步数（Steps）。
*   **效率优势**：得益于 GRPO 算法和 FlashAttention-2 的集成，该框架在有限算力下即可处理长上下文记忆任务。

### 5. 实验数量与充分性
*   **实验规模**：针对两个不同规模的基座模型进行了测试，涵盖了域内和 OOD 三个测试集。
*   **可靠性**：所有评分均采用 **avg@4**（每道题进行 4 次独立试验后取平均值），以减少生成随机性带来的偏差。
*   **充分性评价**：实验设计较为客观，涵盖了从基础性能到泛化能力的维度。虽然实验主要集中在 MemAgent 范式上，但框架本身已内置了对 Memory-R1 和 RMM 的支持，展示了较好的通用性。

### 6. 主要结论与发现
*   **性能提升**：MemFactory 显著提升了基座模型的记忆管理能力。Qwen3-1.7B 在平均分上获得了 **14.8%** 的相对增益。
*   **模型规模效应**：较大的模型（Qwen3-4B-Instruct）在 RL 微调后表现更稳健，不仅在主任务上提升明显（7.3%），在 OOD 任务上也展现了更好的策略迁移能力。
*   **框架有效性**：验证了通过统一框架和 GRPO 算法，即使在轻量级算力下也能有效优化复杂的循环记忆策略。

### 7. 优点与亮点
*   **高度模块化**：首个将记忆操作原子化的框架，支持开发者通过简单的配置组合出复杂的记忆流水线。
*   **训练推理一体化**：不仅支持高效的 RL 训练，也支持通过 OpenAI 风格 API 或 vLLM 进行纯推理评估。
*   **降低门槛**：原生支持 GRPO，解决了记忆增强任务中长上下文与 RL 训练显存开销冲突的痛点。
*   **开箱即用**：预置了多种 SOTA 算法实现，具有很强的实用价值。

### 8. 不足与局限
*   **范式覆盖有限**：虽然支持三大主流范式，但对于更复杂的、涉及多智能体协作的记忆管理尚未深入探讨。
*   **训练效率空间**：论文提到在训练效率方面仍有进一步优化的空间。
*   **小模型泛化风险**：实验显示 1.7B 小模型在 OOD 任务上性能略有下降，说明轻量级模型在学习复杂记忆策略时可能存在过拟合或泛化瓶颈。

（完）
