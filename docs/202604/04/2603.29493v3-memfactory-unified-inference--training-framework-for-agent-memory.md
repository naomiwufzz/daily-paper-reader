---
title: "MemFactory: Unified Inference & Training Framework for Agent Memory"
title_zh: MemFactory：智能体记忆的统一推理与训练框架
authors: "Ziliang Guo, Ziheng Li, Bo Tang, Feiyu Xiong, Zhiyu Li"
date: 2026-03-31
pdf: "https://arxiv.org/pdf/2603.29493v3"
tags: ["query:agent"]
score: 9.0
evidence: 智能体记忆的统一训练框架
tldr: MemFactory是首个针对记忆增强型智能体设计的统一推理与训练框架，旨在解决当前记忆管理研究碎片化的问题。它借鉴LLaMA-Factory的模块化理念，将记忆生命周期抽象为可插拔的原子组件，并集成GRPO算法以优化记忆提取、更新和检索策略。该框架支持Memory-R1等前沿范式，显著降低了开发门槛，并在实验中验证了其对智能体性能的显著提升。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.29493v3/fig-001.webp\", \"caption\": \"Figure 1 The overall architecture of MemFactory, illustrating the interdependencies among the Modules, Agent, Environment, and Trainer layers.\", \"page\": 4, \"index\": 1, \"width\": 929, \"height\": 481}, {\"url\": \"assets/figures/arxiv/2603.29493v3/fig-002.webp\", \"caption\": \"Table 1 Standard interfaces supported by modules in the Module Layer.\", \"page\": 5, \"index\": 2, \"width\": 968, \"height\": 219}]"
motivation: 现有的记忆增强型大模型研究缺乏统一的基础设施，导致记忆操作的集成、训练和评估过程高度碎片化且难以复用。
method: 提出一种类乐高积木的模块化架构，将记忆生命周期原子化，并利用GRPO算法通过多维环境奖励对记忆管理策略进行微调。
result: "在MemAgent架构上的实验表明，MemFactory能有效提升模型性能，相比基础模型最高实现14.8%的相对增益。"
conclusion: MemFactory通过提供标准化、可扩展且易用的基础设施，为记忆驱动的AI智能体研究与创新提供了强有力的支撑。
---

## 摘要
记忆增强型大语言模型（LLMs）对于开发具备长期能力的 AI 智能体至关重要。近期，应用强化学习（RL）来优化记忆操作（如提取、更新和检索）已成为一个极具前景的研究方向。然而，现有的实现方式仍然高度碎片化且针对特定任务，缺乏统一的基础设施来简化这些复杂流水线的集成、训练和评估。为填补这一空白，我们推出了 MemFactory，这是首个专为记忆增强型智能体设计的统一、高度模块化的训练与推理框架。受 LLaMA-Factory 等统一微调框架成功的启发，MemFactory 将记忆生命周期抽象为原子化的、即插即用的组件，使研究人员能够通过“乐高式”架构无缝构建自定义记忆智能体。此外，该框架原生集成了群体相对策略优化（GRPO），以微调由多维环境奖励驱动的内部记忆管理策略。MemFactory 为近期前沿范式（包括 Memory-R1、RMM 和 MemAgent）提供开箱即用的支持。我们在开源的 MemAgent 架构上，利用其公开的训练和评估数据对 MemFactory 进行了实证验证。在各个评估集上，MemFactory 的性能平均优于相应的基座模型，相对提升最高达 14.8%。通过提供标准化、可扩展且易于使用的基础设施，MemFactory 显著降低了准入门槛，为记忆驱动型 AI 智能体的未来创新铺平了道路。

## Abstract
Memory-augmented Large Language Models (LLMs) are essential for developing capable, long-term AI agents. Recently, applying Reinforcement Learning (RL) to optimize memory operations, such as extraction, updating, and retrieval, has emerged as a highly promising research direction. However, existing implementations remain highly fragmented and task-specific, lacking a unified infrastructure to streamline the integration, training, and evaluation of these complex pipelines. To address this gap, we present MemFactory, the first unified, highly modular training and inference framework specifically designed for memory-augmented agents. Inspired by the success of unified fine-tuning frameworks like LLaMA-Factory, MemFactory abstracts the memory lifecycle into atomic, plug-and-play components, enabling researchers to seamlessly construct custom memory agents via a "Lego-like" architecture. Furthermore, the framework natively integrates Group Relative Policy Optimization (GRPO) to fine-tune internal memory management policies driven by multi-dimensional environmental rewards. MemFactory provides out-of-the-box support for recent cutting-edge paradigms, including Memory-R1, RMM, and MemAgent. We empirically validate MemFactory on the open-source MemAgent architecture using its publicly available training and evaluation data. Across the evaluation sets, MemFactory improves performance over the corresponding base models on average, with relative gains of up to 14.8%. By providing a standardized, extensible, and easy-to-use infrastructure, MemFactory significantly lowers the barrier to entry, paving the way for future innovations in memory-driven AI agents.

---

## 论文详细总结（自动生成）

### 论文总结：MemFactory - 智能体记忆的统一推理与训练框架

#### 1. 核心问题与整体含义（研究动机和背景）
随着大语言模型（LLM）向自主智能体（AI Agents）演进，长期记忆管理成为核心挑战。传统的启发式记忆规则（如简单的 RAG 或固定更新规则）在处理长程对话时容易产生冗余、信息冲突及上下文溢出。近期研究（如 Memory-R1, MemAgent）尝试利用强化学习（RL）优化记忆的提取、更新和检索策略，但这些研究往往高度碎片化，代码实现与特定任务深度耦合，缺乏统一的基础设施。
**MemFactory 的核心目标**是填补这一空白，借鉴 LLaMA-Factory 的模块化理念，提供首个专为记忆增强型智能体设计的统一、模块化训练与推理框架，旨在降低 Memory-RL 研究的工程门槛。

#### 2. 方法论：核心思想与关键技术
MemFactory 采用“乐高式”的模块化架构，将复杂的记忆生命周期拆解为四个核心层：
*   **模块层 (Module Layer)：** 定义了记忆操作的原子组件，包括：
    *   **Extractor（提取器）：** 从原始上下文中解析事实和经验。
    *   **Updater（更新器）：** 执行 ADD、DEL、UPDATE、NONE 等操作维护记忆库。
    *   **Retriever（检索器）：** 基于语义或重排序（Rerank）获取相关记忆。
    *   **Agent Module：** 支持端到端的循环记忆策略（如 MemAgent 范式）。
*   **智能体层 (Agent Layer)：** 作为策略执行器，将上述原子模块组合成完整的记忆策略流，支持训练模式（Rollout）和纯推理模式。
*   **环境层 (Environment Layer)：** 充当数据加载器和奖励管理器。提供 `MemoryBankEnv`（长期记忆库）和 `LongcontextEnv`（短期长上下文）两种环境，并集成格式奖励和 LLM-as-a-Judge 等多维奖励信号。
*   **训练层 (Trainer Layer)：** 核心优化引擎，原生集成 **GRPO（Group Relative Policy Optimization）** 算法。GRPO 通过组内奖励归一化估计基准，无需额外的 Critic 网络，显著降低了长上下文训练时的显存开销。

#### 3. 实验设计
*   **数据集与场景：** 使用了 MemAgent 发布的公开数据集，涵盖长文本处理和多轮对话任务。
*   **评估指标 (Benchmark)：** 
    *   `eval_50` 和 `eval_100`：主任务测试集（分别包含 50 和 100 个文档上下文）。
    *   `eval_fwe_16384`：分布外（OOD）测试集，用于验证泛化能力。
*   **对比方法：** 以 Qwen3-1.7B 和 Qwen3-4B-Instruct 作为基座模型（Base Checkpoint），对比经过 MemFactory RL 微调后的性能提升。框架本身预置了对 Memory-R1、MemAgent 和 RMM 三种 SOTA 范式的支持。

#### 4. 资源与算力
*   **硬件设备：** 论文明确指出，整个训练和评估流水线可以在 **单台 NVIDIA A800 80GB GPU** 上执行。
*   **训练细节：** 每个模型训练 250 个有效步（valid steps）。为了提高效率，训练时将原始上下文长度压缩至约三分之一（每样本 50-80 个文档）。
*   **推理设置：** 实验结果基于每道题 4 次独立试验的平均分（avg@4），以减少生成方差。

#### 5. 实验数量与充分性
*   **实验规模：** 针对两个不同规模的基座模型（1.7B 和 4B）在三个测试集上进行了对比实验。
*   **充分性评价：** 实验设计较为客观，涵盖了主任务和 OOD 泛化任务。通过 4 次独立试验取平均值确保了结果的统计可靠性。虽然实验主要聚焦于 MemAgent 这一种架构的复现与优化，但其对不同规模模型的普适性得到了验证。

#### 6. 主要结论与发现
*   **性能提升：** MemFactory 显著提升了基座模型的记忆管理能力。Qwen3-1.7B 在主任务上获得显著增益，平均性能提升 14.8%；Qwen3-4B-Instruct 表现更稳健，平均提升 7.3%，且在 OOD 任务上展现出更好的泛化性。
*   **框架有效性：** 验证了通过 GRPO 优化循环记忆策略（Recurrent Memory Policy）的可行性，证明了统一框架在简化复杂 Memory-RL 流程上的价值。

#### 7. 优点与亮点
*   **高度模块化：** “乐高式”设计允许研究者自由组合 Extractor、Updater 和 Retriever，极大地便利了新算法的消融实验和组件替换。
*   **显存友好：** 原生支持 GRPO 算法，解决了记忆增强型智能体在长上下文训练时显存极度紧张的痛点。
*   **开箱即用：** 预置了多种 SOTA 记忆算法的实现，为社区提供了标准化的 Baseline。

#### 8. 不足与局限
*   **范式覆盖有限：** 虽然支持三种代表性范式，但对于更复杂的分布式记忆或多智能体协同记忆的覆盖尚待加强。
*   **训练效率：** 尽管使用了 GRPO，但在处理极长上下文时，训练吞吐量仍有优化空间。
*   **实验深度：** 论文主要验证了框架的“可实现性”和“基础增益”，缺乏针对不同记忆模块组合（如不同 Retriever 对最终 RL 效果的影响）的深度消融研究。

（完）
