---
title: "FluxEDA: A Unified Execution Infrastructure for Stateful Agentic EDA"
title_zh: FluxEDA：一种用于有状态智能体驱动 EDA 的统一执行基础设施
authors: "Zhengrui Chen, Zixuan Song, Yu Li, Qi Sun, Cheng Zhuo"
date: 2026-03-26
pdf: "https://arxiv.org/pdf/2603.25243v1"
tags: ["query:agent"]
score: 9.0
evidence: 智能体与 EDA 工具交互的有状态基础设施
tldr: 针对现有EDA自动化中LLM代理难以保持工具状态和支持迭代优化的局限，本文提出FluxEDA。这是一个统一的有状态执行基础设施，通过托管网关接口和持久化后端实例，使代理能与异构EDA工具进行状态保持的交互。在时序ECO和单元库优化案例中证明了其支持状态重用、回滚和协同迭代的能力，为智能EDA自动化奠定了实用基础。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.25243v1/fig-001.webp\", \"caption\": \"Figure 1: Comparison between traditional EDA workflows and the proposed unified FluxEDA, highlighting the shift from fragmented scripting and tool-specific orchestration to a cohesive, standardized framework for EDA tool integration.\", \"page\": 1, \"index\": 1, \"width\": 514, \"height\": 239}, {\"url\": \"assets/figures/arxiv/2603.25243v1/fig-002.webp\", \"caption\": \"Figure 2: Overall software architecture of FluxEDA. The system organizes heterogeneous EDA environments into a five-layer stack that provides unified access, controlled capability exposure, and persistent tool execution for agentic and programmable workflows.\", \"page\": 2, \"index\": 2, \"width\": 740, \"height\": 400}, {\"url\": \"assets/figures/arxiv/2603.25243v1/fig-003.webp\", \"caption\": \"Figure 3: Execution signal flow in FluxEDA. Agent requests are issued through task logic, forwarded by the RPC client and session manager to the Tcl gateway, resolved through registered api_*methods, and executed on managed EDA runtimes. Capability discovery, method metadata, typed results, artifacts, and execution status traverse the same structured path.\", \"page\": 3, \"index\": 3, \"width\": 948, \"height\": 377}, {\"url\": \"assets/figures/arxiv/2603.25243v1/fig-004.webp\", \"caption\": \"Figure 5: Search trajectory for Pareto-driven standard-cell sub-library selection. Blue arrows denote the area-driven exploration stage (A-Runs) from the minimal library toward low-area operating points. Red arrows denote the subsequent timing-recovery stage (T-Runs), where the agent first performs local probing around T-Run04 and then transitions to structure-guided restoration, eventually reaching T-Run11. Gray dashed arrows indicate alternative local probes.\", \"page\": 4, \"index\": 4, \"width\": 514, \"height\": 313}, {\"url\": \"assets/figures/arxiv/2603.25243v1/fig-005.webp\", \"caption\": \"Table 2: Key milestones in Pareto-driven standard-cell sub-library selection. The last two columns report the number of exact cell references and cell families used in each synthesized implementation.\", \"page\": 5, \"index\": 5, \"width\": 1022, \"height\": 181}]"
motivation: 现有的EDA自动化集成多依赖脚本或请求级交互，难以在生产环境中保持工具运行状态并支持复杂的迭代优化。
method: 提出一种基于托管网关的统一执行架构，通过结构化请求处理和持久化后端实例，实现与异构EDA工具的有状态交互。
result: 在商业后端工具的ECO和单元库优化实验中，FluxEDA成功支持了多步分析、状态重用、回滚及协调迭代执行。
conclusion: 有状态且受控的基础设施层是实现大模型代理辅助EDA自动化的关键且实用的基础。
---

## 摘要
大语言模型和自主智能体在 EDA 自动化领域的应用正得到日益广泛的探索，但许多现有的集成方式仍依赖于脚本级或请求级的交互，这使得在真实的生产导向环境中难以保留工具状态并支持迭代优化。在这项工作中，我们提出了 FluxEDA，这是一个用于智能体驱动 EDA 的统一且有状态的基础设施底层。FluxEDA 引入了一个基于托管网关的执行接口，具有结构化的请求和响应处理能力；同时，它还维护着持久化的后端实例。这些特性共同允许上层智能体和可编程客户端通过保留的运行时状态与异构 EDA 工具进行交互，而非通过孤立的 shell 调用。我们通过两个具有代表性的商业后端案例研究对该框架进行了评估：自动化布线后时序 ECO 和标准单元子库优化。结果表明，FluxEDA 能够在真实的工具上下文中支持多步分析与优化，包括状态复用、回滚和协同迭代执行。这些发现表明，一个有状态且受控的基础设施层是实现智能体辅助 EDA 自动化的实用基础。

## Abstract
Large language models and autonomous agents are increasingly explored for EDA automation, but many existing integrations still rely on script-level or request-level interactions, which makes it difficult to preserve tool state and support iterative optimization in real production-oriented environments. In this work, we present FluxEDA, a unified and stateful infrastructure substrate for agentic EDA. FluxEDA introduces a managed gateway-based execution interface with structured request and response handling. It also maintains persistent backend instances. Together, these features allow upper-layer agents and programmable clients to interact with heterogeneous EDA tools through preserved runtime state, rather than through isolated shell invocations. We evaluate the framework using two representative commercial backend case studies: automated post-route timing ECO and standard-cell sub-library optimization. The results show that FluxEDA can support multi-step analysis and optimization over real tool contexts, including state reuse, rollback, and coordinated iterative execution. These findings suggest that a stateful and governed infrastructure layer is a practical foundation for agent-assisted EDA automation.

---

## 论文详细总结（自动生成）

以下是对论文《FluxEDA: A Unified Execution Infrastructure for Stateful Agentic EDA》的结构化深入总结：

### 1. 核心问题与研究动机
*   **研究背景**：随着芯片设计复杂度的指数级增长，EDA 流程集成变得极其繁重且易错。虽然大语言模型（LLM）和自主智能体（Agents）为自动化提供了新机会，但现有的集成方式存在严重缺陷。
*   **核心问题**：
    1.  **状态断裂**：现有框架多采用脚本级或请求级的批处理交互，每次操作都是孤立的 shell 调用，无法保留工具的内存状态（如已加载的设计、时序图等），难以支持复杂的迭代优化。
    2.  **集成瓶颈**：异构 EDA 工具（Tcl, Python, Bash 等）接口不一，导致 N×M 的集成难题。
    3.  **可靠性风险**：LLM 生成的脚本缺乏运行时治理，容易导致静默的状态损坏或工具崩溃。
*   **整体含义**：FluxEDA 旨在构建一个**有状态（Stateful）**且**受控（Governed）**的统一执行基础设施，作为智能体与底层异构 EDA 工具之间的稳定桥梁。

### 2. 方法论：核心思想与关键技术
FluxEDA 提出了一个五层软件架构（访问层、通信层、网关层、工具适配层、运行时管理层），其核心技术包括：
*   **托管网关与 Socket-RPC 接口**：通过结构化的 RPC 消息封装工具原生功能，定义了明确的执行边界（仅限注册的 `api_*` 方法），避免 LLM 直接操作 shell 带来的风险。
*   **持久化后端实例（Persistent Instances）**：这是 FluxEDA 的核心创新。它通过 `instance_id` 绑定并维护长效的工具进程，允许跨多次请求保留内存中的设计上下文，支持“分析-执行-精炼”的闭环。
*   **渐进式能力暴露（Progressive Capability Exposure）**：为了节省 LLM 的上下文窗口，网关仅先暴露简单的发现接口（如 `api_list_method`），仅在需要时才查询详细的调用参数。
*   **领域特定技能层（Skills Layer）**：将复杂的流程知识（如任务分解、执行顺序、输入验证）封装为“技能”，指导智能体如何组合原子 API 来完成具体任务。

### 3. 实验设计与 Benchmark
论文通过两个代表性的商业 EDA 后端案例验证了框架的有效性：
*   **案例一：自动化布线后时序 ECO 修复**：
    *   **场景**：在已布线的设计上修复建立时间（Setup）和保持时间（Hold）违例。
    *   **流程**：智能体加载设计 -> 建立基准 -> 迭代调整单元尺寸 -> 状态回滚（当发现优化效果不佳时）-> 修复保持时间。
*   **案例二：Pareto 驱动的标准单元子库优化**：
    *   **场景**：在工艺开发早期，从全库中筛选最小的单元子集，平衡面积与时序。
    *   **流程**：分为面积驱动探索（A-Runs）和时序恢复（T-Runs）两个阶段，通过与 STA（静态时序分析）工具的持续交互进行结构化精炼。
*   **对比基准**：主要对比了全库基准（Baseline）以及不同迭代阶段（A-Run9, T-Run11 等）的 QoR（设计质量）指标。

### 4. 资源与算力
*   **模型使用**：主要使用了 **GPT-5.4**（论文中提到的版本，注：可能为该研究领域预期的未来版本或特定代号）作为核心 LLM。
*   **框架实现**：智能体编排基于 OpenAI Codex 框架实现。
*   **硬件算力**：文中**未明确说明**具体的 GPU 型号、数量或训练/推理时长。实验主要侧重于框架对商业 EDA 工具（运行在 CPU 服务器上）的调度能力。

### 5. 实验数量与充分性
*   **实验规模**：
    *   ECO 案例展示了 3 次关键迭代及状态回滚过程。
    *   单元库优化案例展示了从 A-Run1 到 T-Run13 共 20 多组详细的实验轨迹（见附录）。
*   **充分性评价**：实验涵盖了状态保留、分支探索、回滚和跨工具协同等关键能力，证明了框架在处理“长程任务”时的稳定性。虽然案例数量不多（2个），但每个案例的深度较高，且均基于真实的商业 EDA 环境，具有较强的说服力。

### 6. 主要结论与发现
*   **状态保留至关重要**：FluxEDA 证明了通过保留内存上下文，智能体可以像人类工程师一样进行增量优化，避免了重复加载设计的开销。
*   **回滚机制的价值**：在 ECO 案例中，智能体能够识别无效优化并回滚到之前的最佳状态，这在传统的单向脚本执行中难以实现。
*   **大幅缩减搜索空间**：在单元库优化中，FluxEDA 辅助智能体在减少 75.8% 单元引用的情况下，恢复了 88.4% 的时序损失，实现了极佳的 Pareto 平衡。

### 7. 优点与亮点
*   **架构解耦**：成功将底层工具的异构性与上层智能体的逻辑解耦，解决了 N×M 集成问题。
*   **生产环境友好**：引入了心跳监测、超时控制和异步长任务管理，更符合工业级生产环境对稳定性的要求。
*   **状态化交互**：突破了现有研究中常见的“黑盒脚本生成”模式，实现了真正的“白盒状态交互”。

### 8. 不足与局限
*   **实验覆盖面**：虽然验证了后端流程，但尚未展示在前段合成或验证阶段的大规模应用。
*   **模型依赖**：高度依赖高性能 LLM（如 GPT-5.4）的推理能力，对于开源小模型在相同框架下的表现缺乏评估。
*   **延迟开销**：虽然持久化实例减少了加载时间，但增加的网关层和 RPC 通信可能在极大规模 API 调用时引入额外的延迟。
*   **安全性边界**：虽然限制了 API 访问，但对于合法 API 组合可能产生的逻辑错误（如非预期的设计修改）仍需更复杂的语义检查。

（完）
