---
title: Coding Agents are Effective Long-Context Processors
title_zh: 代码智能体是有效的长上下文处理器
authors: "Weili Cao, Xunjian Yin, Bhuwan Dhingra, Shuyan Zhou"
date: 2026-03-20
pdf: "https://arxiv.org/pdf/2603.20432v1"
tags: ["query:agent"]
score: 9.0
evidence: 代码智能体使用原生工具处理长文本
tldr: "针对大语言模型在处理超长上下文时性能下降且不可解释的问题，本文提出将长文本处理从隐式的注意力机制转向显式的编码代理交互。通过让编码代理利用文件系统组织文本并使用代码、终端等原生工具进行操作，该方法在长文本推理、RAG和超大规模语料问答等任务中表现优异，平均超越现有最先进水平17.3%，为长上下文处理提供了新路径。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.20432v1/fig-001.webp\", \"caption\": \"Figure 4. Two emergent processing strategies. Left: BrowseComp-Plus—iterative query refinement with entity chaining across searches. Right: Oolong-Synthetic—programmatic aggregation via Python scripts with regex patterns. See Appendix C for detailed traces.\", \"page\": 6, \"index\": 1, \"width\": 1017, \"height\": 292}, {\"url\": \"assets/figures/arxiv/2603.20432v1/fig-002.webp\", \"caption\": \"Figure 5. Quantitative characterization of agent strategies per query. The y-axis represents the normalized proportion of each metric, where the values for a given model sum to 1 across all datasets.\", \"page\": 6, \"index\": 2, \"width\": 458, \"height\": 327}, {\"url\": \"assets/figures/arxiv/2603.20432v1/fig-003.webp\", \"caption\": \"Figure 2. Iterative refinement example on Oolong-Real. When asked to identify the last spell cast by Vax’ildan in each episode of a 385K-token transcript, the coding agent wrote a Python script, discovered domain-specific spell references through failure analysis, and iteratively refined its logic.\", \"page\": 2, \"index\": 3, \"width\": 735, \"height\": 298}, {\"url\": \"assets/figures/arxiv/2603.20432v1/fig-004.webp\", \"caption\": \"Figure 3. Text Processing as File System Navigation. We organize text corpus into a Navigable File System of documents and folders. The Coding Agent explores this hierarchy using native tools (e.g., ripgrep, terminal commands), writes Python scripts for Programmatic Aggregation, and saves intermediate results. The agent Iteratively Refines its queries based on discovered information, enabling multi-hop reasoning without fixed retrieval pipelines.\", \"page\": 3, \"index\": 4, \"width\": 735, \"height\": 325}, {\"url\": \"assets/figures/arxiv/2603.20432v1/fig-005.webp\", \"caption\": \"Table 5. Average cost per query across benchmarks.\", \"page\": 8, \"index\": 5, \"width\": 787, \"height\": 264}, {\"url\": \"assets/figures/arxiv/2603.20432v1/fig-006.webp\", \"caption\": \"Table 1. Main results across five benchmarks. Best results are in bold. * indicates results evaluated on the full set, used here only for reference. Reported metrics are Accuracy for BrowseComp-Plus and LongBench, Exact Match (EM) for NQ, and Score for Oolong.\", \"page\": 4, \"index\": 6, \"width\": 822, \"height\": 334}]"
motivation: 传统LLM依赖隐式注意力机制处理长上下文，随着长度增加性能显著下降且缺乏可解释性。
method: 将长文本处理任务委托给编码代理，利用其文件系统导航能力和原生工具进行显式的文本组织与操作。
result: "在多个长文本基准测试中，该方法平均超越现有最先进水平17.3%，并能有效处理高达三万亿token的大规模语料。"
conclusion: 编码代理通过工具熟练度和文件系统组织能力，成为了替代语义搜索或单纯扩展上下文窗口的有效长文本处理方案。
---

## 摘要
大语言模型（LLMs）在扩展以访问海量上下文方面取得了显著进展。然而，这种访问是通过潜在且不可解释的注意力机制实现的，且 LLMs 往往无法有效处理长上下文，随着上下文长度的增加，性能会出现显著下降。在这项工作中，我们研究了是否可以通过允许代码智能体在文件系统中组织文本并利用其原生工具进行操作，将长上下文处理从潜在注意力机制外化为显式的、可执行的交互。我们评估了现有的前沿代码智能体作为处理长上下文任务的通用接口，包括长上下文推理、检索增强生成以及包含高达三万亿 token 的大规模语料库的开放域问答。在多个基准测试中，这些智能体的表现平均优于已发表的最先进方法 17.3%。我们将这种效能归因于两个关键因素：原生工具的熟练程度，使智能体能够利用可执行代码和终端命令而非被动的语义查询；以及对文件系统的熟悉程度，使其能够以目录结构的形式导航海量文本语料库。这些发现表明，将长上下文处理委托给代码智能体为语义搜索或上下文窗口扩展提供了一种有效的替代方案，为 LLMs 的长上下文处理开辟了新方向。

## Abstract
Large Language Models (LLMs) have demonstrated remarkable progress in scaling to access massive contexts. However, the access is via the latent and uninterpretable attention mechanisms, and LLMs fail to effective process long context, exhibiting significant performance degradation as context length increases. In this work, we study whether long-context processing can be externalized from latent attention into explicit, executable interactions, by allowing coding agents to organize text in file systems and manipulate it using its native tools. We evaluate off-the-shelf frontier coding agents as the general interface for tasks that require processing long contexts, including long-context reasoning, retrieval-augmented generation, and open-domain question answering with large-scale corpus contains up to three trillion tokens. Across multiple benchmarks, these agents outperform published state-of-the-art by 17.3% on average. We attribute this efficacy to two key factors: native tool proficiency, which enables agents to leverage executable code and terminal commands rather than passive semantic queries, and file system familiarity, which allows them to navigate massive text corpora as directory structures. These findings suggest that delegating long-context processing to coding agents offers an effective alternative to semantic search or context window scaling, opening new directions for long-context processing in LLMs.

---

## 论文详细总结（自动生成）

### 论文总结：代码智能体是有效的长上下文处理器

#### 1. 核心问题与整体含义（研究动机和背景）
现代大语言模型（LLM）虽然支持数百万 token 的上下文窗口，但在处理长文本时存在两个核心痛点：
*   **性能衰减（Context Rot）**：随着上下文长度增加，模型依赖的隐式注意力机制会导致性能显著下降。
*   **不可解释性**：模型如何从海量信息中提取特定事实的过程是“黑盒”，难以追踪和验证。
*   **RAG 的局限性**：传统的检索增强生成（RAG）依赖固定的检索管道，难以应对需要多步推理、跨文档聚合或复杂逻辑处理的任务。

本研究提出：**将长文本处理任务转化为“文件系统导航与操作”任务**。利用在大型代码库上训练的代码智能体（Coding Agents），将其处理复杂代码结构的能力迁移到通用长文本处理中，通过显式的工具交互（如终端命令、Python 脚本）替代隐式的注意力检索。

#### 2. 论文提出的方法论
核心思想是将大规模语料库视为一个**可导航的文件系统**，并赋予代码智能体自主操作权限。
*   **语料库格式化**：将长文档或大规模语料库组织成目录和 `.txt` 文件结构。对于超大规模语料（如 3T token），则采用结构化存储（如 JSONL）。
*   **智能体接口**：使用现成的（Off-the-shelf）前沿代码智能体（如 OpenAI Codex、Claude Code）。智能体不直接“阅读”全部文本，而是通过以下方式交互：
    *   **终端命令**：使用 `grep`、`ripgrep`、`sed`、`head` 等工具进行快速搜索和定位。
    *   **程序化聚合**：编写并运行 Python 脚本，利用正则表达式或逻辑判断对文本进行过滤、计数和排序。
    *   **迭代优化**：根据初步搜索结果，自主修正查询方向，实现多步推理（Multi-hop reasoning）。
*   **自主决策**：不预设检索流程，智能体根据任务自主选择是顺序扫描、关键词搜索还是编写自定义解析脚本。

#### 3. 实验设计
*   **数据集与场景**：
    *   **BrowseComp-Plus (750M tokens)**：复杂的 Web 浏览多步问答。
    *   **Oolong-Real/Synthetic (385K-536K tokens)**：长文档推理与信息聚合（如统计角色动作）。
    *   **LongBench-v2 (188K tokens)**：涵盖单/多文档问答、代码仓库理解等综合任务。
    *   **Natural Questions (NQ, 3T tokens)**：超大规模语料库下的开放域问答。
*   **对比方法（Baselines）**：
    *   **GPT-5 Full Context**：直接将长文本输入模型（或采用滑动窗口）。
    *   **标准 RAG**：基于 Gemini 嵌入或 BM25 的固定检索管道。
    *   **ReAct Agent**：受限于预定义 API 的搜索智能体。
    *   **Recursive Language Model (RLM)**：通过递归调用自身处理文本的方法。

#### 4. 资源与算力
*   论文**未明确说明**具体的 GPU 型号、数量或训练时长。
*   **原因**：该研究主要评估的是“现成”的前沿模型（如 GPT-5 驱动的 Codex 和 Claude 3.5 Sonnet 驱动的 Claude Code），侧重于推理阶段的智能体策略，而非模型训练。
*   **成本分析**：文中提供了详细的推理成本分析，指出代码智能体的平均每查询成本高于 RAG，但通常低于或等同于全上下文处理（Full Context）。

#### 5. 实验数量与充分性
*   **实验规模**：在 5 个具有代表性的长文本基准上进行了测试，语料跨度从 188K 到 3T token。
*   **消融实验**：
    *   测试了文件系统结构（文件夹 vs 单文件）对性能的影响。
    *   分析了加入外部检索工具（BM25/Dense Retriever）是否能提供额外帮助。
    *   对比了不同模型后端（GPT-5 vs Claude 3.5）的一致性。
*   **充分性评价**：实验设计较为充分且客观。通过定量分析智能体的命令使用频率（Search Intensity, Read Volume, Code Volume），揭示了智能体在不同任务下自发形成的策略差异，增强了结论的说服力。

#### 6. 论文的主要结论与发现
*   **性能卓越**：代码智能体在 4/5 个基准测试中刷新了 SOTA，平均性能提升 **17.3%**。
*   **核心驱动力**：
    1.  **原生工具熟练度**：能够编写代码进行精确的数据处理，而非仅依赖模糊的语义匹配。
    2.  **文件系统熟悉度**：代码训练赋予了智能体在层级结构中导航的强大先验知识。
*   **策略涌现**：智能体会根据任务自发演化出策略：在多步问答中进行“迭代查询优化”，在分析任务中进行“程序化聚合”，在阅读理解中采用“混合搜索-阅读”模式。
*   **反直觉发现**：为代码智能体额外配备标准检索工具（如向量检索）并不总能提升性能，有时反而会抑制智能体原生的探索能力。

#### 7. 优点
*   **通用性与灵活性**：无需针对特定任务进行微调，直接利用代码智能体的通用能力解决长文本问题。
*   **极高的可解释性**：智能体的每一步操作（搜索、读行、写脚本）都有完整的 Trace 记录，用户可以清晰看到答案的来源。
*   **突破窗口限制**：通过将文本外化到文件系统，理论上可以处理无限长度的上下文（如实验中的 3T token）。

#### 8. 不足与局限
*   **推理延迟与成本**：相比于单次检索的 RAG，智能体需要多次迭代，导致响应时间较长且 API 调用成本较高。
*   **工具抑制效应**：如何将传统检索工具与智能体的原生探索能力完美融合仍是挑战。
*   **环境依赖**：需要一个支持代码执行和终端操作的安全沙箱环境，这在某些应用场景下可能受限。
*   **模型偏差**：目前的成功高度依赖于最顶尖的代码模型（如 GPT-5 级别），在较弱的模型上可能无法复现同等水平的自主策略。

（完）
