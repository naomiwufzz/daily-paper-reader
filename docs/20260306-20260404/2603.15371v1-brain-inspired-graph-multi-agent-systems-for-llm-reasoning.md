---
title: Brain-Inspired Graph Multi-Agent Systems for LLM Reasoning
title_zh: 受大脑启发的图多智能体系统用于大语言模型推理
authors: "Guangfu Hao, Yuming Dai, Xianzhe Qin, Shan Yu"
date: 2026-03-16
pdf: "https://arxiv.org/pdf/2603.15371v1"
tags: ["query:agent"]
score: 9.0
evidence: 受大脑启发的图多智能体系统用于复杂推理
tldr: 针对大语言模型在复杂多步推理中的局限性，本文受人类认知“全局工作空间理论”启发，提出了一种脑启发式图多智能体系统（BIGMAS）。该系统将专业化智能体组织为动态有向图，通过中心化共享空间进行协作。实验证明，BIGMAS在Game24等复杂任务上显著提升了标准LLM和推理模型（LRM）的性能，优于ReAct和思维树等基准，展示了多智能体架构与模型推理增强的互补性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.15371v1/fig-001.webp\", \"caption\": \"\", \"page\": 7, \"index\": 1, \"width\": 666, \"height\": 740}]"
motivation: 现有LLM和推理模型在处理极高复杂度任务时仍存在准确率崩溃，单纯依靠模型规模扩展难以解决复杂推理瓶颈。
method: 提出BIGMAS框架，利用GraphDesigner构建任务自适应的智能体拓扑，并通过全局Orchestrator在共享工作空间内进行动态路由与协作。
result: 在Game24、Six Fives和伦敦塔等任务中，BIGMAS在六种前沿模型上均取得了一致的性能提升，显著超越了ReAct和Tree of Thoughts等现有方法。
conclusion: 多智能体架构设计能提供与模型自身推理增强相正交的性能增益，是提升复杂推理能力的有效途径。
---

## 摘要
大语言模型（LLMs）在广泛的语言任务中展现出卓越的能力，但复杂的多步推理仍然是一个根本性的挑战。虽然配备了扩展思维链机制的大推理模型（LRMs）表现出优于标准 LLMs 的性能，但这两类模型在足够复杂的任务上仍会出现准确率崩溃，这表明仅靠扩展模型层面的推理是不够的。受人类认知的全局工作空间理论启发，我们提出了类脑图多智能体系统（BIGMAS），其中专门的 LLM 智能体被组织为动态构建的有向图中的节点，并完全通过一个中心化的共享工作空间进行协作。一个问题自适应的图设计器（GraphDesigner）构建特定任务的智能体拓扑结构，而全局编排器（Orchestrator）利用完整的共享状态进行路由决策，克服了反应式方法中的局部视图瓶颈。在 Game24、Six Fives 和 Tower of London 任务上针对六种前沿 LLMs 的实验表明，BIGMAS 持续提升了标准 LLMs 和 LRMs 的推理性能，优于包括 ReAct 和思维树（Tree of Thoughts）在内的现有多智能体基准，证明了多智能体架构设计提供了与模型层面推理增强相正交的互补增益。

## Abstract
Large Language Models (LLMs) have demonstrated remarkable capabilities across a wide range of language tasks, yet complex multi-step reasoning remains a fundamental challenge. While Large Reasoning Models (LRMs) equipped with extended chain-of-thought mechanisms demonstrate improved performance over standard LLMs, both model types still suffer from accuracy collapse on sufficiently complex tasks, suggesting that scaling model-level reasoning alone is insufficient. Inspired by the global workspace theory of human cognition, we propose Brain-Inspired Graph Multi-Agent Systems (BIGMAS), in which specialized LLM agents are organized as nodes in a dynamically constructed directed graph and coordinate exclusively through a centralized shared workspace. A problem-adaptive GraphDesigner constructs task-specific agent topologies, while a global Orchestrator leverages the complete shared state for routing decisions, overcoming the local-view bottleneck of reactive approaches. Experiments on Game24, Six Fives, and Tower of London across six frontier LLMs demonstrate that BIGMAS consistently improves reasoning performance for both standard LLMs and LRMs, outperforming existing multi-agent baselines including ReAct and Tree of Thoughts, showing that multi-agent architectural design provides complementary gains orthogonal to model-level reasoning enhancements.

---

## 论文详细总结（自动生成）

以下是对论文《Brain-Inspired Graph Multi-Agent Systems for LLM Reasoning》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：尽管大推理模型（LRM，如 OpenAI o1/o3、DeepSeek-R1）通过扩展思维链（CoT）提升了性能，但在面对极高复杂度的多步推理任务时，仍会出现“准确率崩溃”现象。
*   **研究动机**：作者认为单纯扩展单个模型的推理规模已遇到瓶颈。受人类认知科学中的**全局工作空间理论（Global Workspace Theory, GWT）**启发，论文提出将认知负荷分布到多个专业化智能体中，并通过动态构建的图结构和中心化共享空间进行协作，以解决逻辑执行的一致性问题。

### 2. 方法论：核心思想与关键技术
BIGMAS 框架将推理过程分为三个阶段：
*   **问题自适应的图设计（Graph Design）**：由 `GraphDesigner` 智能体分析具体问题，自主构建一个任务特定的有向图 $\mathcal{G}$（包含节点角色、拓扑结构）和工作空间架构 $\kappa$。这意味着不同难度的题目会有不同的智能体协作模式。
*   **中心化共享工作空间（Shared Workspace）**：所有智能体节点不进行点对点通信，而是统一读写一个中心化的工作空间 $\mathcal{B}$。该空间分为只读上下文、读写工作区、系统元数据和最终答案区，确保了信息的全局可见性。
*   **全局编排与鲁棒执行（Graph Execution）**：
    *   **Orchestrator（编排器）**：基于全局状态和执行历史进行动态路由决策，克服了传统方法中智能体仅有局部视角的缺陷。
    *   **写入协议与自纠错**：节点输出需符合结构化指令（append/update/replace），并在写入前进行验证。若失败则触发自纠错循环（Self-correction loop），增强了系统的容错性。

### 3. 实验设计
*   **数据集/场景**：选择了三个具有挑战性且抗污染的组合推理任务：
    1.  **Game24**：算术推理，使用 4 个数字组合成 24。
    2.  **Six Fives**：约束表达式生成，使用 6 个数字 5 达到目标值。
    3.  **Tower of London（伦敦塔）**：顺序规划任务，要求以最少步数达到目标状态。
*   **对比基准（Benchmarks）**：
    *   **Base LLM**：直接单次调用推理。
    *   **ReAct**：反应式推理与行动循环。
    *   **Tree of Thoughts (ToT)**：思维树搜索。
*   **测试模型**：涵盖了 6 种前沿模型，包括标准 LLM 和 LRM（DeepSeek-V3.2、Claude 4.5 Sonnet、Gemini 2.5 Pro、GPT-5 及其推理增强版）。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的硬件资源（如 GPU 型号、数量）或训练时长。
*   **推理成本分析**：文中详细分析了 **Token 消耗**。结果显示，节点执行（Node Execution）占据了约 46%-55% 的 Token 支出，表明计算资源主要用于实质性解题而非系统开销。

### 5. 实验数量与充分性
*   **实验规模**：每个任务随机抽取 100 个实例进行测试。
*   **充分性评估**：
    *   **多模型验证**：在 6 个顶级模型上均观察到一致的性能提升，证明了方法的普适性。
    *   **多维度分析**：除了准确率，还进行了图拓扑分析、节点角色分布分析、Token 消耗分析以及路由行为分析。
    *   **客观性**：对比了 ToT 和 ReAct 等经典框架，实验设计较为公平，涵盖了从标准模型到推理增强模型的完整光谱。

### 6. 主要结论与发现
*   **正交增益**：BIGMAS 提供的性能提升与模型自身的推理增强（如 LRM 的 CoT）是**互补且正交**的。即使是已经具备强大推理能力的模型（如 GPT-5 或开启 Thinking 的 Claude 4.5），在 BIGMAS 架构下仍有显著进步。
*   **结构化优势**：在复杂任务（如伦敦塔）中，BIGMAS 的提升最为显著（如 Claude 4.5 Thinking 版提升了 36%），说明多智能体协作能有效弥补单模型在长程规划中的逻辑断裂。
*   **动态演化**：`GraphDesigner` 能根据任务难度自动调整图的复杂度（节点数和边数），模拟了大脑在处理复杂任务时募集更多功能区域的机制。

### 7. 优点（亮点）
*   **理论支撑深厚**：将认知科学的 GWT 理论成功转化为可落地的多智能体架构。
*   **动态拓扑**：打破了固定工作流的限制，实现了“一题一图”的灵活性。
*   **全局视图**：通过共享工作空间和全局编排器，解决了多智能体系统中常见的“信息碎片化”和“局部最优”问题。
*   **高鲁棒性**：引入验证与自纠错机制，确保了复杂推理链条不会因中间节点的微小错误而崩溃。

### 8. 不足与局限
*   **任务覆盖面**：目前仅在三个组合推理任务上验证，尚未扩展到开放域问答、代码生成或数学竞赛等更广泛的领域。
*   **推理延迟与成本**：由于涉及多次 LLM 调用和复杂的图执行逻辑，其 Token 消耗和响应延迟显著高于单模型推理。
*   **缺乏记忆机制**：`GraphDesigner` 在设计图时是独立处理每个实例的，没有利用历史设计经验进行元学习（Meta-learning）。
*   **终止判定**：实验发现失败的运行往往伴随着更多的路由次数，说明系统在检测“不可解”或“陷入死循环”时的早期停止机制仍有优化空间。

（完）
