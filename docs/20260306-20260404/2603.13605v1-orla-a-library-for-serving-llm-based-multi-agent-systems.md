---
title: "Orla: A Library for Serving LLM-Based Multi-Agent Systems"
title_zh: Orla：一个用于提供基于大语言模型（LLM）的多智能体系统服务的库
authors: "Rana Shahout, Hayder Tirmazi, Minlan Yu, Michael Mitzenmacher"
date: 2026-03-13
pdf: "https://arxiv.org/pdf/2603.13605v1"
tags: ["query:agent"]
score: 9.0
evidence: 用于提供LLM多智能体系统服务的库
tldr: Orla是一个专为LLM多智能体系统设计的推理服务库，旨在解决开发者在手动编排复杂Agent工作流时的低效问题。它通过在现有推理引擎之上构建抽象层，实现了请求执行与工作流策略的分离。Orla引入了阶段映射器、工作流编排器和内存管理器三大核心机制，支持跨模型和后端的资源调度与KV缓存优化。实验证明，Orla在提升推理效率、降低成本及缩短首字延迟（TTFT）方面显著优于传统的单模型服务方案。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.13605v1/fig-001.webp\", \"caption\": \"Figure 8: In the customer support workflow, classify runs on a lightweight backend while later stages run on a heavier backend. The memory manager flushes cache when switching backends, but preserves it when consecutive stages on the same backend share most of their context, avoiding redundant prefilling. All remaining cache is reclaimed when the workflow completes.\", \"page\": 6, \"index\": 1, \"width\": 506, \"height\": 561}, {\"url\": \"assets/figures/arxiv/2603.13605v1/fig-002.webp\", \"caption\": \"Figure 6: An illustration of Orla’s two-level scheduling policy in our running example. Stage scheduling policy selects which stage sub-queue to dequeue from, while request scheduling policy orders requests within a sub-queue. Priority hints can be set dynamically based on ticket classification.\", \"page\": 6, \"index\": 2, \"width\": 506, \"height\": 466}, {\"url\": \"assets/figures/arxiv/2603.13605v1/fig-003.webp\", \"caption\": \"Figure 7: The policy_check stage constructs its prompt from the upstream classify stage’s result.\", \"page\": 6, \"index\": 3, \"width\": 506, \"height\": 242}, {\"url\": \"assets/figures/arxiv/2603.13605v1/fig-004.webp\", \"caption\": \"Figure 1: Orla design.\", \"page\": 2, \"index\": 4, \"width\": 423, \"height\": 450}, {\"url\": \"assets/figures/arxiv/2603.13605v1/fig-005.webp\", \"caption\": \"Figure 5: The Orla API allows users to define complex agentic workflows using a DAG-based approach, as shown in this customer support example.\", \"page\": 5, \"index\": 5, \"width\": 506, \"height\": 691}]"
motivation: 现代智能体应用涉及复杂的多步推理和异构基础设施，开发者需要一种能自动协调工作流策略与底层执行的通用服务框架。
method: Orla通过阶段映射器分配模型、编排器管理资源调度、内存管理器优化跨边界KV缓存，实现了对Agent工作流的精细化控制。
result: 在实际工作流评估中，Orla相比vLLM基准显著降低了延迟和成本，并通过高效的缓存管理缩短了首字生成时间。
conclusion: Orla通过分离执行与策略，为构建高效、可扩展且具备智能体级控制能力的LLM服务系统提供了关键的基础设施支持。
---

## 摘要
我们介绍了 Orla，这是一个用于构建和运行基于大语言模型（LLM）的智能体系统的库。现代智能体应用由结合了多个 LLM 推理步骤、工具调用和异构基础设施的工作流组成。目前，开发人员通常通过手动将编排代码与 LLM 推理引擎及工具执行逻辑相结合来构建这些系统。Orla 提供了一种通用的抽象，将请求执行与工作流级策略分离。它作为现有 LLM 推理引擎之上的服务层：开发人员定义由阶段（stages）组成的工作流，而 Orla 负责管理这些阶段如何在不同模型和后端之间进行映射、执行和协调。它通过三种机制提供智能体级控制：阶段映射器（stage mapper），负责将每个阶段分配给合适的模型和后端；工作流编排器（workflow orchestrator），负责调度阶段并管理其资源和上下文；以及内存管理器（memory manager），负责跨工作流边界管理推理状态（如 KV 缓存）。我们通过一个展示了其多项功能的客户支持工作流来演示 Orla。我们在两个数据集上对 Orla 进行了评估，结果表明，与单模型 vLLM 基准相比，阶段映射改善了延迟和成本，而工作流级缓存管理则降低了首字延迟（time-to-first-token）。

## Abstract
We introduce Orla, a library for constructing and running LLM-based agentic systems. Modern agentic applications consist of workflows that combine multiple LLM inference steps, tool calls, and heterogeneous infrastructure. Today, developers typically build these systems by manually composing orchestration code with LLM serving engines and tool execution logic. Orla provides a general abstraction that separates request execution from workflow-level policy. It acts as a serving layer above existing LLM inference engines: developers define workflows composed of stages, while Orla manages how those stages are mapped, executed, and coordinated across models and backends. It provides agent-level control through three mechanisms: a stage mapper, which assigns each stage to an appropriate model and backend; a workflow orchestrator, which schedules stages and manages their resources and context; and a memory manager, which manages inference state such as the KV cache across workflow boundaries. We demonstrate Orla with a customer support workflow that exercises many of its capabilities. We evaluate Orla on two datasets, showing that stage mapping improves latency and cost compared to a single-model vLLM baseline, while workflow-level cache management reduces time-to-first-token.

---

## 论文详细总结（自动生成）

### Orla: 基于 LLM 的多智能体系统服务库论文总结

#### 1. 核心问题与整体含义（研究动机和背景）
随着大语言模型（LLM）向智能体（Agent）演进，应用不再是单次推理，而是包含推理、规划、工具调用和结果整合的复杂**工作流（Workflows）**。
*   **痛点**：现有的推理引擎（如 vLLM、SGLang）主要针对单次请求的吞吐量和延迟进行优化，而忽略了智能体工作流级别的需求。
*   **挑战**：不同阶段对模型能力要求不同（异构性）、阶段间存在依赖关系（协调性）、以及跨阶段的上下文复用（内存管理）。
*   **核心目标**：Orla 旨在提供一个通用的服务层抽象，将底层的“请求执行”与上层的“工作流策略”分离，简化多智能体系统的构建与运行。

#### 2. 方法论：核心思想与关键技术
Orla 作为一个位于现有推理后端（如 SGLang, Ollama）之上的轻量级库，通过三个核心组件实现智能体级控制：
*   **阶段映射器 (Stage Mapper)**：
    *   将工作流中的每个阶段（Stage）分配给最合适的模型和后端。
    *   支持**显式映射**（开发者指定）和**动态路由**（如使用轻量级模型判断任务复杂度，将简单任务路由至小模型，复杂任务路由至大模型）。
*   **工作流编排器 (Workflow Orchestrator)**：
    *   **调度**：基于有向无环图（DAG）管理阶段依赖，支持 FCFS（先来先服务）或基于优先级的调度策略。
    *   **资源与上下文管理**：负责在阶段间传递输出结果（如 JSON、工具调用结果），并构造后续阶段的 Prompt。
*   **内存管理器 (Memory Manager)**：
    *   在工作流级别（而非请求级别）管理 KV 缓存。
    *   **策略**：包括“小增量保留”（跨阶段复用缓存以减少 Prefill 延迟）、“工作流边界刷新”（完成后释放显存）以及“压力下刷新”。

#### 3. 实验设计
论文在两个主要场景下评估了 Orla 的性能：
*   **场景一：代码生成（SWE-bench Lite）**
    *   **数据集**：包含 2,294 个请求。
    *   **对比方法**：基准线为使用单模型（Qwen3-8B）的 vLLM 服务。Orla 使用 `OneBitStageMapper` 将任务动态路由至 Qwen3-4B（简单）或 Qwen3-8B（复杂）。
*   **场景二：数学推理（DAG-MATH-Formatted-CoT）**
    *   **数据集**：包含 2,894 个请求。
    *   **对比方法**：对比了“逐请求刷新缓存”与 Orla 的“逐工作流刷新缓存”策略，评估首字延迟（TTFT）。

#### 4. 资源与算力
*   **硬件环境**：一台配备 2 颗 AMD EPYC 7313 CPU（共 64 核）、528 GB 系统内存的服务器。
*   **GPU**：1 块 **NVIDIA RTX PRO 6000 Blackwell Max-Q** 工作站显卡（96 GB 显存）。
*   **软件栈**：使用 vLLM 和 SGLang 作为后端引擎，通过 Docker 容器化部署。

#### 5. 实验数量与充分性
*   **实验规模**：论文针对两个标准基准数据集进行了数千次请求的测试（2,294 + 2,894 次），样本量足以支撑统计意义上的结论。
*   **客观性**：实验涵盖了延迟（Wall-clock time）、成本（Cost）和首字延迟（TTFT）等多个维度。通过对比单模型基准和不同的缓存策略，验证了 Orla 在异构资源调度上的优势。实验设计较为公平，考虑了实际云部署中的成本差异。

#### 6. 主要结论与发现
*   **效率提升**：在 SWE-bench 任务中，通过阶段映射，Orla 将墙钟时间缩短了 **38%**，平均完成时间缩短了 **60%**。
*   **成本降低**：由于合理利用了更便宜的小模型，推理成本降低了约 **35%**。
*   **延迟优化**：工作流级别的 KV 缓存管理显著改善了 TTFT 的分布，通过复用前序阶段的缓存避免了重复的 Prefill 计算。

#### 7. 优点
*   **解耦设计**：将工作流逻辑与底层推理引擎解耦，支持插件式更换后端（如从 GPU 上的 SGLang 切换到 CPU 上的 Ollama）。
*   **灵活性**：提供了简单的 API 来定义复杂的 DAG 工作流，并允许开发者自定义调度和映射策略。
*   **实用性**：针对智能体特有的“长上下文复用”和“工具调用等待”问题提供了针对性的内存和资源管理方案。

#### 8. 不足与局限
*   **准确率探讨不足**：实验主要关注系统性能（延迟、成本），虽然提到使用了动态路由，但未详细分析这种路由策略是否会对最终任务的成功率（如代码修复的正确性）产生负面影响。
*   **结构限制**：目前主要支持 DAG（有向无环图）结构，对于包含复杂循环（Cycles）或动态演化拓扑的智能体系统，其表达能力可能受限。
*   **单机局限**：实验主要在单机单卡环境下完成，对于大规模分布式集群中的跨节点 KV 缓存传输和负载均衡讨论较少。

（完）
