---
title: "GoAgent: Group-of-Agents Communication Topology Generation for LLM-based Multi-Agent Systems"
title_zh: GoAgent：面向基于大语言模型的多智能体系统的代理组通信拓扑生成
authors: "Hongjiang Chen, Xin Zheng, Yixin Liu, Pengfei Jiao, Shiyuan Li, Huan Liu, Zhidong Zhao, Ziqi Xu, Ibrahim Khalil, Shirui Pan"
date: 2026-03-20
pdf: "https://arxiv.org/pdf/2603.19677v1"
tags: ["query:agent"]
score: 10.0
evidence: 多智能体系统的通信拓扑生成
tldr: 针对大语言模型多智能体系统（MAS）中通信拓扑构建效率低下的问题，本文提出GoAgent。不同于传统的以节点为中心的拓扑生成，GoAgent将协作小组视为构建的基本原子单位，通过LLM枚举候选小组并自动回归地连接它们，同时引入条件信息瓶颈（CIB）来压缩组间通信、减少冗余。实验证明，GoAgent在六个基准测试中达到SOTA性能，且显著降低了Token消耗。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.19677v1/fig-001.webp\", \"caption\": \"Figure 1: The comparison of two paradigms: (a) nodecentric and (b) group-centric graph generation.\", \"page\": 2, \"index\": 1, \"width\": 433, \"height\": 333}, {\"url\": \"assets/figures/arxiv/2603.19677v1/fig-002.webp\", \"caption\": \"Table 1: Overall performance comparison on six benchmarks (%). The best results are highlighted in bold.\", \"page\": 7, \"index\": 2, \"width\": 943, \"height\": 468}, {\"url\": \"assets/figures/arxiv/2603.19677v1/fig-003.webp\", \"caption\": \"Figure 3: Experiments on token consumption and robustness against prompt injection attacks.\", \"page\": 7, \"index\": 3, \"width\": 450, \"height\": 442}, {\"url\": \"assets/figures/arxiv/2603.19677v1/fig-004.webp\", \"caption\": \"Table 3: Dataset descriptions and statistics.\", \"page\": 13, \"index\": 4, \"width\": 950, \"height\": 719}, {\"url\": \"assets/figures/arxiv/2603.19677v1/fig-005.webp\", \"caption\": \"Figure 2: Overview of the proposed GoAgent.\", \"page\": 4, \"index\": 5, \"width\": 900, \"height\": 425}]"
motivation: 现有的多智能体通信拓扑多以节点为中心，缺乏对任务特定小组结构的显式建模，导致协调效率低且通信开销大。
method: 提出以小组为原子的拓扑生成方法，通过LLM枚举并连接候选小组，并利用条件信息瓶颈（CIB）优化组间通信。
result: "在六个基准测试中取得了93.84%的平均准确率，并减少了约17%的Token消耗。"
conclusion: 显式建模小组结构并结合信息瓶颈优化，能显著提升多智能体系统的协作效率与任务处理能力。
---

## 摘要
基于大语言模型（LLM）的多智能体系统（MAS）在解决复杂任务方面展现了卓越的能力，但其有效性在很大程度上取决于协调智能体交互的底层通信拓扑。在这些系统中，成功解决问题通常需要特定于任务的小组结构来对子任务进行分而治之。然而，大多数现有方法以“以节点为中心”的方式生成通信拓扑，使得小组结构从局部连接决策中隐式出现，而非显式建模，这往往导致协调效果不佳以及不必要的通信开销。为了解决这一局限性，我们提出了 GoAgent（Group-of-Agents），这是一种将协作组显式视为 MAS 构建原子单元的通信拓扑生成方法。具体而言，GoAgent 首先通过 LLM 枚举与任务相关的候选组，然后自回归地选择并连接这些组作为原子单元，以构建最终的通信图，从而共同捕捉组内凝聚力和组间协调。为了减轻拓扑扩展中固有的通信冗余和噪声传播，我们进一步引入了条件信息瓶颈（CIB）目标，用于压缩组间通信，在保留任务相关信号的同时过滤掉冗余的历史噪声。在六个基准测试上的广泛实验表明，GoAgent 达到了最先进的性能，平均准确率达 93.84%，同时减少了约 17% 的 token 消耗。

## Abstract
Large language model (LLM)-based multi-agent systems (MAS) have demonstrated exceptional capabilities in solving complex tasks, yet their effectiveness depends heavily on the underlying communication topology that coordinates agent interactions. Within these systems, successful problem-solving often necessitates task-specific group structures to divide and conquer subtasks. However, most existing approaches generate communication topologies in a node-centric manner, leaving group structures to emerge implicitly from local connectivity decisions rather than modeling them explicitly, often leading to suboptimal coordination and unnecessary communication overhead. To address this limitation, we propose GoAgent (Group-of-Agents), a communication topology generation method that explicitly treats collaborative groups as the atomic units of MAS construction. Specifically, GoAgent first enumerates task-relevant candidate groups through an LLM and then autoregressively selects and connects these groups as atomic units to construct the final communication graph, jointly capturing intra-group cohesion and inter-group coordination. To mitigate communication redundancy and noise propagation inherent in expanding topologies, we further introduce a conditional information bottleneck (CIB) objective that compresses inter-group communication, preserving task-relevant signals while filtering out redundant historical noise. Extensive experiments on six benchmarks demonstrate the state-of-the-art performance of GoAgent with 93.84% average accuracy while reducing token consumption by about 17%.

---

## 论文详细总结（自动生成）

### 论文总结：GoAgent - 面向大语言模型多智能体系统的代理组通信拓扑生成

#### 1. 核心问题与整体含义（研究动机和背景）
在基于大语言模型（LLM）的多智能体系统（MAS）中，智能体之间的**通信拓扑**（即谁与谁交流）对任务成败至关重要。现有的拓扑设计方法大多是“**以节点为中心**”的，即逐个预测智能体及其连接。这种方式存在两个主要缺陷：
*   **缺乏宏观协作结构**：复杂任务通常需要特定的小组（如“开发-测试-审核”组）来分工协作，节点中心法难以显式建模这种高阶结构，导致协作效率低下。
*   **通信冗余与噪声传播**：无约束的节点连接会导致大量的 Token 消耗，并使任务无关的干扰信息在网络中扩散。
**GoAgent** 的核心动机是将“协作小组”视为构建 MAS 的基本原子单位，从而实现更高效、更具结构化的多智能体协作。

#### 2. 方法论：核心思想与关键技术
GoAgent 提出了从“以节点为中心”向“**以小组为中心**”的范式转变，主要包含以下四个阶段：
*   **任务编码与小组发现**：利用预训练编码器将任务请求（Query）向量化。同时，利用 GPT-4 预先生成一个包含多种专家角色和内部拓扑（如链式、全连接、星型）的**候选小组池**。
*   **自回归小组生成**：模型根据当前任务和已生成的拓扑状态，利用 GRU（门控循环单元）逐步预测下一个应加入的小组。
*   **组间边缘预测**：在选定小组后，模型预测该小组与之前已存在小组之间的通信连接。
*   **条件信息瓶颈（CIB）**：这是本文的关键技术。为了过滤冗余信息，引入了 CIB 层。它以任务向量为条件，对组间通信特征进行压缩，仅保留与任务最相关的信号，从而抑制噪声传播并减少不必要的连接。
*   **训练目标**：结合了预测任务损失（交叉熵）和 KL 散度正则项（用于信息压缩），通过教师强制（Teacher Forcing）在启发式搜索生成的优质轨迹上进行训练。

#### 3. 实验设计
*   **数据集/场景**：涵盖了通用推理（MMLU）、数学推理（GSM8K, MultiArith, SVAMP, AQuA）和代码生成（HumanEval）共六大基准测试。
*   **对比方法**：
    *   **单智能体**：Vanilla, CoT, Self-Consistency。
    *   **固定拓扑 MAS**：Chain, Tree, Complete, Random, LLM-Debate。
    *   **可学习拓扑 MAS**：AgentPrune, AgentDropout, G-Designer, EIB-LEARNER, ARG-Designer（最新的自回归节点生成方法）。

#### 4. 资源与算力
*   **模型调用**：主要使用 `gpt-4o-2024-08-06` 接口进行智能体执行。
*   **生成器训练**：论文提到使用了 AdamW 优化器，训练 100 个 epoch，batch size 为 40。
*   **算力细节**：文中**未明确说明**具体的 GPU 型号、数量及总训练时长。但提到生成器本身非常轻量（隐藏层维度为 256），且每类任务仅需 40-60 条查询数据即可完成训练，暗示其训练开销远小于 LLM 推理开销。

#### 5. 实验数量与充分性
*   **实验规模**：在 6 个主流数据集上进行了全面测试，并进行了消融实验（验证小组机制和 CIB 的有效性）、Token 效率分析、鲁棒性分析（针对提示词注入攻击）以及案例研究。
*   **充分性与公平性**：实验对比了从基础到最前沿的各类方法，覆盖面广。消融实验清晰地展示了各组件的贡献。鲁棒性实验模拟了真实场景下的攻击，增强了说服力。整体实验设计较为客观、严谨。

#### 6. 主要结论与发现
*   **性能领先**：GoAgent 在所有基准测试中均达到 SOTA，平均准确率达 93.84%，尤其在复杂任务（如 MMLU 和 HumanEval）上提升显著。
*   **高效率**：相比于之前的 SOTA 方法（如 ARG-Designer），GoAgent 在保持高精度的同时，减少了约 **17%** 的 Token 消耗。
*   **强鲁棒性**：得益于 CIB 的过滤机制，当系统中某个智能体受到恶意提示词攻击时，GoAgent 能有效阻止错误信息的扩散，表现出比节点中心法更强的稳定性。

#### 7. 优点（亮点）
*   **范式创新**：首次提出以“小组”为原子单位构建 MAS，符合人类组织管理的直觉。
*   **理论结合**：巧妙地将信息瓶颈（IB）理论应用于多智能体拓扑生成，解决了通信冗余与噪声传播的痛点。
*   **即插即用**：生成器模型轻量，训练数据需求极低，易于部署和扩展。

#### 8. 不足与局限
*   **依赖预定义池**：小组池是离线生成的，模型无法在推理过程中动态创造全新的角色或小组结构，灵活性受限于初始池的质量。
*   **任务类型局限**：目前主要集中在静态推理任务，尚未在高度动态、交互式的环境（如具身智能或实时多智能体强化学习场景）中进行验证。
*   **冷启动依赖**：需要通过启发式搜索预先构建高质量的训练轨迹，这在某些全新领域可能存在数据获取难度。

（完）
