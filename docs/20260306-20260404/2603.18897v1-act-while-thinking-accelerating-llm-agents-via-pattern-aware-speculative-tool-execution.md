---
title: "Act While Thinking: Accelerating LLM Agents via Pattern-Aware Speculative Tool Execution"
title_zh: 边思边行：通过模式感知的投机性工具执行加速大语言模型智能体
authors: "Yifan Sui, Han Zhao, Rui Ma, Zhiyuan He, Hao Wang, Jianxun Li, Yuqing Yang"
date: 2026-03-19
pdf: "https://arxiv.org/pdf/2603.18897v1"
tags: ["query:agent"]
score: 9.0
evidence: LLM智能体的投机性工具执行
tldr: 针对LLM智能体在“模型推理-工具调用”循环中因串行执行导致的严重延迟问题，本文提出PASTE方法。该方法利用智能体任务中稳定的控制流和可预测的数据依赖性，通过模式感知的投机性工具执行技术，在模型生成过程中预先执行工具以隐藏延迟。实验证明，PASTE在显著降低任务完成时间的同时，大幅提升了系统的执行吞吐量。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.18897v1/fig-001.webp\", \"caption\": \"Figure 4: Histogram of tool execution time. Blue bars repre-\", \"page\": 3, \"index\": 1, \"width\": 484, \"height\": 294}, {\"url\": \"assets/figures/arxiv/2603.18897v1/fig-002.webp\", \"caption\": \"Figure 6: Example user-defined speculation eligibility policy.\", \"page\": 8, \"index\": 2, \"width\": 518, \"height\": 329}, {\"url\": \"assets/figures/arxiv/2603.18897v1/fig-003.webp\", \"caption\": \"Figure 5: System architecture of PASTE.\", \"page\": 4, \"index\": 3, \"width\": 425, \"height\": 350}]"
motivation: LLM智能体现有的串行执行模式导致模型在等待外部工具返回结果时产生巨大的延迟瓶颈。
method: 提出PASTE方法，通过识别应用层稳定的工具调用序列和参数传递模式，实现投机性的工具并行执行。
result: "与现有基准相比，PASTE将平均任务完成时间缩短了48.5%，并将工具执行吞吐量提升了1.8倍。"
conclusion: PASTE通过投机执行机制有效隐藏了工具调用延迟，是加速LLM智能体自主任务处理的高效方案。
---

## 摘要
以大语言模型（LLM）驱动的智能体正成为自主任务解决的主流范式。与标准的推理负载不同，智能体运行在严格串行的“LLM-工具”循环中，LLM 在每一步都必须等待外部工具的执行。这种执行模型引入了严重的延迟瓶颈。为解决这一问题，我们提出了 PASTE，一种模式感知的投机性工具执行方法，旨在通过投机执行来隐藏工具延迟。PASTE 基于以下洞察：尽管智能体请求在语义上具有多样性，但它们表现出稳定的应用级控制流（重复出现的工具调用序列）和可预测的数据依赖关系（工具间的参数传递）。通过利用这些特性，PASTE 通过投机性工具执行提升了智能体服务的性能。与最先进的基准方法相比，实验结果表明 PASTE 将平均任务完成时间缩短了 48.5%，并将工具执行吞吐量提高了 1.8 倍。

## Abstract
LLM-powered agents are emerging as a dominant paradigm for autonomous task solving. Unlike standard inference workloads, agents operate in a strictly serial "LLM-tool" loop, where the LLM must wait for external tool execution at every step. This execution model introduces severe latency bottlenecks. To address this problem, we propose PASTE, a Pattern-Aware Speculative Tool Execution method designed to hide tool latency through speculation. PASTE is based on the insight that although agent requests are semantically diverse, they exhibit stable application level control flows (recurring tool-call sequences) and predictable data dependencies (parameter passing between tools). By exploiting these properties, PASTE improves agent serving performance through speculative tool execution. Experimental results against state of the art baselines show that PASTE reduces average task completion time by 48.5% and improves tool execution throughput by 1.8x.

---

## 论文详细总结（自动生成）

### 论文总结：Act While Thinking (PASTE)

#### 1. 核心问题与整体含义（研究动机和背景）
论文关注的是 **LLM 智能体（Agents）的执行效率瓶颈**。当前的智能体遵循严格串行的“思考-行动（LLM-Tool）”循环：LLM 生成工具调用指令 -> 等待外部工具执行 -> 获取结果 -> LLM 继续推理。
*   **痛点**：实验表明，工具执行时间占总请求时间的 35% 到 61%。在工具执行期间，昂贵的 LLM 计算资源处于闲置状态，导致极高的端到端延迟。
*   **核心目标**：通过“边思边行”（在 LLM 生成过程中投机性地预执行工具），打破串行依赖，隐藏工具执行延迟。

#### 2. 方法论：核心思想与关键技术
PASTE 提出了**模式感知的投机性工具执行（Pattern-Aware Speculative Tool Execution）**，其核心思想是利用智能体行为在微观上的可预测性。

*   **核心思想**：虽然智能体任务多样，但其工具调用序列（控制流）和参数传递（数据流）具有稳定的模式。例如，`git clone` 后通常接 `git checkout`；`search` 结果中的 URL 通常是后续 `web_fetch` 的参数。
*   **关键技术细节**：
    *   **模式元组（Pattern Tuple）**：定义为 `(context, prediction, function, probability)`。它将执行结构（工具类型序列）与执行内容（具体的参数映射函数）解耦。
    *   **模式挖掘与验证**：通过两阶段挖掘算法从历史轨迹中提取频繁出现的工具序列，并推断参数之间的符号依赖关系（如从 JSON 结果中提取特定字段作为下一个工具的输入）。
    *   **机会主义调度器（Opportunistic Scheduler）**：
        *   **优先级管理**：将任务分为“权威调用”（正式请求）和“投机调用”（预测请求）。
        *   **资源隔离与抢占**：投机任务仅利用空闲（Slack）资源运行，一旦发生资源竞争，立即抢占或终止投机任务，确保不干扰正式任务。
        *   **任务晋升**：如果 LLM 随后发出的正式请求与正在运行的投机任务匹配，该任务会被“晋升”为正式任务，直接返回结果。

#### 3. 实验设计
*   **应用场景与智能体**：
    *   **Virtual-Lab**：科学研究智能体。
    *   **Qwen Deep Research**：深度研究与报告撰写智能体。
    *   **Gemini-CLI**：代码修改与通用任务智能体。
*   **数据集/Benchmark**：DeepResearchBench（深度研究）、SWE-bench（软件工程/代码修复）、ScholarQA（科学领域问答）。
*   **对比方法**：
    *   **ORION**：针对 Serverless DAG 执行优化的系统。
    *   **SpecFaaS**：针对 Serverless 函数的投机执行系统。

#### 4. 资源与算力
*   **硬件环境**：使用了一个包含 4 个节点的集群。每个节点配备 96 核 AMD EPYC CPU、512GB 内存和 **8 张 NVIDIA A100-80G GPU**（总计 32 张 A100）。
*   **模型配置**：
    *   **API**：Gemini-2.5-Pro, GPT-5.2。
    *   **本地模型**：Qwen-DeepResearch-30B。
*   **实现**：中间层代理使用 TypeScript（约 8000 行）和 Python（约 4000 行）编写。

#### 5. 实验数量与充分性
*   **实验规模**：论文进行了多维度的评估，包括端到端延迟、工具执行延迟、吞吐量测试、预测准确率分析、可扩展性测试以及副作用安全性审计。
*   **充分性与公平性**：
    *   使用了真实的生产环境轨迹（Azure Functions 轨迹）来模拟请求到达过程，增加了实验的现实意义。
    *   在相同的硬件、LLM 配置和资源预算下对比基准方法，确保了对比的公平性。
    *   测试了超过 20,000 次投机动作以验证安全性。

#### 6. 主要结论与发现
*   **延迟降低**：PASTE 将平均任务完成时间缩短了 **48.5%**，在某些场景下尾部延迟（P99）降低了 61.9%。
*   **性能提升**：工具执行吞吐量提升了 **1.8 倍**，平均工具执行加速比达 1.71x - 1.83x。
*   **预测质量**：虽然 Top-1 预测准确率约为 27.8%，但 **Top-3 召回率达到 43.9%**，整体命中率高达 **93.8%**，证明了投机执行的有效性。
*   **低开销**：每减少 1 秒延迟仅消耗极少的额外 CPU 和内存资源，调度开销小于 100ms。

#### 7. 优点
*   **解耦设计**：通过模式元组解耦控制流和数据流，解决了 LLM 生成内容高度波动导致的预测难题。
*   **风险控制**：引入了用户定义的“投机准入策略”，能够处理带有副作用的操作（如通过 dry-run 替代实际执行），保证了系统的安全性。
*   **资源高效**：采用机会主义调度，仅利用闲置资源，不影响主任务性能。

#### 8. 不足与局限
*   **冷启动依赖**：模式挖掘依赖于历史执行轨迹，对于全新的、无历史参考的智能体应用，初期加速效果可能受限。
*   **参数预测局限**：目前的参数映射函数（Value Mapping Function）主要基于简单的字段提取和索引，对于需要复杂逻辑推理才能生成的参数，预测成功率可能较低。
*   **副作用处理风险**：虽然有策略控制，但对于某些难以定义“安全变体”的破坏性工具，系统只能选择不进行投机，限制了加速空间。

（完）
