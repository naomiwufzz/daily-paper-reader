---
title: Enhancing Web Agents with a Hierarchical Memory Tree
title_zh: 利用层级记忆树增强 Web 智能体
authors: "Yunteng Tan, Zhi Gao, Xinxiao Wu"
date: 2026-03-07
pdf: "https://arxiv.org/pdf/2603.07024v1"
tags: ["query:agent"]
score: 9.0
evidence: LLM网络智能体逻辑规划的分层记忆结构
tldr: 针对LLM网页智能体在跨网站任务中泛化性差的问题，本文提出分层记忆树（HMT）框架。HMT通过自动化流水线将历史轨迹抽象为意图、阶段和动作三层结构，实现了逻辑规划与具体执行的解耦。配合阶段感知的推理机制，HMT能有效避免工作流失配，显著提升了智能体在Mind2Web和WebArena等基准测试中的跨域泛化能力。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.07024v1/fig-001.webp\", \"caption\": \"Fig. 6. Analysis of two representative failure modes in cross-website grounding. (a) Ambiguous Grounding: The agent retrieves a generic descriptor (text_contains: more) lacking hierarchical constraints. Consequently, the Actor assigns a higher similarity (score 0.92) to the distractor candidate than to the intended target (score 0.89), leading to an execution error. (b) State Verification Error: The Planner retrieves a strict success criterion expecting a NAVIGATION event (e.g., URL change or page reload). Although the action triggers a visual update (modal popup), the URL remains static. The Planner’s rigid verification logic (url_change = False and page_reload = False) fails to recognize the visual progress, incorrectly marking the step as incomplete and initiating a retry loop.\", \"page\": 10, \"index\": 1, \"width\": 969, \"height\": 591}]"
motivation: 现有的扁平化记忆结构将高层逻辑与特定网站的动作细节混杂，导致智能体在面对新环境时出现逻辑不一致和工作流失配。
method: 提出HMT框架，将记忆组织为意图、阶段（含预设/后置条件）和动作三层，并采用规划器与执行器分离的推理机制。
result: 在Mind2Web和WebArena上的实验表明，HMT在跨网站和跨领域场景下的表现显著优于传统的扁平记忆方法。
conclusion: 结构化的记忆层次对于提升网页智能体的鲁棒性和跨环境泛化能力至关重要。
---

## 摘要
基于大语言模型的 Web 智能体通过先进的推理和指令遵循能力，在自动化 Web 交互方面展现出强大潜力。虽然源自历史轨迹的检索式记忆使这些智能体能够处理复杂的长程任务，但现有方法在泛化到未见过的网站时仍面临挑战。我们发现，这一挑战源于扁平化的记忆结构，这种结构将高层任务逻辑与特定网站的操作细节纠缠在一起。这种纠缠在面对新环境时会导致工作流失配，即检索到的内容与当前网页混淆，从而导致逻辑不一致的执行。为解决这一问题，我们提出了层级记忆树（HMT），这是一个旨在显式解耦逻辑规划与动作执行的结构化框架。HMT 通过自动化抽象流水线从原始轨迹构建三层架构：意图层（Intent level）将多样的用户指令映射为标准化的任务目标；阶段层（Stage level）定义了以可观测的前置条件和后置条件为特征的可重用语义子目标；动作层（Action level）存储了与可迁移语义元素描述相匹配的动作模式。利用该结构，我们开发了一种包含规划器（Planner）和执行器（Actor）的阶段感知推理机制。通过显式验证前置条件，规划器将当前状态与正确的逻辑子目标对齐以防止工作流失配，而执行器则通过将存储的语义描述与目标页面匹配来落实动作。在 Mind2Web 和 WebArena 上的实验结果表明，HMT 显著优于扁平记忆方法，特别是在跨网站和跨领域场景中，凸显了结构化记忆对于 Web 智能体鲁棒泛化的必要性。

## Abstract
Large language model-based web agents have shown strong potential in automating web interactions through advanced reasoning and instruction following. While retrieval-based memory derived from historical trajectories enables these agents to handle complex, long-horizon tasks, current methods struggle to generalize across unseen websites. We identify that this challenge arises from the flat memory structures that entangle high-level task logic with site-specific action details. This entanglement induces a workflow mismatch in new environments, where retrieved contents are conflated with current web, leading to logically inconsistent execution. To address this, we propose Hierarchical Memory Tree (HMT), a structured framework designed to explicitly decouple logical planning from action execution. HMT constructs a three-level hierarchy from raw trajectories via an automated abstraction pipeline: the Intent level maps diverse user instructions to standardized task goals; the Stage level defines reusable semantic subgoals characterized by observable pre-conditions and post-conditions; and the Action level stores action patterns paired with transferable semantic element descriptions. Leveraging this structure, we develop a stage-aware inference mechanism comprising a Planner and an Actor. By explicitly validating pre-conditions, the Planner aligns the current state with the correct logical subgoal to prevent workflow mismatch, while the Actor grounds actions by matching the stored semantic descriptions to the target page. Experimental results on Mind2Web and WebArena show that HMT significantly outperforms flat-memory methods, particularly in cross-website and cross-domain scenarios, highlighting the necessity of structured memory for robust generalization of web agents.

---

## 论文详细总结（自动生成）

以下是对论文《Enhancing Web Agents with a Hierarchical Memory Tree》的结构化深入总结：

### 1. 核心问题与研究背景
*   **研究动机**：现有的基于大语言模型（LLM）的 Web 智能体在处理复杂、长程任务时，通常依赖检索历史成功轨迹作为参考。然而，这些记忆大多采用“扁平化”结构，将高层任务逻辑（如“预订机票”）与特定网站的底层动作细节（如点击特定的 HTML ID `#btn-123`）混杂在一起。
*   **核心痛点**：这种“意图-执行纠缠”（Intention-Execution Entanglement）导致智能体在面对未见过的新网站时，会尝试执行旧网站的特定动作，产生工作流失配和上下文污染，严重限制了智能体的跨网站泛化能力。

### 2. 方法论：层级记忆树（HMT）
*   **核心思想**：通过构建一个三层结构的记忆树，显式地将“做什么（逻辑规划）”与“怎么做（动作执行）”解耦。
*   **关键技术细节**：
    *   **意图层（Intent Level）**：将多样的用户指令标准化为统一的任务目标和约束，消除表述差异。
    *   **阶段层（Stage Level）**：将任务分解为可重用的语义子目标（如“过滤结果”），并为每个阶段定义**预置条件**和**后置条件**（基于可观测的 UI 状态），用于对齐执行进度。
    *   **动作层（Action Level）**：存储抽象的动作模式和**语义元素描述**（如“点击包含‘搜索’文字的按钮”），而非原始的 DOM ID 或坐标。
*   **推理机制（Planner-Actor）**：
    *   **Planner（规划器）**：负责状态抽象与阶段选择，通过验证当前页面是否满足预置/后置条件，确保智能体处于正确的逻辑阶段。
    *   **Actor（执行器）**：负责动作落地，根据动作层的语义描述在当前页面寻找匹配的 UI 元素并执行。
    *   **置信度回退机制**：当 Planner 对阶段选择不确定时，会自动扩大检索范围或回退至无记忆基准策略，增强鲁棒性。

### 3. 实验设计
*   **数据集/场景**：
    *   **Mind2Web**：离线设置，包含 137 个网站的 2000+ 任务，重点测试跨任务（Cross-Task）、跨网站（Cross-Website）和跨领域（Cross-Domain）的泛化能力。
    *   **WebArena**：在线交互设置，涵盖购物、CMS、GitLab、地图等领域，测试智能体在部署过程中的知识积累。
*   **对比方法**：包括 MindAct（通用智能体）、AWM（智能体工作流记忆）、SteP、AutoEval 等代表性方法，以及一个“扁平检索”基准（Flat Retrieval）。

### 4. 资源与算力
*   **模型使用**：全流程采用 **GPT-4** 作为骨干模型（Backbone）。
*   **算力说明**：论文未明确提及具体的 GPU 型号、数量或训练时长。由于该方法主要基于 LLM 的 Prompting 和自动化抽象流水线，而非大规模参数微调，其算力消耗主要集中在 API 调用和推理延迟上。

### 5. 实验数量与充分性
*   **实验规模**：实验涵盖了 Mind2Web 的三个测试切片和 WebArena 的五个主要领域，实验组数充足。
*   **消融实验**：设计了 5 组消融实验，分别验证了层级结构、前后置条件、语义描述、规划器模块以及置信度回退机制的有效性。
*   **客观性**：通过对比 StepSR（步骤成功率）和 TaskSR（任务成功率）等标准指标，并进行了详细的检索质量分析和效率分析，实验设计较为客观、公平。

### 6. 主要结论与发现
*   **泛化性能显著提升**：在 Mind2Web 的跨网站测试中，HMT 的步骤成功率比 AWM 提升了 6.0%，证明了语义描述在处理布局变化时的优越性。
*   **逻辑一致性增强**：在 GitLab 和 CMS 等逻辑复杂的长程任务中，HMT 显著优于扁平记忆方法，有效减少了工作流失配。
*   **效率优化**：由于将冗长的原始 HTML 轨迹抽象为紧凑的语义节点，上下文 Token 消耗减少了约 **72.7%**，推理成本降低了 **71.0%**。

### 7. 优点（亮点）
*   **解耦设计**：成功解决了意图与执行的纠缠问题，是提升 Web 智能体泛化性的关键。
*   **语义化定位**：利用语义描述代替硬编码 ID，使记忆具备了跨平台的“可迁移性”。
*   **阶段感知**：引入前后置条件验证，使智能体具备了自我纠错和进度对齐的能力。

### 8. 不足与局限
*   **空间任务局限**：在依赖精确空间坐标的任务（如地图操作）中，纯文本的语义抽象效果不如原始轨迹回放。
*   **语义歧义风险**：当页面存在多个语义相似的元素（如多个“更多”按钮）且缺乏足够的上下文约束时，Actor 可能会发生误触。
*   **状态验证失效**：在某些单页应用（SPA）中，视觉更新不伴随 URL 变化，Planner 可能会因为无法捕捉到预期的“导航信号”而陷入重试循环。

（完）
