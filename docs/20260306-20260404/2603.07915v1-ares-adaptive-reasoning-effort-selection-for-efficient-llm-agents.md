---
title: "Ares: Adaptive Reasoning Effort Selection for Efficient LLM Agents"
title_zh: Ares：面向高效大语言模型智能体的自适应推理开销选择
authors: "Jingbo Yang, Bairu Hou, Wei Wei, Yujia Bao, Shiyu Chang"
date: 2026-03-09
pdf: "https://arxiv.org/pdf/2603.07915v1"
tags: ["query:agent"]
score: 9.0
evidence: 多步智能体任务中的自适应推理力度选择
tldr: "针对大模型智能体在多步任务中推理成本高昂的问题，本文提出Ares框架。Ares通过轻量级路由模型，为每个任务步骤动态选择最合适的推理级别（如高/中/低）。通过专门的数据生成流水线训练路由，实现在保证任务成功率的同时大幅降低推理Token消耗。在多个基准测试中，Ares在几乎不损失性能的前提下，减少了高达52.7%的推理成本。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.07915v1/fig-001.webp\", \"caption\": \"Table 6. Dataset statistics of reasoning-effort labels across benchmarks.\", \"page\": 15, \"index\": 1, \"width\": 1017, \"height\": 1151}, {\"url\": \"assets/figures/arxiv/2603.07915v1/fig-002.webp\", \"caption\": \"Figure 1. Overview of the Adaptive Reasoning Effort Selection (ARES) Framework. Left: Traditional Model Routing which often incurs extra inference costs without KV cache reuse. Middle: Our proposed ARES framework, dynamically allocates reasoning effort at each step. Right: ARES (red star) achieves the optimal balance between performance and cost compared to baselines.\", \"page\": 2, \"index\": 2, \"width\": 1013, \"height\": 262}, {\"url\": \"assets/figures/arxiv/2603.07915v1/fig-003.webp\", \"caption\": \"Table 1. Main evaluation results in TAU-Bench, BrowserComp-Plus, and WebArena, using gpt-oss-20b as the backbone LLM. Arrows (↑ / ↓) indicating an increase or decrease in absolute value. Orange and blue indicate beneficial and detrimental changes.\", \"page\": 7, \"index\": 3, \"width\": 958, \"height\": 865}, {\"url\": \"assets/figures/arxiv/2603.07915v1/fig-004.webp\", \"caption\": \"Table 2. RL results in TAU-Bench using gpt-oss-20b as the backbone LLM. Arrows (↑ / ↓) indicating an increase or decrease in absolute value. Orange and blue indicate beneficial and detrimental changes.\", \"page\": 8, \"index\": 4, \"width\": 956, \"height\": 442}, {\"url\": \"assets/figures/arxiv/2603.07915v1/fig-005.webp\", \"caption\": \"Figure 2. Overview of the ARES training pipeline. (1) Trajectory Collection: Optimal ground-truth paths are defined by filtering successful trajectories with minimal steps. (2) Effort Annotation: The minimum sufficient reasoning effort for each step is identified via sampling and LLM verification. (3) Rationale Generation: A teacher LLM generates semantic justifications based on task observations and complexity. (4) Supervised Fine-tuning: The ARES router is fine-tuned to jointly predict rationales and effort labels. (5) Reinforcement Learning: The fine-tuned ARES router will be further trained using GRPO with outcome, reasoning cost, and format reward.\", \"page\": 4, \"index\": 5, \"width\": 1013, \"height\": 721}, {\"url\": \"assets/figures/arxiv/2603.07915v1/fig-006.webp\", \"caption\": \"Figure 3. Selection of reasoning effort by ARES on the WebArena benchmark. Left: Percentage distribution of low, medium, and high effort levels across task step indices. Right: Distribution of effort levels categorized by specific action types.\", \"page\": 9, \"index\": 6, \"width\": 1010, \"height\": 296}]"
motivation: 长链条推理虽然准确但成本极高，且不同任务步骤对推理能力的需求存在显著差异，固定高强度推理造成了资源浪费。
method: 提出一种动态推理选择框架，利用轻量级路由器预测每步所需的最小推理级别，并通过自动化的数据生成流水线进行微调。
result: "在TAU-Bench、WebArena等多个智能体任务中，Ares在维持高成功率的同时，将推理Token使用量降低了最高52.7%。"
conclusion: Ares证明了按需分配推理资源是提升大模型智能体效率的有效途径，为低成本、高性能的智能体部署提供了新方案。
---

## 摘要
由具备思考能力的大语言模型（LLM）驱动的现代智能体通过长链条思维推理实现了高准确率，但也产生了巨大的推理成本。虽然许多 LLM 现在支持可配置的推理级别（如高/中/低），但静态策略通常效果不佳：在每一步都使用低开销模式会导致性能显著下降，而随机选择则无法保证准确率或提供实质性的成本降低。然而，智能体应当将高推理开销保留给困难步骤（如导航复杂的网站结构），而在简单步骤（如打开目标 URL）中使用低开销模式。在本文中，我们提出了 Ares，这是一个专为多步智能体任务量身定制的逐步骤动态推理开销选择框架。Ares 采用一个轻量级路由模型，根据交互历史预测每一步最合适的最低推理级别。为了训练该路由模型，我们开发了一个数据生成流水线，用于识别成功完成步骤所需的最小推理开销。随后，我们对路由模型进行微调以预测这些级别，从而实现对任何 LLM 智能体的即插即用集成。我们在多种智能体任务上评估了 Ares，包括面向工具使用智能体的 TAU-Bench、面向深度研究智能体的 BrowseComp-Plus 以及面向 Web 智能体的 WebArena。实验结果表明，与固定的高开销推理相比，Ares 将推理 Token 使用量减少了高达 52.7%，同时对任务成功率的影响微乎其微。

## Abstract
Modern agents powered by thinking LLMs achieve high accuracy through long chain-of-thought reasoning but incur substantial inference costs. While many LLMs now support configurable reasoning levels (e.g., high/medium/low), static strategies are often ineffective: using low-effort modes at every step leads to significant performance degradation, while random selection fails to preserve accuracy or provide meaningful cost reduction. However, agents should reserve high reasoning effort for difficult steps like navigating complex website structures, while using lower-effort modes for simpler steps like opening a target URL. In this paper, we propose Ares, a framework for per-step dynamic reasoning effort selection tailored for multi-step agent tasks. Ares employs a lightweight router to predict the lowest appropriate reasoning level for each step based on the interaction history. To train this router, we develop a data generation pipeline that identifies the minimum reasoning effort required for successful step completion. We then fine-tune the router to predict these levels, enabling plug-and-play integration for any LLM agents. We evaluate Ares on a diverse set of agent tasks, including TAU-Bench for tool use agents, BrowseComp-Plus for deep-research agents, and WebArena for web agents. Experimental results show that Ares reduces reasoning token usage by up to 52.7% compared to fixed high-effort reasoning, while introducing minimal degradation in task success rates.

---

## 论文详细总结（自动生成）

### 论文总结：Ares：面向高效大语言模型智能体的自适应推理开销选择

#### 1. 核心问题与整体含义（研究动机和背景）
随着推理型大语言模型（如 OpenAI o1, DeepSeek-R1 等）的兴起，智能体（Agents）通过长链条思维（CoT）在复杂任务中取得了显著进步。然而，这种能力的提升伴随着巨大的**推理成本**。在多步决策任务中，并非每一步都需要高强度的推理（例如，打开一个 URL 远比规划复杂的搜索路径简单）。
*   **核心矛盾**：固定使用高强度推理会导致严重的资源浪费；而固定使用低强度推理或随机选择则会显著降低任务成功率。
*   **研究目标**：开发一种能够针对智能体任务的每一个步骤，动态选择“最经济且足够”的推理级别的框架，以平衡性能与成本。

#### 2. 方法论：核心思想与关键技术
论文提出了 **ARES** 框架，其核心是一个轻量级的**推理开销路由器（Router）**。
*   **核心思想**：在智能体执行每一步动作前，先由一个轻量级模型（如 Qwen3-1.7B）分析当前环境和历史，预测该步骤所需的最小推理级别（Low, Medium, High），随后主模型按此级别生成推理和动作。
*   **关键技术细节**：
    1.  **自动化数据生成流水线**：
        *   **轨迹收集**：首先获取高质量的成功任务轨迹。
        *   **最小开销标注**：对轨迹中的每一步，分别用不同推理级别进行多次采样（K=3），识别出能稳定产生正确动作的**最低**级别作为标签。
        *   **理由生成（Rationale Generation）**：利用强大的教师模型（如 GPT-5）为这些标签生成简短的解释，辅助路由器学习。
    2.  **训练策略**：
        *   **监督微调（SFT）**：训练路由器预测“理由 + 推理级别”。
        *   **强化学习（RL）**：使用 **GRPO 算法**进一步优化。奖励函数设计为：$R = R_{outcome} + R_{cost} + R_{format}$。其中 $R_{cost}$ 对成功轨迹中的 Token 消耗进行惩罚，促使模型在保证成功的前提下选择更低的开销。
    3.  **KV Cache 复用**：由于是在同一模型的不同推理模式间切换，ARES 支持 KV 缓存复用，避免了多模型路由带来的额外计算延迟。

#### 3. 实验设计
*   **实验场景与数据集**：
    *   **TAU-Bench**：评估工具使用能力（零售与航空领域）。
    *   **BrowseComp-Plus**：评估深度研究与搜索能力。
    *   **WebArena**：评估复杂的网页导航与真实环境交互能力。
*   **Benchmark 与对比方法**：
    *   **主模型（Backbone）**：主要使用 `gpt-oss-20b`。
    *   **对比基准**：固定策略（Always Low/Medium/High）、随机策略（Random）、基于 Prompt 的路由（使用 GPT-5 或 Gemini-3 Pro 作为路由器）。

#### 4. 资源与算力
*   **模型规模**：路由器采用了轻量级的 `Qwen3-1.7B`。
*   **训练细节**：SFT 阶段训练 3 个 epoch，学习率 5e-6；RL 阶段训练 5 个 epoch，学习率 1.5e-6，Group size G=16。
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量及总训练时长。但从模型规模（1.7B）和数据集规模（约 1k-40k 条数据）推断，其训练开销相对较小。

#### 5. 实验数量与充分性
*   **实验规模**：涵盖了三个领域截然不同的基准测试，总计处理了数万条训练样本（如 TAU-Bench 约 4.3 万条）。
*   **消融实验**：非常充分。作者验证了 SFT 的必要性、推理理由（Rationale）的作用、RL 奖励函数中归一化的影响，以及在不同规模主模型（从 20B 扩展到 120B）上的泛化能力。
*   **客观性**：通过多次采样（K=3）来消除 LLM 生成的随机性影响，并使用了 LLM Judge 进行语义等价性评估，实验设计较为严谨、公平。

#### 6. 主要结论与发现
*   **大幅降本**：相比于固定的高强度推理，ARES 在 TAU-Bench 上减少了高达 **52.7%** 的推理 Token 消耗。
*   **性能保持甚至提升**：在 WebArena 中，ARES 的成功率（46.5%）甚至超过了固定高强度推理（45.0%）。这表明**过度思考（Overthinking）**有时反而会导致逻辑漂移或格式错误，而动态调整能规避此问题。
*   **强泛化性**：在 20B 模型上训练的路由器可以直接应用于 120B 模型，并依然保持显著的节流效果。

#### 7. 优点（亮点）
*   **即插即用**：作为外部路由器，无需修改主模型参数，易于集成到现有智能体架构中。
*   **效率极高**：利用轻量级模型做决策，且支持 KV Cache 复用，几乎不增加推理延迟。
*   **数据驱动的精细化控制**：通过 RL 进一步微调，解决了 SFT 贪婪预测无法处理多步误差累积的问题。

#### 8. 不足与局限
*   **依赖模型特性**：该方法前提是主模型必须支持可配置的推理级别（如内置的思考模式切换），对于不支持此类接口的模型适用性受限。
*   **标注成本**：虽然是自动化流水线，但为了确定“最小开销”，需要对每一步进行多次推理尝试，初期数据准备的计算量较大。
*   **任务覆盖**：目前主要集中在文本和网页交互，尚未在多模态智能体（如视觉导航）或极长时程的任务中进行验证。

（完）
