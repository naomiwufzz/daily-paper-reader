---
title: "Governed Memory: A Production Architecture for Multi-Agent Workflows"
title_zh: 受控记忆：一种用于多智能体工作流的生产级架构
authors: Hamed Taheri
date: 2026-03-18
pdf: "https://arxiv.org/pdf/2603.17787v1"
tags: ["query:agent"]
score: 9.0
evidence: 多智能体工作流的共享记忆与治理层
tldr: 针对企业级多智能体工作流中存在的内存孤岛、治理碎片化和冗余交付等挑战，本文提出了“受控内存”（Governed Memory）架构。该架构通过双重内存模型、分层治理路由、反射约束检索和闭环模式生命周期，实现了跨智能体的共享内存与统一治理。实验证明，该系统在保持高召回率和合规性的同时，显著降低了Token消耗，并已在Personize.ai投入生产。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.17787v1/fig-001.webp\", \"caption\": \"Figure 2. Dual extraction pipeline. A single LLM call produces both open-set facts and schemaenforced typed properties, followed by quality gates and write-side deduplication.\", \"page\": 6, \"index\": 1, \"width\": 896, \"height\": 323}, {\"url\": \"assets/figures/arxiv/2603.17787v1/fig-002.webp\", \"caption\": \"Figure 3. Governance routing with tiered modes (fast/full) and progressive context delivery. The session layer tracks delivered variables to inject only delta content on each autonomous step.\", \"page\": 7, \"index\": 2, \"width\": 891, \"height\": 361}, {\"url\": \"assets/figures/arxiv/2603.17787v1/fig-003.webp\", \"caption\": \"Table 1. Experimental dataset summary.\", \"page\": 10, \"index\": 3, \"width\": 825, \"height\": 202}, {\"url\": \"assets/figures/arxiv/2603.17787v1/fig-004.webp\", \"caption\": \"Table 8. Recurring diagnostic patterns from evaluation records.\", \"page\": 23, \"index\": 4, \"width\": 733, \"height\": 302}, {\"url\": \"assets/figures/arxiv/2603.17787v1/fig-005.webp\", \"caption\": \"Figure 4. Reflection-bounded retrieval with entity-scoped isolation. The optional reflection loop generates targeted follow-up queries within bounded rounds; entity isolation is enforced by CRM key prefiltering.\", \"page\": 8, \"index\": 5, \"width\": 890, \"height\": 385}, {\"url\": \"assets/figures/arxiv/2603.17787v1/fig-006.webp\", \"caption\": \"Figure 1. Governed Memory four-layer architecture. Agent nodes interact through a shared, organization-scoped API surface. Layers are independently configurable.\", \"page\": 5, \"index\": 6, \"width\": 787, \"height\": 479}]"
motivation: 企业级AI应用中多个独立智能体缺乏共享内存和统一治理，导致数据孤岛、合规风险及资源浪费。
method: 提出一种结合原子事实与强类型属性的双重内存模型，并引入分层治理路由、实体隔离检索及AI辅助的闭环模式管理。
result: "实验显示该架构实现了99.6%的事实召回率、100%的合规性，并减少了50%的Token使用量，且不损失检索质量。"
conclusion: 受控内存架构有效解决了多智能体协作中的治理与效率难题，是实现生产级可靠AI工作流的关键基础设施。
---

## 摘要
企业级 AI 在工作流中部署了数十个自主智能体节点，每个节点作用于相同的实体，但缺乏共享记忆和统一治理。我们识别了由这种记忆治理缺失导致的五个结构性挑战：跨智能体工作流的记忆孤岛；跨团队和工具的治理碎片化；下游系统无法使用的非结构化记忆；自主多步执行中的冗余上下文传递；以及缺乏反馈循环导致的隐性质量下降。我们提出了“受控记忆”（Governed Memory），这是一个通过四种机制解决上述差距的共享记忆与治理层：结合了开集原子事实与模式强制（schema-enforced）类型属性的双重记忆模型；具有渐进式上下文传递的分层治理路由；具有实体范围隔离的反思限制检索；以及包含 AI 辅助编写和自动化逐属性精炼的闭环模式生命周期。我们通过受控实验（N=250，五种内容类型）验证了每种机制：互补的双模态覆盖实现了 99.6% 的事实召回率；治理路由准确率达 92%；渐进式传递减少了 50% 的 Token 消耗；在 500 次对抗性查询中实现了零跨实体泄露；100% 的对抗性治理合规性；且输出质量在每个实体约七个受控记忆时达到饱和。在 LoCoMo 基准测试中，该架构实现了 74.8% 的总准确率，证实了治理和模式强制不会造成检索质量损失。该系统已在 Personize.ai 投入生产。

## Abstract
Enterprise AI deploys dozens of autonomous agent nodes across workflows, each acting on the same entities with no shared memory and no common governance. We identify five structural challenges arising from this memory governance gap: memory silos across agent workflows; governance fragmentation across teams and tools; unstructured memories unusable by downstream systems; redundant context delivery in autonomous multi-step executions; and silent quality degradation without feedback loops. We present Governed Memory, a shared memory and governance layer addressing this gap through four mechanisms: a dual memory model combining open-set atomic facts with schema-enforced typed properties; tiered governance routing with progressive context delivery; reflection-bounded retrieval with entity-scoped isolation; and a closed-loop schema lifecycle with AI-assisted authoring and automated per-property refinement. We validate each mechanism through controlled experiments (N=250, five content types): 99.6% fact recall with complementary dual-modality coverage; 92% governance routing precision; 50% token reduction from progressive delivery; zero cross-entity leakage across 500 adversarial queries; 100% adversarial governance compliance; and output quality saturation at approximately seven governed memories per entity. On the LoCoMo benchmark, the architecture achieves 74.8% overall accuracy, confirming that governance and schema enforcement impose no retrieval quality penalty. The system is in production at Personize.ai.

---

## 论文详细总结（自动生成）

这是一份关于论文《Governed Memory: A Production Architecture for Multi-Agent Workflows》的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
在企业级 AI 应用中，通常会部署数十个自主智能体（Agent）节点来处理相同的一组实体（如客户、交易）。然而，现有的 RAG（检索增强生成）系统主要关注单次检索的准确性，导致了**“记忆治理鸿沟”**：
*   **记忆孤岛**：不同 Agent 之间不共享上下文。
*   **治理碎片化**：合规性、品牌调性和政策无法统一同步到所有 Agent。
*   **非结构化死胡同**：纯文本记忆无法被下游 CRM 或分析系统直接利用。
*   **上下文冗余**：在多步自主循环中，重复注入相同的政策导致 Token 浪费和模型注意力分散。
*   **质量隐性下降**：缺乏对记忆准确性和模式（Schema）漂移的监控。

该研究旨在构建一个生产级的共享记忆与治理层，使多个 Agent 能在统一的规则和结构化数据下协同工作。

### 2. 方法论：核心思想与关键技术
论文提出了一个四层架构的“受控记忆”系统：
*   **双重记忆模型（Dual Memory Model）**：
    *   **开集记忆**：提取原子化、自包含的事实（如“CTO 正在评估三个供应商”），以向量形式存储。
    *   **模式强制记忆**：根据预定义 Schema 提取强类型属性（如“交易金额: 450,000”），支持置信度评分和下游系统调用。
*   **分层治理路由（Tiered Governance Routing）**：
    *   **快速模式**：基于嵌入相似度和关键词匹配（约 850ms），无需 LLM 调用。
    *   **全量模式**：使用 LLM 进行两阶段结构化分析（2-5s）。
    *   **渐进式交付**：会话感知技术，仅向 Agent 注入自上次步骤以来的“增量”上下文，减少 Token 消耗。
*   **反思限制检索（Reflection-Bounded Retrieval）**：
    *   引入受限的反思循环（通常 ≤2 轮），由 LLM 判断证据是否完整，并生成针对性的后续查询。
    *   **实体隔离**：通过 CRM 键（如 Email、ID）进行硬隔离，防止跨租户/跨实体的数据泄露。
*   **闭环模式生命周期**：
    *   提供 AI 辅助的 Schema 编写、基于红利（Rubric）的自动评分以及逐属性的自动化精炼。

### 3. 实验设计与 Benchmark
*   **数据集**：使用包含 250 个样本的合成数据集（涵盖通话记录、文档、邮件、转录稿、聊天记录五种类型），并嵌入了已知的地标事实（Ground Truth）。
*   **Benchmark**：
    *   **LoCoMo**：评估长短期对话记忆的通用基准。
    *   **对抗性测试**：500 次对抗性查询用于测试实体隔离，50 个场景用于测试治理合规性。
*   **对比方法**：对比了“无记忆”、“原始 RAG 记忆”、“开集+治理”以及“全量受控记忆”四种配置。同时参考了 Mem0、Zep 和 OpenAI 内置记忆的公开表现。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量或训练时长。
*   **实现细节**：系统已在 Personize.ai 投入生产，使用了 LanceDB（向量库）和 DynamoDB（存储），嵌入模型使用了 `text-embedding-3-small`。由于该架构主要基于推理侧的编排和工程优化，而非从头训练大模型，因此其核心开销在于 LLM API 的调用。

### 5. 实验数量与充分性
*   **实验规模**：共进行了约 15 组核心实验（E1-E15），涵盖了从提取质量、路由精度、Token 节省到语义冲突解决等多个维度。
*   **充分性评价**：
    *   **客观性**：通过合成数据嵌入 Ground Truth 的方式，使得指标（如召回率、隔离率）可量化且客观。
    *   **覆盖面**：消融实验非常详尽，验证了每一层架构的增量价值。
    *   **局限性**：虽然 N=250 在统计上对于特定任务是足够的，但主要依赖合成数据可能无法完全模拟真实世界中极端杂乱、格式错误的生产数据。

### 6. 主要结论与发现
*   **高可靠性**：实现了 99.6% 的事实召回率和 100% 的对抗性治理合规性。
*   **效率提升**：渐进式上下文交付平均减少了 **50.3%** 的 Token 消耗。
*   **零泄露**：通过 CRM 键过滤实现了 3800 个结果中的零跨实体数据泄露。
*   **质量饱和点**：研究发现，当每个实体的受控记忆达到约 **7 条**时，输出质量趋于饱和，这为生产环境提供了重要的参数参考。
*   **无性能损失**：在 LoCoMo 基准上达到 74.8% 的准确率，证明增加治理层不会损害检索质量。

### 7. 优点与亮点
*   **架构完备性**：不仅关注检索，更关注“写入治理”和“模式演进”，填补了 RAG 到生产级 Agent 基础设施之间的空白。
*   **双模态互补**：成功证明了“非结构化事实”与“强类型属性”结合的必要性（38% 的洞察仅存在于开集记忆中）。
*   **工程实用性**：渐进式交付和快速路由模式直接解决了企业应用中的成本（Token）和延迟（Latency）痛点。

### 8. 不足与局限
*   **启发式依赖**：质量门控（如指代消解评分）依赖于模式匹配而非深层语义分析，可能存在误判。
*   **并发冲突未解决**：论文提到，虽然解决了时间上的语义冲突（新旧信息覆盖），但尚未测试多个 Agent 同时写入同一实体时的并发冲突。
*   **脱敏局限**：隐私脱敏主要基于正则匹配，对于非标准格式或上下文相关的敏感信息可能存在漏检风险。
*   **评估偏差**：自评估系统（LLM-as-a-Judge）虽然有红利约束，但仍可能存在模型固有的偏见。

（完）
