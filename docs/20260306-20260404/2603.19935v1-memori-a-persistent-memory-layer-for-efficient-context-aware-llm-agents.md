---
title: "Memori: A Persistent Memory Layer for Efficient, Context-Aware LLM Agents"
title_zh: Memori：一种用于高效、上下文感知 LLM 智能体的持久化记忆层
authors: "Luiz C. Borro, Luiz A. B. Macarini, Gordon Tindall, Michael Montero, Adam B. Struck"
date: 2026-03-20
pdf: "https://arxiv.org/pdf/2603.19935v1"
tags: ["query:agent"]
score: 9.0
evidence: 为上下文感知LLM智能体设计的持久记忆层
tldr: "本研究针对LLM智能体在多轮对话中持久化记忆缺失及Token成本高昂的问题，提出了Memori。这是一个与模型无关的持久化记忆层，通过将非结构化对话转化为语义三元组和摘要，实现了精准检索。实验表明，Memori在LoCoMo基准测试中达到81.95%的准确率，且Token消耗仅为全上下文方法的5%，显著降低了部署成本。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.19935v1/fig-001.webp\", \"caption\": \"Figure 1: High-level architecture illustrating the system’s structure, data flow, and the interaction between its core components.\", \"page\": 2, \"index\": 1, \"width\": 786, \"height\": 493}, {\"url\": \"assets/figures/arxiv/2603.19935v1/fig-002.webp\", \"caption\": \"Figure 2: Accuracy of Memori across different reasoning categories. Bar heights represent the mean accuracy (%), while error bars indicate the standard deviation for n = 3 runs.\", \"page\": 5, \"index\": 2, \"width\": 786, \"height\": 490}]"
motivation: 现有的LLM记忆方案通常依赖于注入大量原始对话，导致Token成本高昂、性能下降且存在供应商锁定。
method: Memori将记忆视为数据结构化问题，通过高级增强流水线将非结构化对话转换为紧凑的语义三元组和对话摘要。
result: "在LoCoMo基准测试中，Memori以仅5%的上下文长度实现了81.95%的准确率，比全上下文方法节省了20倍以上的成本。"
conclusion: 有效的智能体记忆应依赖于结构化数据表示而非单纯扩大上下文窗口，从而实现可扩展且低成本的部署。
---

## 摘要
随着大语言模型（LLM）演进为自主智能体，API 层的持久化记忆对于实现跨 LLM 和多会话交互的上下文感知行为至关重要。现有方法往往导致供应商锁定，并依赖于将大量原始对话注入提示词，从而导致高昂的 Token 成本和性能下降。我们提出了 Memori，这是一个与 LLM 无关的持久化记忆层，它将记忆视为一个数据结构化问题。其高级增强流水线将非结构化对话转换为紧凑的语义三元组和对话摘要，从而实现精确检索和连贯推理。在 LoCoMo 基准测试的评估中，Memori 达到了 81.95% 的准确率，在优于现有记忆系统的同时，每次查询仅使用 1,294 个 Token（约占全上下文的 5%）。这显著降低了成本，包括比竞争方法减少了 67% 的 Token 使用量，且与全上下文方法相比节省了 20 倍以上的成本。这些结果表明，LLM 智能体中有效的记忆取决于结构化表示而非更大的上下文窗口，从而实现了可扩展且具有成本效益的部署。

## Abstract
As large language models (LLMs) evolve into autonomous agents, persistent memory at the API layer is essential for enabling context-aware behavior across LLMs and multi-session interactions. Existing approaches force vendor lock-in and rely on injecting large volumes of raw conversation into prompts, leading to high token costs and degraded performance.   We introduce Memori, an LLM-agnostic persistent memory layer that treats memory as a data structuring problem. Its Advanced Augmentation pipeline converts unstructured dialogue into compact semantic triples and conversation summaries, enabling precise retrieval and coherent reasoning.   Evaluated on the LoCoMo benchmark, Memori achieves 81.95% accuracy, outperforming existing memory systems while using only 1,294 tokens per query (~5% of full context). This results in substantial cost reductions, including 67% fewer tokens than competing approaches and over 20x savings compared to full-context methods.   These results show that effective memory in LLM agents depends on structured representations instead of larger context windows, enabling scalable and cost-efficient deployment.

---

## 论文详细总结（自动生成）

这是一份关于论文《Memori: A Persistent Memory Layer for Efficient, Context-Aware LLM Agents》的结构化深入总结：

### 1. 核心问题与研究动机
*   **核心问题**：LLM 智能体在处理长期、跨会话交互时，缺乏高效的持久化记忆机制。
*   **研究背景**：
    *   **上下文腐烂（Context Rot）**：直接将原始对话历史注入 Prompt 会导致上下文窗口迅速膨胀，模型容易忽略关键信息或产生不一致输出。
    *   **成本高昂**：Token 消耗随对话增长呈指数级增加，导致 API 调用成本不可持续。
    *   **供应商锁定**：现有记忆方案往往绑定特定模型或平台。
*   **整体含义**：Memori 旨在通过将记忆从“存储问题”转变为“数据结构化问题”，提供一个与模型无关的中间层，实现低成本、高精度的长效记忆。

### 2. 方法论：高级增强流水线（Advanced Augmentation）
Memori 的核心思想是**“蒸馏与结构化”**，其关键技术流程如下：
*   **语义提取与三元组生成**：系统不存储原始句子，而是将对话拆解为原子知识单元，构建“主体-谓词-客体”（Subject-Predicate-Object）的语义三元组。这能过滤掉口语废话，保留高信号事实。
*   **对话摘要（Conversation Summarization）**：在生成三元组的同时，系统会生成高层级的对话摘要，捕捉用户的意图演变、任务背景和时间线逻辑。
*   **双层关联索引**：三元组（提供精确事实）与摘要（提供叙事背景）通过来源 ID 相互链接。
*   **混合检索机制**：结合了基于向量嵌入（Gemma-300 模型）的余弦相似度搜索和基于关键词的 BM25 匹配，确保检索的全面性。

### 3. 实验设计
*   **数据集/基准测试**：使用 **LoCoMo (Long Conversation Memory)** 基准测试。该测试专门设计用于评估智能体在多会话、长文本背景下的状态跟踪、时间推理和偏好提取能力。
*   **对比方法**：
    *   **Full-Context (Ceiling)**：将全部历史注入 Prompt（作为性能上限）。
    *   **主流记忆框架**：Zep、LangMem、Mem0。
*   **评估指标**：采用 **LLM-as-a-Judge** 模式，使用 GPT-4o-mini 作为裁判，评估 Single-hop（单跳）、Multi-hop（多跳）、Temporal（时间）和 Open-domain（开放域）四类推理任务的准确率。

### 4. 资源与算力
*   **模型使用**：实验中生成答案和作为裁判的模型均为 **GPT-4o-mini**。
*   **嵌入模型**：使用了 **Gemma-300** 嵌入模型。
*   **存储与检索**：使用 **FAISS** 进行本地向量索引和相似度搜索。
*   **算力说明**：文中**未明确说明**具体的 GPU 型号、数量或训练时长。由于 Memori 主要是推理侧的记忆层架构，其核心开销在于 API 调用和本地向量库维护，而非大规模模型训练。

### 5. 实验数量与充分性
*   **实验规模**：针对 LoCoMo 数据集中的 1,529 个问题进行了测试（涵盖多跳 282 个、时间 321 个、开放域 96 个、单跳 830 个）。
*   **充分性**：
    *   **重复实验**：Memori 的性能值取自 3 轮实验的平均值，并标注了标准差，具有一定的统计客观性。
    *   **对比维度**：不仅对比了准确率，还详细对比了 **Token 消耗量**和**经济成本**，这在工程应用研究中非常关键。
    *   **公平性**：排除了 LoCoMo 中的对抗性类别，以确保与其他已发表基准结果的可比性。

### 6. 主要结论与发现
*   **性能领先**：Memori 整体准确率达到 **81.95%**，在检索类系统中排名第一，显著优于 Mem0 (62.47%)，并超越了 Zep 和 LangMem。
*   **极致的成本效率**：Memori 每次查询平均仅添加 **1,294 个 Token**，仅占全上下文长度的 **4.97%**。
*   **经济优势**：相比全上下文方法节省了 **20 倍以上**的成本；相比 Zep 减少了 **67%** 的 Token 消耗，且准确率更高。
*   **结构化胜过规模**：实验证明，记忆的质量取决于其结构化程度，而非上下文窗口的大小。

### 7. 优点
*   **高信号/低噪声**：通过语义三元组极大地压缩了信息，解决了“迷失在中间（Lost in the middle）”的幻觉问题。
*   **解耦设计**：作为独立的 API 层，不依赖特定 LLM，具有极强的通用性。
*   **双重表示**：结合了“原子事实（三元组）”和“宏观背景（摘要）”，兼顾了精确度和连贯性。

### 8. 不足与局限
*   **时间推理仍有提升空间**：虽然 Memori 在时间推理上表现不错（80.37%），但仍略逊于 LangMem (86.92%)，说明单纯的三元组+摘要在处理复杂状态变更时仍有局限。
*   **开放域推理挑战**：在开放域任务中表现一般（63.54%），这可能是因为此类任务需要更广泛的原始文本支持，而过度压缩会导致信息丢失。
*   **实时性开销**：虽然文中未详述，但“高级增强流水线”涉及实时提取三元组和生成摘要，这会增加额外的处理延迟和初始处理成本。

（完）
