---
title: "ToolFlood: Beyond Selection -- Hiding Valid Tools from LLM Agents via Semantic Covering"
title_zh: ToolFlood：超越选择——通过语义覆盖从大语言模型智能体中隐藏有效工具
authors: "Hussein Jawad, Nicolas J-B Brunel"
date: 2026-03-14
pdf: "https://arxiv.org/pdf/2603.13950v1"
tags: ["query:agent"]
score: 9.0
evidence: 通过检索层语义覆盖攻击工具增强型LLM智能体
tldr: "本研究针对大模型智能体（LLM Agents）的工具检索阶段提出了名为ToolFlood的新型攻击方法。不同于以往针对工具选择的攻击，ToolFlood通过在嵌入空间中注入少量精心设计的对抗性工具，利用语义覆盖策略占据检索结果的前k个位置，从而将所有良性工具挤出智能体的上下文。该方法采用LLM生成多样化元数据并结合贪心算法优化覆盖率。实验表明，在仅注入1%工具的情况下，攻击成功率高达95%，揭示了现有工具检索机制的严重安全漏洞。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.13950v1/fig-001.webp\", \"caption\": \"Table 1. ToolFlood effectiveness on MetaTool and ToolBench (test split) using retrieval with text-embedding-3-small: Top-k Domination Rate (TDR) and Attack Success Rate (ASR), with average injection budget (Avg. B) and poisoning rate (Avg. PR); ASR reported for selector LLMs (GPT-4o, GPT-5-mini, GPT-4o-mini).\", \"page\": 7, \"index\": 1, \"width\": 954, \"height\": 206}, {\"url\": \"assets/figures/arxiv/2603.13950v1/fig-002.webp\", \"caption\": \"Table 6. ToolBench tasks and example queries (from train/test sets).\", \"page\": 18, \"index\": 2, \"width\": 868, \"height\": 1074}, {\"url\": \"assets/figures/arxiv/2603.13950v1/fig-003.webp\", \"caption\": \"Figure 1. ASR vs. injection budget (B level) per task on ToolBench (test split). Higher B increases top-k domination opportunities, yielding higher ASR (retrieval: text-embedding-3-small, selector LLM: GPT-4o-mini).\", \"page\": 8, \"index\": 3, \"width\": 492, \"height\": 267}, {\"url\": \"assets/figures/arxiv/2603.13950v1/fig-004.webp\", \"caption\": \"Table 5. MetaTool tasks and example queries (from train/test sets).\", \"page\": 17, \"index\": 4, \"width\": 869, \"height\": 990}]"
motivation: 现有研究多关注工具选择阶段的攻击，而忽视了大规模工具库中检索层易受语义空间覆盖攻击的鲁棒性问题。
method: 采用两阶段策略，先利用LLM生成多样化工具描述，再通过迭代贪心算法选择能最大化覆盖用户查询嵌入空间的对抗性工具。
result: "在标准基准测试中，ToolFlood仅需极低的注入比例（如ToolBench中的1%）即可实现高达95%的攻击成功率。"
conclusion: 该研究证明了基于嵌入的工具检索系统存在严重的安全隐患，攻击者可以通过语义覆盖轻易屏蔽合法工具。
---

## 摘要
大语言模型 (LLM) 智能体越来越多地使用外部工具来处理复杂任务，并依赖基于嵌入的检索来选择一小部分 top-k 子集进行推理。随着这些系统的规模扩大，尽管之前的研究已经探讨了针对工具选择的攻击，但该检索阶段的鲁棒性尚未得到充分研究。本文介绍了 ToolFlood，这是一种针对工具增强型 LLM 智能体的检索层攻击。ToolFlood 并非在检索后改变所选工具，而是通过注入少量受攻击者控制的工具来使检索过程本身过载；这些工具的元数据通过利用嵌入空间的几何特性进行了精心布置。这些工具在语义上覆盖了许多用户查询，占据了 top-k 结果的主导地位，并将所有良性工具挤出了智能体的上下文。ToolFlood 采用了一种两阶段的对抗性工具生成策略：首先对目标查询的子集进行采样，并利用 LLM 迭代生成多样化的工具名称和描述；随后运行迭代贪婪选择，在余弦距离阈值下选择能最大化覆盖嵌入空间中剩余查询的工具，直到所有查询都被覆盖或达到预算上限。我们提供了检索饱和度的理论分析，并在标准基准测试上表明，ToolFlood 在较低的注入率下（在 ToolBench 中为 1%）可实现高达 95% 的攻击成功率。代码将在以下链接公开：https://github.com/as1-prog/ToolFlood

## Abstract
Large Language Model (LLM) agents increasingly use external tools for complex tasks and rely on embedding-based retrieval to select a small top-k subset for reasoning. As these systems scale, the robustness of this retrieval stage is underexplored, even though prior work has examined attacks on tool selection. This paper introduces ToolFlood, a retrieval-layer attack on tool-augmented LLM agents. Rather than altering which tool is chosen after retrieval, ToolFlood overwhelms retrieval itself by injecting a few attacker-controlled tools whose metadata is carefully placed by exploiting the geometry of embedding space. These tools semantically span many user queries, dominate the top-k results, and push all benign tools out of the agent's context.   ToolFlood uses a two-phase adversarial tool generation strategy. It first samples subsets of target queries and uses an LLM to iteratively generate diverse tool names and descriptions. It then runs an iterative greedy selection that chooses tools maximizing coverage of remaining queries in embedding space under a cosine-distance threshold, stopping when all queries are covered or a budget is reached. We provide theoretical analysis of retrieval saturation and show on standard benchmarks that ToolFlood achieves up to a 95% attack success rate with a low injection rate (1% in ToolBench). The code will be made publicly available at the following link: https://github.com/as1-prog/ToolFlood

---

## 论文详细总结（自动生成）

### 论文总结：ToolFlood — 通过语义覆盖在 LLM 智能体中隐藏有效工具

#### 1. 核心问题与整体含义（研究动机和背景）
随着大语言模型（LLM）智能体生态系统的扩张，系统通常依赖**检索层（Retrieval Layer）**从海量工具库中筛选出 Top-k 个相关工具放入上下文。
*   **研究动机**：先前的安全研究主要集中在“工具选择”阶段（即工具已被检索到，如何诱导模型误选），而忽视了检索阶段的脆弱性。
*   **核心问题**：攻击者能否通过注入少量精心设计的“伪装工具”，在检索阶段就占据所有的 Top-k 位置，从而将所有合法的良性工具彻底挤出智能体的视线？这种攻击被称为 **Top-k 占据（Top-k Domination）**，本质上是一种针对工具可见性的拒绝服务攻击（DoS）。

#### 2. 方法论：核心思想与算法流程
ToolFlood 提出了一种基于嵌入空间几何特性的两阶段攻击策略：
*   **核心思想**：利用语义覆盖（Semantic Covering）逻辑，生成在嵌入空间中位于大量潜在查询“中心”的对抗性工具元数据。
*   **关键技术细节**：
    1.  **第一阶段：蒙特卡洛候选生成（Phase 1）**：
        *   从目标查询集中随机采样小规模子集。
        *   利用攻击者控制的 LLM（如 GPT-4o-mini）为这些子集生成通用的工具名称和描述，使其在语义上能同时匹配多个查询。
    2.  **第二阶段：迭代贪心语义多重覆盖（Phase 2）**：
        *   将问题建模为“预算约束下的最大覆盖问题”。
        *   使用代理嵌入模型（Shadow Retriever）计算候选工具与查询的余弦距离。
        *   通过贪心算法，每一轮选择能覆盖最多“未饱和”查询（即被对抗工具占据位置不足 $k$ 个的查询）的工具，直到达到注入预算。
*   **理论保证**：论文证明了随着迭代次数增加，该方法能以极高概率产生足以覆盖整个目标查询空间的对抗工具集。

#### 3. 实验设计
*   **数据集/场景**：
    *   **MetaTool**：包含 199 个良性工具的小型库。
    *   **ToolBench**：包含 11,760 个良性工具的大型库（更接近真实规模）。
*   **Benchmark 与任务**：定义了 10 个目标任务（如空间图像、金融分析、健身计划等），每个任务包含 100 个生成查询和 50 个测试查询。
*   **对比方法**：
    *   **Random-Sybil Injection**：随机注入符合格式但不经过优化的伪造工具。
    *   **PoisonedRAG (适配版)**：将针对 RAG 系统的检索中毒攻击迁移至工具检索场景。
*   **评估指标**：Top-k 占据率（TDR）和最终攻击成功率（ASR）。

#### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的硬件配置（如 GPU 型号或数量）。
*   **计算开销分析**：主要的计算成本在于：
    1.  调用 LLM API（如 GPT-4o-mini）生成候选工具元数据。
    2.  计算嵌入向量（使用 OpenAI 或 Sentence-Transformers 接口）。
    3.  执行贪心筛选算法（属于轻量级 CPU 运算）。
    由于不涉及大规模模型微调，该攻击的算力门槛较低，具有很强的实用性。

#### 5. 实验数量与充分性
*   **实验规模**：
    *   涵盖了从百级到万级不同规模的工具库。
    *   测试了多种检索器（OpenAI `text-embedding-3-small/large`、`all-MiniLM-L6-v2`）之间的**迁移性（Transferability）**。
    *   评估了多种选择器 LLM（GPT-4o, GPT-4o-mini 等）。
*   **客观性与公平性**：采用了“无污染”拆分（训练集用于生成攻击，测试集用于评估），并对比了现有的检索层防御手段（如 MMR 重排序、Llama Prompt Guard 过滤）。实验设计较为严谨，能够客观反映检索层的安全风险。

#### 6. 主要结论与发现
*   **极高成功率**：在 ToolBench 中，仅需注入约 **1%** 的对抗工具，即可实现超过 **95%** 的攻击成功率。
*   **检索层脆弱性**：一旦 Top-k 被占据，下游的所有防御（如提示词过滤、输出检查）都会失效，因为良性工具根本没有进入上下文。
*   **黑盒迁移性强**：即使攻击者不知道目标系统使用的具体嵌入模型，利用开源模型作为代理进行优化，依然能保持很高的攻击效力。
*   **现有防御乏力**：传统的重排序（MMR）虽能降低占据率，但无法完全阻止攻击；而专门针对提示词注入的防御（Prompt Guard）对这种语义层面的“淹没”攻击几乎无效。

#### 7. 优点（亮点）
*   **视角独特**：将攻击目标从“诱导选择”提升到“控制可见性”，揭示了智能体架构中检索层的系统性风险。
*   **效率极高**：通过贪心优化，攻击者可以用极少的注入成本实现大范围的语义覆盖。
*   **模型无关性**：攻击不依赖于特定 LLM 的权重，具有普适性。

#### 8. 不足与局限
*   **注入权限假设**：攻击前提是攻击者有权向工具库（如 GPT Store 或 LangChain Hub）上传工具。在审核极严的私有库中，实施难度较大。
*   **静态检索假设**：实验主要针对基于向量相似度的密集检索。如果系统采用混合检索（如结合 BM25 或基于规则的硬过滤），攻击效果可能会打折扣。
*   **防御探索有限**：虽然测试了 MMR，但尚未探讨更复杂的防御（如基于用户反馈的信誉系统或实时异常流量监测）。

（完）
