---
title: "ALARA for Agents: Least-Privilege Context Engineering Through Portable Composable Multi-Agent Teams"
title_zh: 智能体的 ALARA 原则：通过可移植、可组合的多智能体团队实现最小特权上下文工程
authors: "Christopher J. Agostino, Nayan D'Souza"
date: 2026-03-20
pdf: "https://arxiv.org/pdf/2603.20380v1"
tags: ["query:agent"]
score: 9.0
evidence: 便携式可组合多智能体团队及统一管理框架
tldr: 本研究针对多智能体系统中配置碎片化和权限管理混乱的问题，借鉴辐射防护的ALARA原则，提出了最小权限上下文工程方法。通过引入声明式的上下文-智能体-工具（CAT）数据层及配套的命令行工具npcsh，实现了对智能体工具访问和上下文的结构化管理。该方法不仅提高了配置的可移植性和可维护性，还通过强制性的行为约束确保了智能体操作的确定性。实验评估了22个本地模型在多种复杂任务中的表现，为构建安全高效的协作式智能体基础设施提供了重要参考。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.20380v1/fig-001.webp\", \"caption\": \"Fig. 4. Mean tool calls per task versus agency score (r ≈ 0.7).\", \"page\": 5, \"index\": 1, \"width\": 506, \"height\": 408}, {\"url\": \"assets/figures/arxiv/2603.20380v1/fig-002.webp\", \"caption\": \"Fig. 5. First-attempt pass rate (green) and additional gain from retries (orange) by task category.\", \"page\": 5, \"index\": 2, \"width\": 508, \"height\": 373}, {\"url\": \"assets/figures/arxiv/2603.20380v1/fig-003.webp\", \"caption\": \"Fig. 3. Agency (npcsh score) versus intelligence (MMLU). The regression line (r ≈ 0.8) with 1σ band; outliers beyond 1σ are labeled.\", \"page\": 5, \"index\": 3, \"width\": 497, \"height\": 406}, {\"url\": \"assets/figures/arxiv/2603.20380v1/fig-004.webp\", \"caption\": \"TABLE I Benchmark results across model families.\", \"page\": 4, \"index\": 4, \"width\": 438, \"height\": 533}, {\"url\": \"assets/figures/arxiv/2603.20380v1/fig-005.webp\", \"caption\": \"Fig. 2. Benchmark score versus parameter count across model families.\", \"page\": 4, \"index\": 5, \"width\": 507, \"height\": 311}, {\"url\": \"assets/figures/arxiv/2603.20380v1/fig-006.webp\", \"caption\": \"Fig. 6. Pass rate by model and task category. Models ordered by overall score (left to right); categories by mean difficulty (top to bottom).\", \"page\": 6, \"index\": 6, \"width\": 1044, \"height\": 532}, {\"url\": \"assets/figures/arxiv/2603.20380v1/fig-007.webp\", \"caption\": \"Fig. 1. The CAT data layer. Left: context files (orange) scope teams of NPCs (blue). Right: Jinxes (green) compose as a DAG rooted at python. Level-1 Jinxes (chat, sh, web search, screenshot) each use python as their engine. Level-2 Jinxes combine level1 steps: react chains chat + python, computer use chains chat + screenshot + sh, and delegate uses chat + sh. Shared parents (e.g. chat used by all three composites) make the graph a DAG rather than a tree. Dashed arrows show tool-catalog assignment to NPCs.\", \"page\": 2, \"index\": 7, \"width\": 515, \"height\": 254}]"
motivation: 现有的多智能体框架在工具访问和上下文管理上缺乏统一机制，导致配置碎片化且难以在团队间共享和维护。
method: 提出一种声明式的上下文-智能体-工具（CAT）数据层，并开发命令行工具npcsh，通过结构化文件强制执行最小权限原则。
result: 在22个本地模型上针对115项实际任务进行了约2500次测试，详细刻画了不同规模模型在文件操作、搜索及协作等方面的能力边界。
conclusion: 该系统通过结构化约束确保了智能体行为的确定性，为构建安全、可组合且易于协作的多智能体系统提供了有效路径。
---

## 摘要
工业界从业者和学术研究人员经常使用多智能体系统来加速工作，然而，这些系统运行的框架并未提供一种简单、统一的机制来可扩展地管理智能体控制框架（harness）的关键方面，这既影响了单个人机交互的质量，也影响了从业者通过共享智能体基础设施协作达成共同目标的能力。虽然智能体框架已经实现了日益复杂的多智能体系统，但定义这些智能体行为的行为规范仍然分散在散文式指令文件、框架内部配置以及像 MCP 服务器这样独立于单个智能体定义的机制中，使得这些规范难以在团队和项目之间共享、进行版本控制或协同维护。我们将辐射防护中的 ALARA 原则（暴露量保持在合理达到的最低水平）应用于智能体上下文，引入了一个通过相互关联的文件表达的声明式“上下文-智能体-工具”（CAT）数据层，将每个智能体的工具访问权限和上下文限制在其角色所需的最低限度，并开发了用于执行该系统的命令行 shell npcsh。由于系统以结构化方式解析并强制执行这些文件，修改智能体的工具列表会产生确定的行为变化，而非模型可能遵循也可能不遵循的建议。我们在涵盖文件操作、网络搜索、多步脚本编写、工具链调用和多智能体委派的 115 项实际任务中，评估了 22 个参数量从 6 亿到 350 亿的本地托管模型，通过约 2500 次总执行量，刻画了哪些模型系列在哪些任务类别中取得成功，以及它们在何处失效。

## Abstract
Industry practitioners and academic researchers regularly use multi-agent systems to accelerate their work, yet the frameworks through which these systems operate do not provide a simple, unified mechanism for scalably managing the critical aspects of the agent harness, impacting both the quality of individual human-agent interactions and the capacity for practitioners to coordinate toward common goals through shared agent infrastructure. Agent frameworks have enabled increasingly sophisticated multi-agent systems, but the behavioral specifications that define what these agents can do remain fragmented across prose instruction files, framework-internal configuration, and mechanisms like MCP servers that operate separately from individual agent definitions, making these specifications difficult to share, version, or collaboratively maintain across teams and projects. Applying the ALARA principle from radiation safety (exposures kept as low as reasonably achievable) to agent context, we introduce a declarative context-agent-tool (CAT) data layer expressed through interrelated files that scope each agent's tool access and context to the minimum its role requires, and \texttt{npcsh}, a command-line shell for executing it. Because the system parses and enforces these files structurally, modifying an agent's tool list produces a guaranteed behavioral change rather than a suggestion the model may or may not follow. We evaluate 22 locally-hosted models from 0.6B to 35B parameters across 115 practical tasks spanning file operations, web search, multi-step scripting, tool chaining, and multi-agent delegation, characterizing which model families succeed at which task categories and where they break down across $\sim$2500 total executions.

---

## 论文详细总结（自动生成）

这篇论文提出了一种名为 **ALARA for Agents** 的框架，旨在通过声明式的“最小特权”原则解决多智能体系统（MAS）中配置碎片化、安全风险及性能退化的问题。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：当前多智能体系统的行为规范（如工具调用权限、协作逻辑）散落在自然语言指令（如 `.cursorrules`）、框架代码（如 LangGraph）和外部协议（如 MCP）中。这种**碎片化**导致：
    *   **不可靠性**：模型对自然语言指令的遵循具有随机性，且长上下文会稀释指令强度。
    *   **安全风险**：过度授权（Over-privileged）使得智能体易受提示词注入攻击。
    *   **维护困难**：配置难以在团队间共享、版本化或跨框架移植。
*   **研究动机**：借鉴核辐射防护中的 **ALARA 原则**（As Low As Reasonably Achievable，保持在合理达到的最低水平），将其应用于智能体的上下文工程，通过结构化约束而非散文式建议来定义智能体权限。

### 2. 核心方法论：CAT 数据层
论文引入了声明式的 **CAT（Context-Agent-Tool）** 数据层，通过三种相互关联的文件类型实现：
*   **Context（上下文文件）**：定义团队层级的范围，指定编排者（Orchestrator）和子团队结构，实现层级化管理。
*   **NPC（智能体定义文件）**：定义单个智能体的模型配置、指令以及 **Jinx 列表**。Jinx 列表既是工具目录也是权限集，未列出的工具在模型 Schema 中根本不存在，从而实现硬件级的“最小特权”。
*   **Jinxes（执行模板）**：基于 YAML 的 Jinja 模板，定义工具的输入、描述和执行步骤（支持 Python、Bash、LLM 或嵌套其他 Jinx）。
    *   **核心思想**：将复杂的 ReAct 循环分解为确定性的骨架，减少模型的解释负担。
    *   **npcsh**：一个配套的命令行 Shell，用于解析并执行这些声明式文件，确保修改工具列表能产生确定的行为变化。

### 3. 实验设计与 Benchmark
*   **数据集/场景**：设计了一个包含 **115 项实际任务** 的 Benchmark，涵盖 13 个类别：文件操作、Shell 命令、系统任务、Web 搜索、Git、文本处理、工具链、多步推理、Python、数据处理、调试、脚本编写和**智能体委派（Delegation）**。
*   **对比方法**：评估了 **22 个本地托管模型**，参数量跨度从 0.6B 到 35B，涵盖 Qwen3/3.5、GLM、Gemma3、Llama 3.1/3.2、Mistral、Phi-4、OLMo2 等 9 个模型系列。
*   **评估指标**：任务成功率（通过文件系统状态验证）、尝试次数、工具调用次数、任务耗时。

### 4. 资源与算力
*   **硬件环境**：所有模型均通过 **Ollama** 在**消费级硬件**上运行。
*   **具体配置**：文中未明确给出具体的 GPU 型号（如 RTX 4090 等），但强调了是本地运行。
*   **实验规模**：总计进行了约 **2,530 次任务执行**（115 任务 × 22 模型），每个任务限时 360 秒，最多重试 5 次。

### 5. 实验数量与充分性
*   **实验充分性**：实验覆盖了从极小规模（0.6B）到中等规模（35B）的广泛模型，且针对每个任务都有详细的 Trace 追踪（重试、工具调用等），数据量较为充足。
*   **客观性与公平性**：使用了统一的框架（npcsh）和验证脚本，确保了不同模型在相同“脚手架”下的公平竞争。通过对比云端模型（GPT-4o 等）超过 90% 的得分，证明了 Benchmark 的有效性（即失败主要是模型能力问题而非框架限制）。

### 6. 主要结论与发现
*   **训练胜过规模**：模型系列间的差异（Between-family）远大于参数规模带来的差异。专门针对工具调用训练的 4B 模型表现甚至优于未优化的 27B 模型。
*   **能力相关性**：智能体表现与 MMLU 得分强相关 ($r \approx 0.8$)，但存在显著离群值。
*   **工具调用是关键**：工具调用量是性能的最强预测因子 ($r \approx 0.7$)，优于任务时长或重试次数。
*   **重试的边际效应**：约 80% 的成功发生在首次尝试。重试对 Web 搜索有效（提升 21%），但对**委派任务**几乎无效（仅提升 3%），因为累积的错误上下文会干扰模型。
*   **委派最难**：多智能体委派是所有模型表现最差的类别，反映了当前小模型在复杂协作逻辑上的短板。

### 7. 优点与亮点
*   **架构创新**：将安全领域的“最小特权”原则转化为可执行的声明式文件系统，解决了提示词指令的不确定性。
*   **高可移植性**：CAT 数据层不依赖特定框架状态，可直接在 Shell、API、IDE 或 Python 中复用。
*   **深度分析**：不仅给出了分数，还深入分析了重试机制、工具调用频率与任务类别的关系，对实际工程有指导意义。

### 8. 不足与局限
*   **模型覆盖**：主要集中在本地小模型，虽然对比了云端模型，但缺乏对闭源模型在相同框架下的详细 Trace 分析。
*   **任务复杂度上限**：虽然包含多步任务，但对于极长链路或超大规模协作的压力测试尚不充分。
*   **硬件细节缺失**：未提供具体的推理延迟数据或显存占用情况，对于追求极致性能的开发者参考受限。
*   **委派失败归因**：论文指出委派任务失败率高，但未能完全区分是模型推理能力不足，还是 Jinx 模板在处理复杂反馈循环时仍显僵硬。

（完）
