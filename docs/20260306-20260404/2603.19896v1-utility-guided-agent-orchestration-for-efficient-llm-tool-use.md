---
title: Utility-Guided Agent Orchestration for Efficient LLM Tool Use
title_zh: 效用引导的智能体编排：实现高效的大语言模型工具使用
authors: "Boyan Liu, Gongming Zhao, Hongli Xu"
date: 2026-03-20
pdf: "https://arxiv.org/pdf/2603.19896v1"
tags: ["query:agent"]
score: 9.0
evidence: 高效LLM工具调用的效用导向编排
tldr: 针对大语言模型智能体在工具使用中面临的质量与成本权衡难题，本文提出了一种效用引导的编排策略。该方法将智能体决策视为显式的优化问题，通过平衡预期收益、步骤成本、不确定性和冗余度，在响应、检索、工具调用等动作间进行动态选择。实验证明，该框架能有效控制智能体行为，在保证任务质量的同时显著优化资源消耗，为研究工具使用中的质量-成本权衡提供了可控且可分析的机制。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.19896v1/fig-001.webp\", \"caption\": \"Fig. 5. Relationship between heuristic signals and continuation behavior. Continue-rate is much lower in low expected-gain buckets and substantially higher in mid/high-gain buckets, supporting the interpretation of these signals as decision heuristics rather than decorative outputs.\", \"page\": 11, \"index\": 1, \"width\": 747, \"height\": 326}, {\"url\": \"assets/figures/arxiv/2603.19896v1/fig-002.webp\", \"caption\": \"Fig. 6. Policy ablation results. Removing gain-, uncertainty-, redundancy-, or stop-related control generally pushes the agent toward a less favorable quality–cost regime.\", \"page\": 11, \"index\": 2, \"width\": 646, \"height\": 406}, {\"url\": \"assets/figures/arxiv/2603.19896v1/fig-003.webp\", \"caption\": \"Fig. 1. Overview of the proposed utility-guided agent orchestration framework. At each step, the agent constructs a state representation from the current query, interaction history, and tool observations. A utility scorer evaluates candidate actions using estimated gain, step cost, uncertainty, and redundancy, and the action selector chooses the highest-utility action. The process iterates until a stopping condition is met.\", \"page\": 2, \"index\": 3, \"width\": 576, \"height\": 669}, {\"url\": \"assets/figures/arxiv/2603.19896v1/fig-004.webp\", \"caption\": \"Fig. 2. Main quality–cost trade-off measured by F1 versus tokens on the shared HotpotQA sample.\", \"page\": 8, \"index\": 4, \"width\": 662, \"height\": 452}, {\"url\": \"assets/figures/arxiv/2603.19896v1/fig-005.webp\", \"caption\": \"Fig. 3. Main quality–cost trade-off measured by F1 versus wall-clock time. This complements Figure 2 by showing the latency-side frontier.\", \"page\": 8, \"index\": 5, \"width\": 663, \"height\": 459}, {\"url\": \"assets/figures/arxiv/2603.19896v1/fig-006.webp\", \"caption\": \"Fig. 4. Effect of reasoning depth on answer quality and cost. Increasing the maximum number of reasoning steps improves F1 at first, but also increases token usage and latency, illustrating diminishing marginal returns.\", \"page\": 9, \"index\": 6, \"width\": 706, \"height\": 544}]"
motivation: 现有的智能体工作流在固定流程的僵化与自由推理的高昂成本（如延迟和Token消耗）之间难以取得平衡。
method: 提出一种效用引导的编排策略，通过量化评估增益、成本、不确定性和冗余来显式决定智能体的下一步动作。
result: 实验表明，该策略在多种任务中均能通过显式的编排信号有效调节智能体行为，实现比ReAct等传统方法更优的质量-成本权衡。
conclusion: 轻量级的效用设计为大模型智能体的行为控制提供了一种实用且可分析的机制，显著增强了工具使用过程的可控性与效率。
---

## 摘要
使用工具的大语言模型（LLM）智能体通常在回答质量与执行成本之间面临根本性的权衡。固定工作流虽然稳定但缺乏灵活性，而诸如 ReAct 等自由形式的多步推理方法虽然可能提升任务性能，但代价是过多的工具调用、更长的执行轨迹、更高的 Token 消耗以及增加的延迟。在本文中，我们将智能体编排视为一个显式的决策问题，而非完全依赖于提示词层面的行为。我们提出了一种效用引导的编排策略，通过平衡预估收益、步骤成本、不确定性和冗余，在响应、检索、工具调用、验证和停止等动作中进行选择。我们的目标并非追求绝对最优的任务性能，而是提供一个可控且可分析的策略框架，用于研究工具使用型 LLM 智能体在质量与成本之间的权衡。在直接回答、阈值控制、固定工作流、ReAct 及多种策略变体上的实验表明，显式的编排信号会显著影响智能体行为。对成本定义、工作流公平性和冗余控制的进一步分析表明，轻量级的效用设计可以为智能体控制提供一种合理且实用的机制。

## Abstract
Tool-using large language model (LLM) agents often face a fundamental tension between answer quality and execution cost. Fixed workflows are stable but inflexible, while free-form multi-step reasoning methods such as ReAct may improve task performance at the expense of excessive tool calls, longer trajectories, higher token consumption, and increased latency. In this paper, we study agent orchestration as an explicit decision problem rather than leaving it entirely to prompt-level behavior. We propose a utility-guided orchestration policy that selects among actions such as respond, retrieve, tool call, verify, and stop by balancing estimated gain, step cost, uncertainty, and redundancy. Our goal is not to claim universally best task performance, but to provide a controllable and analyzable policy framework for studying quality-cost trade-offs in tool-using LLM agents. Experiments across direct answering, threshold control, fixed workflows, ReAct, and several policy variants show that explicit orchestration signals substantially affect agent behavior. Additional analyses on cost definitions, workflow fairness, and redundancy control further demonstrate that lightweight utility design can provide a defensible and practical mechanism for agent control.

---

## 论文详细总结（自动生成）

### 论文结构化深入总结：效用引导的智能体编排（Utility-Guided Agent Orchestration）

#### 1. 论文的核心问题与整体含义（研究动机和背景）
在利用大语言模型（LLM）构建智能体（Agent）时，开发者面临一个核心矛盾：**回答质量与执行成本之间的权衡**。
*   **背景**：现有的智能体运行模式主要分为两类：一是**固定工作流**，虽然成本可控但过于僵化，无法应对复杂多变的中间证据；二是**自由形式推理**（如 ReAct），虽然灵活且质量高，但往往导致过多的工具调用、冗余的推理步骤、高昂的 Token 消耗和显著的延迟。
*   **核心问题**：如何将智能体的“编排（Orchestration）”从隐含的提示词行为转变为显式的决策问题？即智能体不仅要学会“如何使用工具”，更要学会“何时使用工具”以及“何时停止”。

#### 2. 论文提出的方法论
论文提出了一种**效用引导的编排框架**，将智能体的每一步动作选择建模为一个显式的优化问题。
*   **核心思想**：在每个决策步骤 $t$，智能体根据当前状态 $s_t$ 从动作空间 $\mathcal{A} = \{respond, retrieve, tool\_call, verify, stop\}$ 中选择效用值最高的动作。
*   **效用函数公式**：
    $$U(a | s_t) = Gain(a | s_t) - \lambda_1 StepCost(a | s_t) - \lambda_2 Uncertainty(a | s_t) - \lambda_3 Redundancy(a | s_t)$$
*   **关键技术细节**：
    *   **预估增益 (Gain)**：由 LLM 自行评估采取该动作对最终回答质量的边际贡献。
    *   **步骤成本 (Step Cost)**：一个轻量级的归一化代理指标，用于抑制无意义的轨迹扩张。
    *   **不确定性 (Uncertainty)**：评估当前证据是否充足，高不确定性会触发更多检索或验证。
    *   **冗余度 (Redundancy)**：惩罚重复或语义相似的动作，以压缩执行轨迹。
*   **流程**：构建状态表示 $\rightarrow$ 计算各动作效用 $\rightarrow$ 选择最高效用动作 $\rightarrow$ 执行并更新状态 $\rightarrow$ 循环直至触发 `stop` 或达到预算上限。

#### 3. 实验设计
*   **数据集/场景**：使用了 **HotpotQA** 开发集的 200 个随机样本，这是一个需要多步检索和推理的知识密集型问答任务。
*   **Benchmark 与对比方法**：
    *   **Direct**：直接回答，不使用工具。
    *   **Fixed Workflows**：包括 minimal（单次检索）、search-twice（两次检索）、search-verify（检索后验证）等固定流程。
    *   **Threshold Control**：基于简单阈值的控制。
    *   **ReAct**：经典的“推理-动作-观察”循环。
    *   **Policy Variants**：本文提出的效用策略及其不同成本定义（Token成本、延迟成本）的变体。
*   **评估指标**：F1 分数（质量）、Token 消耗、墙钟时间（Wall Time）、效率得分（F1/Tokens）。

#### 4. 资源与算力
*   **模型使用**：实验主要基于 **Llama-3.1-8B** 局部部署模型进行。
*   **算力细节**：论文**未明确说明**具体的 GPU 型号、数量以及具体的训练/推理总时长。由于该方法侧重于推理侧的编排逻辑而非模型训练，其算力消耗主要体现在 LLM 的推理调用上。

#### 5. 实验数量与充分性
*   **实验规模**：论文进行了多维度的深入分析，包括：
    *   主实验（质量-成本帕累托前沿分析）。
    *   推理深度分析（探讨步骤增加带来的边际收益递减）。
    *   成本定义分析（对比步骤代理成本与真实 Token/延迟成本的一致性）。
    *   工作流公平性分析（通过增强固定工作流基准来确保对比的客观性）。
    *   消融实验（逐一移除效用组件以验证其必要性）。
*   **充分性评价**：实验设计较为**充分且客观**。作者不仅关注 F1 分数，还深入探讨了启发式信号（如 Gain 和 Uncertainty）与实际行为的相关性，证明了该框架的解释力。

#### 6. 主要结论与发现
*   **显式编排有效性**：效用引导的策略能提供比固定工作流更优的灵活性，并比 ReAct 提供更强的成本可控性。
*   **边际收益递减**：增加推理步骤初期能显著提升 F1，但随后 Token 和延迟成本会线性增长，而质量提升趋于平缓。
*   **启发式信号的价值**：虽然 Gain 和 Uncertainty 是 LLM 的自评信号（非校准概率），但它们在引导智能体停止或继续执行方面具有显著的统计相关性。
*   **冗余控制**：引入语义冗余惩罚能显著压缩 Token 消耗和工具调用次数，尽管在当前实现下会略微增加计算延迟。

#### 7. 优点（亮点）
*   **解耦控制与推理**：将“如何做”与“是否值得做”分离，使智能体行为更易于审计和调节。
*   **轻量级且可解释**：不需要复杂的强化学习训练，通过简单的效用公式即可实现对质量-成本权衡的精细控制。
*   **深入的系统级分析**：论文对成本代理、工作流公平性和冗余控制的讨论非常扎实，具有很强的工程参考价值。

#### 8. 不足与局限
*   **启发式信号的局限**：Gain 和 Uncertainty 依赖于 LLM 的自我评估，可能存在过度自信或评估不准的情况，缺乏校准。
*   **场景覆盖有限**：实验主要集中在 HotpotQA 问答任务，尚未在更复杂的多工具调用（如 API 组合、长期规划）场景中验证。
*   **延迟开销**：语义冗余检测和效用评分本身会带来额外的计算开销，在极度追求低延迟的场景下可能成为瓶颈。
*   **超参数敏感性**：效用函数中的权重系数（$\lambda$）可能需要针对不同任务进行手动调优。

（完）
