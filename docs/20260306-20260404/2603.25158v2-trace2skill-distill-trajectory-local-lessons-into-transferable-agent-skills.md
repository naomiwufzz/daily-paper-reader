---
title: "Trace2Skill: Distill Trajectory-Local Lessons into Transferable Agent Skills"
title_zh: Trace2Skill：将轨迹局部经验提炼为可迁移的智能体技能
authors: "Jingwei Ni, Yihao Liu, Xinpeng Liu, Yutao Sun, Mengyu Zhou, Pengyu Cheng, Dexin Wang, Xiaoxi Jiang, Guanjun Jiang"
date: 2026-03-26
pdf: "https://arxiv.org/pdf/2603.25158v2"
tags: ["query:agent"]
score: 9.0
evidence: 为大模型智能体提炼可迁移技能
tldr: 针对LLM智能体技能获取效率低且泛化性差的问题，本文提出Trace2Skill框架。该框架模拟专家行为，通过并行分析大量执行轨迹，提取局部经验并利用归纳推理将其层级化整合为统一的技能目录。Trace2Skill支持从零创建或深化现有技能，无需参数微调或外部检索。实验证明，该方法在多项复杂任务中显著优于基准，且生成的技能具有极强的跨模型迁移能力和分布外泛化性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.25158v2/fig-001.webp\", \"caption\": \"Table 6 | Agentic error analysis (+Error) vs. single-LLM-call error analysis (+Error LLM) across all Author–Mode combinations (%). Same-model cells are shaded; plain cells are cross-model transfer. Bold = better of the two conditions per cell.\", \"page\": 10, \"index\": 1, \"width\": 731, \"height\": 461}, {\"url\": \"assets/figures/arxiv/2603.25158v2/fig-002.webp\", \"caption\": \"Figure 2 | Overview of Trace2Skill’s three-stage pipeline. Stage 1: a frozen agent 𝜋𝜃 rolls out on the evolving set using an initial skill S0 (human-written or LLM-drafted), producing labeled trajectories T − (failures) and T + (successes). Stage 2: parallel error and success analysts independently processes individual traces and proposes skill patches. Stage 3: all patches are merged into a single consolidated update via inductive reasoning with programmatic conflict prevention, producing an evolved skill S∗ with improved performance and generalization.\", \"page\": 3, \"index\": 2, \"width\": 983, \"height\": 357}]"
motivation: 现有的自动化技能生成方法往往依赖浅层知识或过度拟合局部轨迹，导致生成的技能碎片化且缺乏通用性。
method: Trace2Skill通过并行分析多样化的执行轨迹，提取特定经验并利用归纳推理将其层级化地整合为统一、无冲突的技能目录。
result: 在电子表格、视觉问答和数学推理等领域表现优异，且生成的技能具有极强的跨模型规模迁移能力和分布外泛化性。
conclusion: 该研究证明了复杂的智能体经验可以被转化为高度可迁移的声明式技能，无需参数微调或外部检索即可显著提升模型性能。
---

## 摘要
为大语言模型（LLM）智能体配备特定领域技能对于解决复杂任务至关重要。然而，人工编写技能造成了严重的扩展性瓶颈。相反，自动化的技能生成往往产生脆弱或碎片化的结果，因为其要么依赖于浅层的参数化知识，要么顺序地过拟合于不可泛化的轨迹局部经验。为克服这一问题，我们推出了 Trace2Skill，这是一个模拟人类专家编写技能方式的框架：通过全面分析广泛的执行经验，然后将其提炼为单一且综合的指南。Trace2Skill 并非顺序地对单个轨迹做出反应，而是派遣一组并行的子智能体来分析多样化的执行池。它提取特定于轨迹的经验，并通过归纳推理将其分层整合进一个统一、无冲突的技能目录中。Trace2Skill 既支持深化现有的人工编写技能，也支持从零开始创建新技能。在电子表格、视觉问答（VisionQA）和数学推理等具有挑战性的领域进行的实验表明，Trace2Skill 相比于包括 Anthropic 官方 xlsx 技能在内的强基准模型有显著提升。至关重要的是，这种基于轨迹的演化不仅仅是记忆任务实例或模型特定的怪癖：演化出的技能可以跨 LLM 规模迁移，并能泛化到分布外（OOD）设置。例如，由 Qwen3.5-35B 在其自身轨迹上演化出的技能，在 WikiTableQuestions 任务上将 Qwen3.5-122B 智能体的表现提升了高达 57.65 个绝对百分点。最终，我们的结果表明，复杂的智能体经验可以被打包成高度可迁移的声明式技能——无需参数更新，无需外部检索模块，且可以使用参数量小至 35B 的开源模型。

## Abstract
Equipping Large Language Model (LLM) agents with domain-specific skills is critical for tackling complex tasks. Yet, manual authoring creates a severe scalability bottleneck. Conversely, automated skill generation often yields fragile or fragmented results because it either relies on shallow parametric knowledge or sequentially overfits to non-generalizable trajectory-local lessons. To overcome this, we introduce Trace2Skill, a framework that mirrors how human experts author skills: by holistically analyzing broad execution experience before distilling it into a single, comprehensive guide. Instead of reacting sequentially to individual trajectories, Trace2Skill dispatches a parallel fleet of sub-agents to analyze a diverse pool of executions. It extracts trajectory-specific lessons and hierarchically consolidates them into a unified, conflict-free skill directory via inductive reasoning. Trace2Skill supports both deepening existing human-written skills and creating new ones from scratch. Experiments in challenging domains, such as spreadsheet, VisionQA and math reasoning, show that Trace2Skill significantly improves upon strong baselines, including Anthropic's official xlsx skills. Crucially, this trajectory-grounded evolution does not merely memorize task instances or model-specific quirks: evolved skills transfer across LLM scales and generalize to OOD settings. For example, skills evolved by Qwen3.5-35B on its own trajectories improved a Qwen3.5-122B agent by up to 57.65 absolute percentage points on WikiTableQuestions. Ultimately, our results demonstrate that complex agent experience can be packaged into highly transferable, declarative skills -- requiring no parameter updates, no external retrieval modules, and utilizing open-source models as small as 35B parameters.

---

## 论文详细总结（自动生成）

以下是对论文《Trace2Skill: Distill Trajectory-Local Lessons into Transferable Agent Skills》的结构化深入总结：

### 1. 核心问题与整体含义
*   **研究动机**：虽然为大语言模型（LLM）智能体配备领域特定技能（如标准作业程序 SOP）至关重要，但人工编写技能效率低下，而现有的自动化方法（如在线顺序更新或基于检索的经验库）往往导致技能碎片化、过拟合于特定轨迹，且难以在不同规模的模型间迁移。
*   **核心问题**：如何将智能体在执行任务过程中产生的局部、具体的经验（轨迹），转化为全面、无冲突且具有高度可迁移性的声明式技能目录？
*   **整体含义**：论文提出了 **Trace2Skill** 框架，模拟人类专家“先观察大量案例，再总结通用指南”的行为模式，通过并行分析轨迹池并进行归纳推理，生成可跨模型、跨领域使用的技能。

### 2. 方法论
Trace2Skill 包含三个主要阶段：
1.  **轨迹生成（Trajectory Generation）**：使用初始技能（人工编写或 LLM 粗略生成的草案）运行智能体，收集成功（$T^+$）和失败（$T^-$）的执行轨迹。
2.  **并行多智能体补丁提议（Parallel Multi-Agent Patch Proposal）**：
    *   派遣一组并行的子智能体（分析师）独立处理每条轨迹。
    *   **成功分析师（$A^+$）**：识别有助于成功的通用行为模式。
    *   **错误分析师（$A^-$）**：采用 ReAct 式的智能体循环，通过检查文件、对比真值来诊断失败根因，并验证修复方案，从而提出针对性的技能补丁。
3.  **无冲突补丁整合（Conflict-Free Patch Consolidation）**：
    *   **层级化合并**：将所有补丁通过 LLM 进行层级化合并，利用归纳推理识别在多个补丁中重复出现的“普遍模式”。
    *   **程序化校验**：通过格式检查和冲突检测（如避免对同一行进行矛盾修改）确保最终技能目录的连贯性。
    *   **输出形式**：生成一个包含主文档（`SKILL.md`）和辅助资源（脚本、参考资料）的结构化目录。

### 3. 实验设计
*   **实验场景与数据集**：
    *   **电子表格处理**：SpreadsheetBench（验证集、Soft/Hard 分数）。
    *   **分布外（OOD）泛化**：WikiTableQuestions（将维基百科表格转为 Excel 格式）。
    *   **数学推理**：DAPO-Math, AIME 2026。
    *   **视觉问答（VQA）**：DocVQA。
*   **对比方法（Baselines）**：
    *   **No Skill**：无技能输入。
    *   **Human-Written**：Anthropic 官方提供的 Excel 技能。
    *   **Parametric**：仅靠 LLM 内部知识生成的技能草案。
    *   **ReasoningBank**：基于检索的经验存储方法。
    *   **顺序更新（Sequential Updates）**：模拟在线学习的逐条更新模式。
*   **跨模型评估**：测试由 Qwen3.5-35B 生成的技能在 Qwen3.5-122B 上的表现，反之亦然。

### 4. 资源与算力
*   **算力消耗**：
    *   **轨迹生成**：使用 122B 模型生成 200 条轨迹（每条 50+ 轮对话）仅需不到 2 个 GPU 小时。
    *   **整合效率**：在 8 卡 A800 节点上，并行整合仅需约 3 分钟，而顺序更新模式（Seq-B=1）需要 60 分钟。
*   **模型使用**：主要使用 Qwen3.5-35B-A3B 和 Qwen3.5-122B-A10B 开源模型，无需依赖闭源商业模型。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从电子表格到数学、视觉等多个异构领域，实验设计包含 3 个随机种子取平均值。
*   **充分性**：
    *   进行了详尽的消融实验，对比了“仅错误分析”、“仅成功分析”以及“结合分析”的效果。
    *   对比了智能体化分析（Agentic）与普通 LLM 分析的差异。
    *   跨模型规模的迁移实验有力证明了技能的通用性，而非简单的模型怪癖记忆。
*   **客观性**：使用了官方 Benchmark 评测标准，并对比了人类专家编写的强基准。

### 6. 主要结论与发现
*   **显著提升**：Trace2Skill 在所有任务中均显著优于基准。例如，35B 模型演化的技能使 122B 模型在 WikiTQ 上的表现提升了 57.65%。
*   **超强的迁移性**：小模型（35B）生成的技能往往能大幅提升大模型（122B）的性能，证明了经验可以被提炼为模型无关的声明式知识。
*   **并行优于顺序**：并行整合不仅效率高，且通过归纳推理过滤了偶然性噪声，比在线顺序编辑更稳健。
*   **声明式优于检索式**：单一、综合的技能文档比基于检索的经验库（ReasoningBank）效果更好，且无需额外的检索模块。

### 7. 优点
*   **模拟人类专家**：通过“整体分析”而非“局部反应”，避免了技能目录的碎片化和冗余。
*   **自包含与可迁移**：生成的技能是纯文本/脚本目录，不依赖特定模型参数或外部检索系统，易于分发。
*   **智能体化诊断**：错误分析师具备“复现与验证”能力，确保提炼出的教训是真实且有效的。
*   **开源友好**：证明了中等规模的开源模型（35B）即可完成高质量的技能演化任务。

### 8. 不足与局限
*   **因果归因困难**：目前难以精确量化技能目录中某一个特定补丁对最终性能提升的边际贡献。
*   **动态剪枝缺失**：缺乏在推理过程中动态追踪和修剪无效或干扰性技能片段的机制。
*   **视觉领域的局限**：实验发现 35B 模型在 DocVQA 任务上虽然执行能力尚可，但作为“分析师”总结技能的能力较弱，说明技能编写对模型的反思能力有更高要求。
*   **应用限制**：目前主要针对结构化或半结构化任务，对于极度开放或缺乏明确真值校验的任务，错误分析师的效用可能会打折扣。

（完）
